# Copyright (c) 2026 Nokia
# SPDX-License-Identifier: BSD-3-Clause
# =============================================================================
# pattern-language.yaml
# A Pattern Language for Declarative Agents
# =============================================================================
#
# WHAT THIS FILE IS
# -----------------
# This is a self-contained dictionary of the eleven design patterns for
# building declarative agents. It is written in the tradition of Christopher
# Alexander's "A Pattern Language" (1977): the patterns are not an unordered
# catalogue of tips but a *language*. Each pattern names a recurring field of
# forces and the configuration that resolves them, and each connects to the
# patterns above it (which create its context) and below it (which complete
# it). Read top to bottom, the connections form a grammar; assembled for a
# given system, the chosen patterns form a sentence.
#
# This file is INDEPENDENT of the monograph chapters. The essence of each
# pattern — its context, the forces in tension, and the solution — is captured
# here directly, and every external example carries a full citation resolved
# in the `bibliography` block at the foot of the file. Nothing here requires a
# chapter to be understood.
#
# A NOTE ON A PATTERN AS BOTH RECIPE AND CONSTRAINT
# -------------------------------------------------
# In Alexander's form a pattern is stated as an imperative ("therefore: do
# this") but it works by forbidding configurations that leave its forces
# unresolved. The `solution` field is the recipe; the `forces` field is the
# constraint. They are one thing seen from two sides: the prescription is the
# negative space of the constraint.
#
# A NOTE ON PROVENANCE (read this before citing the patterns as "proven")
# -----------------------------------------------------------------------
# These patterns were first extracted from a single reference implementation —
# a declarative coding-agent harness. A single system can reveal a pattern's
# forces but cannot, by itself, establish that the resolution recurs. To place
# each pattern on firmer ground, every entry lists external `examples` drawn
# from independent, shipped systems and prior literature where the same forces
# are resolved the same way. The `confidence` field records how strong that
# external corroboration is. The contribution of this work is therefore not the
# invention of these structures but their naming as a coherent language and
# their application to the agentic domain.
#
# HOW TO READ EACH ENTRY
# ----------------------
#   id            stable machine-readable key
#   number        position in the language (larger/earlier -> smaller/later)
#   name          human-readable pattern name
#   also_known_as prior names for the same structure
#   confidence    Alexandrian rank of belief that this is a true invariant:
#                   2  a deep invariant seen across many independent systems
#                   1  well-supported, but the specific form here is newer
#                   0  tentative; largely from the reference implementation
#   intent        one-sentence statement of what the pattern does
#   context       the situation (and larger patterns) that must exist first
#   problem       the headline tension, stated as a question of forces
#   applicability when to use the pattern, and when its cost is not justified
#   forces        the competing pressures the pattern must hold in balance
#                 (this is the CONSTRAINT side of the pattern)
#   solution      the imperative resolution (this is the RECIPE side)
#   implementation practical checks and construction rules that make the
#                 pattern executable without consulting the monograph
#   consequences  what the resolution buys (benefits) and costs (liabilities)
#   grammar       how this pattern connects to the others:
#                   within    larger patterns whose context this helps complete
#                   requires  patterns that must already be in place
#                   contains  smaller patterns that complete this one
#                   enables   patterns this one makes possible
#                   overlaps  patterns addressing a related force differently
#   syntax        a one-line "sentence" showing the pattern in combination
#   examples      independent occurrences, each cited to the bibliography;
#                 kind: external (recurrence evidence) | internal (reference impl)
#
# =============================================================================

title: A Pattern Language for Declarative Agents
version: 1
based_on: >
  Christopher Alexander, Sara Ishikawa, Murray Silverstein, "A Pattern
  Language: Towns, Buildings, Construction" (Oxford University Press, 1977).

declarative_agent:
  summary: >
    A declarative agent is an agent whose behaviour is expressed as data —
    a state machine, a set of tools, and a configuration — interpreted by a
    fixed engine, rather than as imperative logic hand-written for each agent.
    The engine is a generic binary that never changes; the agent is the data
    it runs. One binary runs N agents, and changing an agent's behaviour is an
    edit to data, not a code change that must pass through the build cycle.
  the_three_parts:
    state_machine: >
      The control flow, expressed as a transition table: states are execution
      phases, signals are typed tool outcomes, and each (state, signal) pair
      maps to a next state and a tool to dispatch. This is the agent's whole
      loop as data — observe, decide, act, validate — with no conditionals or
      loops hidden in code. Because it is finite data, it can be statically
      checked (reachability, determinism, completeness) before the agent runs.
    tools: >
      The agent's verbs, each declared as a typed contract: inputs, emittable
      signals, side effects, reversibility, and relationships. The machine
      dispatches tools by name; one tool crosses the model boundary and returns
      probabilistic results, the rest are deterministic. Tool availability,
      contracts, and bindings are configured; implementations remain ordinary
      code behind the contract.
    configuration: >
      The runtime binding: which model and provider to call, temperature and
      budget limits, which machine to run, and where the tool declarations
      live. Swapping the model or tightening a budget is a one-field edit that
      touches neither the engine nor the other layers.
  versus_imperative_logic: >
    In an imperative agent the loop is code — branches buried in callbacks,
    tangled with the rest of the system. A reviewer cannot trace every path
    through a loop that branches on every tool outcome, an unexpected output
    silently falls through, and every behaviour change is a code change that
    risks breaking unrelated logic and must pass the full development cycle.
    The declarative form makes the control flow a readable, diffable, and
    statically checkable artifact: an unhandled outcome is a load-time error,
    not a silent dead end, and a behaviour change is a configuration edit that
    can be deployed without rebuilding the binary. Imperative code does not
    vanish — it survives as the fixed engine, the tool implementations, and the
    port adapters — but agent-specific behaviour never lives there.
  relationship_to_patterns: >
    The declarative agent is the whole this language builds. Machine Interpreter
    (pattern 1) establishes the machine; Tool Contract (pattern 3) specifies
    the tools; Agent-as-Data (pattern 2) packages machine, tools, and
    configuration as one governable artifact; the remaining patterns refine
    what each layer exposes, reverses, observes, gates, classifies, and probes.
  canonical_example:
    machine: |
      name: generator
      initial_state: Composing
      terminal_states: [Succeeded, Failed]
      transitions:
        - { state: Composing,   signal: Seed,             next: Composing,   action: invoke_llm }
        - { state: Composing,   signal: LLMResponded,     next: Parsing,     action: parse_response }
        - { state: Composing,   signal: BudgetExceeded,   next: Failed }
        - { state: Parsing,     signal: ToolCall,         next: Dispatching, action: $tool }
        - { state: Parsing,     signal: Completion,       next: ValidatingBuild, action: build }
        - { state: Dispatching, signal: ToolDone,         next: Composing,   action: invoke_llm }
        - { state: Dispatching, signal: ToolFailed,       next: Composing,   action: invoke_llm }
        - { state: ValidatingBuild, signal: ToolDone,     next: ValidatingLint, action: lint }
        - { state: ValidatingBuild, signal: ToolFailed,   next: Composing,   action: invoke_llm }
        - { state: ValidatingLint,  signal: ToolDone,     next: ValidatingTest, action: test }
        - { state: ValidatingLint,  signal: ToolFailed,   next: Composing,   action: invoke_llm }
        - { state: ValidatingTest,  signal: ToolDone,     next: Succeeded }
        - { state: ValidatingTest,  signal: ToolFailed,   next: Composing,   action: invoke_llm }
    tools: |
      tools:
        - read
        - write
        - edit
        - invoke_llm
        - parse_response
        - done
        - build
        - lint
        - test
    profile: |
      name: generator
      machine: machine.yaml
      tools: [tools.yaml]
      tool_declarations: [llm/default.yaml]
      tool_config_dirs:
        - /opt/agent-core/tools/builtin/filesystem
        - /opt/agent-core/tools/exec/go
    validation_story: >
      If any explicit validation state's ToolFailed transition is removed, the
      engine rejects the machine at load time because the selected build, lint,
      or test word can emit ToolFailed where that signal is unhandled. This is
      the central promise: an unexpected outcome becomes a validation error
      before the model runs, not a production dead end.
  validation_binding: >
    validation_story appears only in this worked example; it is not a
    per-pattern field. Each pattern binds its claims to validation through its
    typed invariants list instead. Executable checks name the command that
    enforces the invariant or the issue that creates that command. Manual
    checks state why no mechanical verdict exists.

conventions:
  confidence_scale:
    "2": Deep invariant; the same force is resolved the same way across many independent systems and prior literature.
    "1": Well supported by prior art, but the specific agentic form presented here is more recent.
    "0": Tentative; corroboration is thin and the structure is largely from the single reference implementation.
  relationship_types:
    within: Larger patterns whose context this pattern helps complete.
    requires: Patterns that must already be in place for this one to hold.
    contains: Smaller patterns that complete or refine this one.
    enables: Patterns that this one makes possible.
    overlaps: Patterns that address a neighbouring force by a different means.

# =============================================================================
# THE PATTERNS
# =============================================================================

patterns:

  # ---------------------------------------------------------------------------
  - id: machine-interpreter
    number: 1
    name: Machine Interpreter
    also_known_as: [State Interpreter, Interpreter, Engine Loop, Runtime]
    confidence: 2
    intent: >
      Separate control flow from execution by expressing the agent loop as a
      transition table that a fixed engine interprets, so behaviour is data,
      not code.
    context: >
      Every agent runs the same loop — observe, decide, act, validate — and
      branches on the outcome of each step, including the unpredictable output
      of a probabilistic model. Written as imperative code, that loop hides its
      states in scattered conditionals and no reviewer can trace every path.
    problem: >
      How do you make an agent's control flow reviewable, statically
      checkable, and changeable without editing code, when some of its
      branches depend on outputs no one can predict?
    applicability: >
      Use this for workflows made of discrete operations that branch on typed
      outcomes: LLM agents, CI pipelines, evaluation harnesses, migration
      scripts, and any execution that must survive process boundaries or be
      audited. It is less useful for a straight-line script whose steps and
      outcomes never vary.
    forces:
      - An unexpected tool or model output must not silently fall through the code.
      - Behaviour must be changeable without rebuilding the binary.
      - The full set of reachable states must be knowable before the agent runs.
      - The model must be free to choose tools, yet only within declared bounds.
    solution: >
      Express the loop as a finite-state machine in a data file: states are
      execution phases, signals are typed tool outcomes, and (state, signal) ->
      (next_state, tool) is the transition table. A fixed engine interprets the
      table and dispatches tools by name. Every signal a tool can emit must
      appear in the table; an unhandled signal is a load-time error, not a
      silent dead end. Because the table is finite data, reachability,
      determinism, and completeness are checkable before the model is called.
    implementation: >
      The machine must load, serialize, and validate without executing code. The
      validator checks referenced states, terminal reachability, tool-name
      resolution, and that every signal emitted by every dispatchable tool is
      handled in that state. Tools are opaque to the machine; the tool never
      knows the machine state, and the machine never knows tool internals. The
      $tool slot may resolve a model-chosen tool at runtime, but dispatch is
      still validated against the declared registry and handled signal set.
    invariants:
      - id: P1.1
        statement: Every machine is finite, refers only to declared grammar, and has no unreachable or dead non-terminal state.
        check:
          kind: executable
          tier: compiler
          command: >-
            cd agent-core && go test -count=1 -v ./internal/runtime/core -run
            'TestParseMachineSpecValidationErrors|TestDiagnoseMachineSpecReportsReachabilityAndDeadGrammar'
          negative_test: TestParseMachineSpecValidationErrors
      - id: P1.2
        statement: Every signal emitted by a dispatchable tool is handled from the state that receives the tool result.
        check:
          kind: executable
          tier: compiler
          command: >-
            cd agent-core && go test -count=1 -v ./internal/tools/catalog -run
            TestValidateToolEmitsMissingFollowupTransition
          negative_test: TestValidateToolEmitsMissingFollowupTransition
    consequences:
      benefits:
        - Control flow is a readable, diffable artifact.
        - Unhandled outcomes are caught statically, before execution.
        - Behaviour changes are configuration edits, not code changes.
      liabilities:
        - The engine, tool implementations, and adapters remain compiled code.
        - Anything the schema cannot express requires new code, not new data.
    grammar:
      within: []
      requires: []
      contains: [agent-as-data, tool-contract, bidirectional-log, transition-spans, boundary-tool, approval-gate, convergence-taxonomy, operator-port]
      enables: [agent-as-data, tool-contract, phase-scoped-toolset, inference-boundary, bidirectional-log, transition-spans, boundary-tool, approval-gate, convergence-taxonomy, operator-port]
      overlaps: []
    syntax: >
      Begin here. Every other pattern in this language presupposes a Machine
      Interpreter; without an explicit machine there is nothing to scope,
      trace, roll back, gate, classify, or probe.
    examples:
      - system: Harel Statecharts
        cite: harel-statecharts-1987
        kind: external
        note: The visual FSM formalism establishing that complex reactive control flow is best expressed as an explicit, analysable machine.
      - system: XState
        cite: xstate-2024
        kind: external
        note: A widely adopted statechart interpreter for application control flow; demonstrates the interpreter-over-data structure in production.
      - system: BPMN engines
        cite: omg-bpmn-2011
        kind: external
        note: Business processes modelled as data and executed by a fixed engine; the same separation of flow-as-data from a generic runtime.
      - system: SCXML
        cite: w3c-scxml-2015
        kind: external
        note: A W3C executable state-machine notation; direct precedent for serializing reactive control flow as data interpreted by a conforming processor.
      - system: Workflow nets (Petri nets)
        cite: van-der-aalst-workflow-1998
        kind: external
        note: Formal grounding for representing workflow control flow as an analysable structure rather than procedural code.
      - system: TLA+
        cite: lamport-tla-2002
        kind: external
        note: System behaviour specified as states and actions with model checking over reachable behaviours — the formal end of pre-run machine analysis.
      - system: Translation validation
        cite: pnueli-translation-validation-1998
        kind: external
        note: Validates generated artifacts against source-level intent after translation; analogous to load-time checking that the machine faithfully covers declared tool outcomes.
      - system: Action Language
        cite: bultan-action-lang-2000
        kind: external
        note: A specification language for model checking reactive systems; close precedent for treating a transition table as analyzable behavioural data.
      - system: LangGraph
        cite: langgraph-2024
        kind: external
        note: Agent workflows as directed graphs of nodes; shows developers prefer declaring transitions over nesting conditionals, though its code-valued conditions resist static analysis.
      - system: StateFlow
        cite: wu-stateflow-2024
        kind: external
        note: Models task-solving as an explicit state machine and reports measurable gains over unstructured loops — empirical evidence that making the machine explicit improves agent performance.
      - system: AWS Step Functions
        cite: aws-step-functions-2024
        kind: external
        note: Serverless workflows declared in Amazon States Language and executed by a fixed service; a production-scale example of JSON state machines as operational control flow.
      - system: Donna
        cite: tiendil-donna-2025
        kind: external
        note: Markdown-defined workflows run as deterministic state machines for coding agents, with transitions validated before execution.
      - system: Jido
        cite: jido-2025
        kind: external
        note: An Elixir agent framework with explicit state transformations, signal routing, directives, an FSM execution strategy, and optional AI/LLM integration.
      - system: Lean4Agent
        cite: lean4agent-2026
        kind: external
        note: Formal verification of agent workflows in dependent type theory — the upper bound of the pattern's verifiability.
      - system: Redux
        cite: redux-2015
        kind: external
        note: UI state as a reducer over (state, action) -> state; the same table-driven transition discipline in front-end engineering.
      - system: Reference implementation — executor agent
        cite: reference-implementation
        kind: internal
        note: A coding agent whose whole control flow lives in machine.yaml; deleting one transition makes the engine reject the machine at load time.
        evidence:
          classification: shipped
          checks:
            - path: applications/catalog/agents/executor/machine.yaml
              artifact: machine
              assertion: yaml_fields
              fields: [initial_state, states, signals, transitions, terminal_states]
            - path: agent-core/internal/tools/catalog/emits_from_stl_test.go
              artifact: go_test
              assertion: go_test
              test: TestValidateToolEmitsMissingFollowupTransition
    provenance: [reference-implementation, external-corroboration]

  # ---------------------------------------------------------------------------
  - id: agent-as-data
    number: 2
    name: Agent-as-Data
    also_known_as: [Declarative Agent, Configuration-Driven Agent]
    confidence: 1
    intent: >
      Express the complete agent definition — machine, tools, and model binding
      — as data interpreted by a fixed binary, so an agent is a dataset the
      engine runs rather than behaviour buried in code.
    context: >
      A Machine Interpreter already turns control flow into data. But an agent is
      more than its loop: it is also a set of tool declarations and a model
      binding. When these are scattered across code, config, and deployment
      scripts, the agent cannot be reviewed, versioned, or governed as one unit.
    problem: >
      How do you run many different agents from one binary, and review, version,
      and validate each agent as a single governable artifact?
    applicability: >
      Use this when several agents should share one runtime while differing by
      machine, tool inventory, model binding, budget, or policy; when behaviour
      needs code-review-like governance as data; or when model swaps must be
      evaluated without changing the harness. It adds little when there is only
      one purpose-built program and no expected variation.
    forces:
      - Different agents (coding, evaluation, benchmarking, spec validation) should share one runtime.
      - The whole agent should be reviewable and versionable as data.
      - The model should be swappable to isolate its contribution.
      - The entire definition should be validated before deployment.
    solution: >
      Define the agent as three data layers — a machine (control flow), tool
      declarations (capabilities and contracts), and a profile (model binding,
      budgets, tool roots) — and have one compiled interpreter load, cross-
      validate, and run any definition that conforms to the schema. One binary,
      N agents; deploying a new agent means writing data, not compiling code.
    implementation: >
      Keep agent-specific behaviour out of the binary. The profile names the
      machine, tool declaration roots, model/provider configuration, budgets,
      and composition overlays. At load time, cross-validate schema shape,
      machine transitions, tool contracts, scoped visibility, and model binding
      before any tool or model is invoked. Registry factories and adapters may
      be compiled code, but they provide capability rather than per-agent policy.
    invariants:
      - id: P2.1
        statement: Every profile resolves a complete machine, tool selection, declaration, and configuration closure before execution.
        check:
          kind: executable
          tier: construction
          command: >-
            cd agent-core && go test -count=1 -v ./internal/tools/catalog -run
            'TestLoadProfile_MissingMachine|TestBuildProgramRefRejectsDeclarationIncludeCycle|TestBuildProgramRefReportsMissingAsset'
          negative_test: TestLoadProfile_MissingMachine
      - id: P2.2
        statement: Agent identity and agent-specific workflow policy do not change the fixed engine's behavior.
        check:
          kind: executable
          tier: mechanical_test
          command: >-
            cd agent-core && go test -count=1 -v ./internal/runtime/core -run
            'TestLoop_DeclarativeInit_MachineNameDoesNotChangeEngineBehavior|TestCoreSourceDoesNotEmbedModeOrToolPolicyNames'
          negative_test: TestCoreSourceDoesNotEmbedModeOrToolPolicyNames
      - id: P2.3
        statement: Every field declared by the agent data schema is read by the interpreter or explicitly classified as documentation-only.
        check:
          kind: executable
          tier: mechanical_test
          command: >-
            cd magefiles && go test -count=1 -v . -run
            'TestPatternInvariantDeclaredFields$|TestPatternInvariantDeclaredFieldRejectsUnreadField|TestPatternInvariantDeclaredFieldWholeValueConsumption|TestPatternInvariantDeclaredFieldParentPresenceIsNotChildRead'
          negative_test: TestPatternInvariantDeclaredFieldRejectsUnreadField
          module: agent-core
          root_types:
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/tools/catalog.ToolDef
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/runtime/core.MachineSpec
          whole_value_functions:
            - encoding/json.Marshal
            - gopkg.in/yaml.v3.Marshal
          documentation_only_fields:
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/runtime/core.MachineSpec.invariants
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/runtime/core.MachineSpec.lifecycle
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/runtime/core.MachineSpec.pipeline_diagram
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/tools/catalog.ToolErrorContract.condition
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/tools/catalog.ToolErrorContract.message_shape
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/tools/catalog.ToolErrorContract.state_after_failure
          documentation_only_reason: >-
            MachineSpec invariants, lifecycle, and pipeline_diagram are
            author-facing narrative metadata. ToolErrorContract condition,
            message_shape, and state_after_failure are failure-contract prose
            used by specification review; runtime routing reads errors[].signal
            and does not interpret these fields as control policy.
    consequences:
      benefits:
        - Agent changes are surface-level data diffs a reviewer can approve.
        - Schema, machine, and contract validation run in CI before deployment.
        - Model swaps are a one-field edit, isolating inference as the variable.
      liabilities:
        - Engine internals, factories, and adapters stay compiled.
        - Novel semantics need a new builtin or factory, not a declaration.
    grammar:
      within: [machine-interpreter]
      requires: [machine-interpreter, tool-contract]
      contains: [phase-scoped-toolset, inference-boundary]
      enables: [phase-scoped-toolset, inference-boundary]
      overlaps: []
    syntax: >
      Once you have a Machine Interpreter, package its machine together with its
      Tool Contracts and a model binding as one declarative artifact; then use
      Phase-Scoped Toolset and Inference Boundary to control what each layer exposes.
    examples:
      - system: Kubernetes
        cite: burns-borg-omega-kubernetes-2016
        kind: external
        note: The operator writes desired state as data and a fixed control loop reconciles reality to it — orchestration as data, not scripts.
      - system: Functional Core, Imperative Shell
        cite: bernhardt-fcis-2012
        kind: external
        note: The same split this pattern enforces — a fixed imperative shell (the interpreter) around declarative, data-first definitions.
      - system: Meta-Harness
        cite: meta-harness-2026
        kind: external
        note: Treats scaffolding around LLM calls as an optimizable object, supporting the claim that agent harness structure is a first-class design artifact.
      - system: AutoHarness
        cite: autoharness-2026
        kind: external
        note: Automatically generates harness code to improve LLM agent performance, corroborating that the surrounding runtime definition materially shapes agent behaviour.
      - system: Code as Agent Harness
        cite: ning-code-as-harness-2026
        kind: external
        note: Frames agent systems as executable, verifiable, stateful harnesses — close prior art for treating the agent wrapper as the unit of engineering.
      - system: Towards a Science of Scaling Agent Systems
        cite: kim-scaling-agents-2025
        kind: external
        note: Argues that agent systems introduce scaling failures beyond single-model behaviour, supporting the need to govern harness structure as a system artifact.
      - system: HARBOR
        cite: harbor-harness-optimization-2026
        kind: external
        note: Optimizes coding-agent harnesses directly, reinforcing the harness as an adjustable design surface separate from the model.
      - system: Temporal
        cite: temporal-2024
        kind: external
        note: A contrasting point — durable execution with human-in-the-loop waits, but workflows are imperative code, so the agent is not reviewable as pure data.
      - system: Reference implementation — one binary, many agents
        cite: reference-implementation
        kind: internal
        note: Executor, critic, bench, and jurist are four shipped profile families over the same binary.
        evidence:
          classification: shipped
          checks:
            - path: applications/catalog/agents/executor/profile.yaml
              artifact: profile
              assertion: yaml_reference
              field: machine
              target: applications/catalog/agents/executor/machine.yaml
              target_artifact: machine
            - path: applications/catalog/agents/critic/profile.yaml
              artifact: profile
              assertion: yaml_reference
              field: machine
              target: applications/catalog/agents/critic/machine.yaml
              target_artifact: machine
            - path: applications/catalog/agents/bench/profile.yaml
              artifact: profile
              assertion: yaml_reference
              field: machine
              target: applications/catalog/agents/bench/machine.yaml
              target_artifact: machine
            - path: applications/catalog/agents/specification-critic/profile.yaml
              artifact: profile
              assertion: yaml_reference
              field: machine
              target: applications/catalog/agents/specification-critic/machine.yaml
              target_artifact: machine
    provenance: [reference-implementation, external-corroboration]

  # ---------------------------------------------------------------------------
  - id: tool-contract
    number: 3
    name: Tool Contract
    also_known_as: [Typed Tool, Command Contract]
    confidence: 2
    intent: >
      Specify each tool as a typed contract — inputs, emittable signals, side
      effects, and an Undo — so the machine can validate it, the engine can
      dispatch it, and rollback can reverse it from the declaration and the
      tool's receipt.
    context: >
      An agent acts only through its tools. Frameworks usually define tools as
      function signatures with prose docstrings, which say what arguments to
      pass but not how the tool changes the world, whether that change can be
      undone, or how it fails mid-execution. The model infers the rest — and
      infers differently across contexts and model versions.
    problem: >
      How do you define a tool so that a model can select it, a developer can
      implement it, and an engine can reverse it — all without re-deriving its
      behaviour and without hidden side effects?
    applicability: >
      Use this whenever tools are implemented by developers, selected by a
      model, composed by a machine, or reversed by rollback. The discipline pays
      off across teams, model generations, and multi-step plans. It is overkill
      for a one-off script with one hard-coded operation and no composition,
      selection, or reversal.
    forces:
      - One definition must serve three audiences: model, developer, engine.
      - A tool's side effects and failure modes must be explicit, not inferred.
      - Each tool should do exactly one agent-visible thing so contract and effect composition live in the machine.
      - Reversibility must be knowable before the tool is dispatched.
    solution: >
      Make each tool an atomic operation with a structured contract: problem,
      goals, non-goals, requirements, acceptance criteria, and a reversibility
      tier (reversible / compensatable / irreversible), plus its emittable
      signals and declared predecessors and successors. Atomicity is judged at
      selectable domain-operation and rollback boundaries, not by counting
      validation, parsing, formatting, protocol, or helper-process stages used
      to construct one result. Composition emerges later, in the machine —
      never hidden behind one contract.
    implementation: >
      A complete contract must answer four questions: can a developer implement
      it, can the agent select it, can its output compose with the next tool,
      and is failure state defined. A tool must not hide workflow loops,
      separately selectable domain branches, or undeclared child-tool dispatch.
      Bounded internal control flow that validates input, enforces policy,
      implements one protocol, parses/formats one result, or completes one
      recovery boundary is not composition by itself. Reversibility must be
      explicit: reversible tools restore from their receipt, compensatable tools
      declare corrective action and semantic differences, and irreversible
      tools are skipped and logged. Omitting reversibility is a contract error,
      not irreversibility.
      Enforce completeness during authoring and specification audit. At ordinary
      runtime startup, validate only wiring needed for execution: parse retries,
      emitted-signal routes, and receipt-consuming undo.
    invariants:
      - id: P3.1
        statement: Every declared tool carries the complete six-section contract required for implementation, selection, composition, failure handling, and reversal.
        check:
          kind: executable
          tier: linter
          command: >-
            cd agent-core && go test -count=1 -v ./pkg/spec -run
            'TestContractBaselineMatchesCorpus|TestContractBaselineRatchet'
          negative_test: TestContractBaselineRatchet
      - id: P3.2
        statement: A tool contract represents one agent-selectable domain operation and does not hide separately selectable effects or rollback boundaries.
        check:
          kind: manual
          reason: >-
            Distinguishing a compound domain operation from bounded validation,
            parsing, protocol handling, or recovery implementation requires
            semantic judgment over both the declaration and its implementation.
    consequences:
      benefits:
        - A uniform, independently testable and reversible tool inventory.
        - Precise output schemas let the agent plan sequences without trial and error.
        - Declared reversibility tiers tell the rollback walk what each tool's receipt can undo.
      liabilities:
        - Decomposition yields more, smaller tools to select among.
        - Every tool needs a full contract, not a docstring — real up-front cost.
    grammar:
      within: [machine-interpreter]
      requires: [machine-interpreter]
      contains: []
      enables: [agent-as-data, phase-scoped-toolset, inference-boundary, bidirectional-log, boundary-tool, approval-gate]
      overlaps: []
    syntax: >
      Under a Machine Interpreter, give every tool a Tool Contract; its
      reversibility tier is read by Bidirectional Log, its boundary kind by
      Boundary Tool and Inference Boundary, and its emittable signals by the
      machine's static validation.
    examples:
      - system: Design by Contract (Eiffel)
        cite: meyer-dbc-1997
        kind: external
        note: Preconditions, postconditions, and invariants as first-class specifications — the direct ancestor of a typed tool contract.
      - system: Command pattern (Gang of Four)
        cite: gamma-gof-1994
        kind: external
        note: An operation encapsulated as an object with execute and undo — the structural core of a reversible tool.
      - system: OpenAPI Specification
        cite: openapi-spec-2024
        kind: external
        note: Typed request/response contracts for services; agent tool declarations can be generated from an OpenAPI spec and then edited.
      - system: Model Context Protocol
        cite: anthropic-mcp-2024
        kind: external
        note: A standard interface for exposing tools to models with typed inputs and declared behaviour.
      - system: SWE-agent Agent-Computer Interface
        cite: yang-swe-agent-2024
        kind: external
        note: Shows that tool and environment interfaces are decisive for software-engineering agents, strengthening the case for treating tool shape as a contract.
      - system: ReAct
        cite: yao-react-2023
        kind: external
        note: Establishes the reasoning-and-acting loop in which language models choose actions from observations; Tool Contract makes those actions explicit and machine-checkable.
      - system: Reference implementation — YAML tool declarations
        cite: reference-implementation
        kind: internal
        note: Shipped runtime startup validates parse-retry wiring, emitted-signal routes, and receipt contracts before execution; full six-section completeness belongs to authoring and specification audit.
        evidence:
          classification: shipped
          checks:
            - path: agent-core/cmd/agent/main.go
              artifact: go_source
              assertion: go_symbol
              symbol: validateRuntimeToolWiring
            - path: agent-core/cmd/agent/main_tool_validation_test.go
              artifact: go_test
              assertion: go_test
              test: TestRuntimeStartupValidatesWiringNotFullContractCompleteness
      - system: Reference implementation — contract authoring and specification audit
        cite: reference-implementation
        kind: internal
        note: Shipped validators report full completeness during authoring, and the public specification audit enforces it for selected tools; ordinary startup does not invoke that check.
        evidence:
          classification: shipped
          checks:
            - path: agent-core/internal/tools/catalog/contracts.go
              artifact: go_source
              assertion: go_symbol
              symbol: ValidateToolContracts
            - path: agent-core/internal/tools/catalog/contracts.go
              artifact: go_source
              assertion: go_symbol
              symbol: ReviewToolAuthoring
            - path: agent-core/pkg/spec/validate_tools.go
              artifact: go_source
              assertion: go_symbol
              symbol: checkSelectedToolContractCompleteness
            - path: agent-core/internal/tools/catalog/contracts_test.go
              artifact: go_test
              assertion: go_test
              test: TestValidateToolContractsReportsMissingFields
            - path: agent-core/pkg/spec/validate_tools_test.go
              artifact: go_test
              assertion: go_test
              test: TestValidate_SelectedToolContractCompletenessErrorsForActiveMigratedTool
    provenance: [reference-implementation, external-corroboration]

  # ---------------------------------------------------------------------------
  - id: phase-scoped-toolset
    number: 4
    name: Phase-Scoped Toolset
    also_known_as: [Scoped Toolset, Per-Phase Tools, Least-Authority Manifest]
    confidence: 2
    intent: >
      Derive the tools valid in the current phase from machine routes and tool
      outcomes, so unroutable tools are invisible to the model.
    context: >
      In an Agent-as-Data every tool is declared, and the machine knows
      which phase it is in. If the whole tool set is offered at every step, the
      model can call a tool at the wrong time — building before the code is
      ready, re-running a subject during a checking phase.
    problem: >
      How do you keep the model from invoking a tool in a phase where it does
      not belong, and keep each decision's tool manifest small?
    applicability: >
      Use this when the registry is larger than the useful set in any one
      phase, when global visibility creates token waste or misuse, or when some
      tools must be impossible before a state such as validation or approval.
      It is unnecessary when every external tool is genuinely valid in every
      model-facing phase.
    forces:
      - The model should never be able to select an out-of-phase tool.
      - The per-call manifest should stay small to sharpen model choices.
      - Scoping must derive from machine grammar and ToolDef outcomes, not prompt exhortation.
      - Some actions must be made structurally impossible, not just unlikely.
    solution: >
      Derive model-visible tools from each $tool transition's target state and
      the emitted signals that state can route. Include only external tools;
      optional tool-level phases may narrow but never widen the derived set.
      Use the same availability rule for manifest generation, parsing, and
      dispatch.
    implementation: >
      For each MachineSpec transition with action $tool, treat its target as a
      candidate phase. A ToolDef is available there only when it is external,
      its optional phases admit the target, and every emitted signal is handled
      from that target (or the target is terminal). Store the result as derived
      ToolSpec phases. Registry Manifest and ResolveExternalTool must share this
      rule so out-of-state calls fail before execution.
    invariants:
      - id: P4.1
        statement: A model-visible tool is available in a phase only when every signal it emits is routable from that phase.
        check:
          kind: executable
          tier: compiler
          command: >-
            cd agent-core && go test -count=1 -v ./internal/tools/catalog -run
            'TestDeriveDynamicToolPhasesScopesExternalToolsByRoutableSignals|TestApplyDynamicToolPhasesMakesMissingEmitToolsUnavailable'
          negative_test: TestApplyDynamicToolPhasesMakesMissingEmitToolsUnavailable
      - id: P4.2
        statement: Manifest generation, model-response parsing, and dynamic dispatch apply one shared external-tool availability rule.
        check:
          kind: executable
          tier: mechanical_test
          command: >-
            cd agent-core && go test -count=1 -v ./internal/runtime/core -run
            TestManifestAndAvailableExternalToolNamesUseAvailabilityRule
          negative_test: TestManifestAndAvailableExternalToolNamesUseAvailabilityRule
    consequences:
      benefits:
        - Out-of-phase actions are impossible, not merely improbable.
        - Smaller per-call manifests improve selection accuracy.
      liabilities:
        - Requires the machine's phases and emitted-signal routes to be well factored.
        - Missing follow-up transitions can hide a tool until diagnostics expose the unroutable signal.
    grammar:
      within: [agent-as-data]
      requires: [machine-interpreter, agent-as-data, tool-contract]
      contains: []
      enables: [approval-gate]
      overlaps: []
    syntax: >
      Within an Agent-as-Data, derive each Tool Contract's phases from $tool
      grammar and emitted-signal routes, then apply any narrower tool-level
      phase restriction.
    examples:
      - system: Principle of Least Privilege
        cite: saltzer-schroeder-1975
        kind: external
        note: A component should hold only the authority its current task requires — the security principle this pattern applies per machine state.
      - system: Capability-based security
        cite: dennis-vanhorn-1966
        kind: external
        note: Authority is conferred by holding an unforgeable capability; a state's tool manifest is precisely the set of capabilities held in that phase.
      - system: OAuth 2.0 scopes
        cite: hardt-oauth-2012
        kind: external
        note: Access tokens carry narrowed scopes rather than full authority — the same attenuation applied to which actions are exposed.
      - system: Reference implementation — derived dynamic availability
        cite: reference-implementation
        kind: internal
        note: The catalog derives ToolSpec phases from $tool targets and routable emits, intersects explicit tool-level phases, and uses one registry rule for manifests and dispatch.
        evidence:
          classification: shipped
          checks:
            - path: agent-core/internal/tools/catalog/emits_test.go
              artifact: go_test
              assertion: go_test
              test: TestDeriveDynamicToolPhasesScopesExternalToolsByRoutableSignals
            - path: agent-core/internal/tools/catalog/emits_test.go
              artifact: go_test
              assertion: go_test
              test: TestDeriveDynamicToolPhasesRespectsExplicitPhaseNarrowing
            - path: agent-core/internal/tools/catalog/emits_test.go
              artifact: go_test
              assertion: go_test
              test: TestApplyDynamicToolPhasesFiltersRegistryManifest
            - path: agent-core/internal/runtime/core/registry_test.go
              artifact: go_test
              assertion: go_test
              test: TestManifestAndAvailableExternalToolNamesUseAvailabilityRule
            - path: agent-core/internal/tools/catalog/phase_scoped_documentation_test.go
              artifact: go_test
              assertion: go_test
              test: TestPhaseScopedChapterExamplesLoadAndDeriveAvailability
    provenance: [reference-implementation, external-corroboration]

  # ---------------------------------------------------------------------------
  - id: inference-boundary
    number: 5
    name: Inference Boundary
    also_known_as: [Model Adapter, Provider Port]
    confidence: 2
    intent: >
      Place all model inference behind one tool and provider-adapter interface,
      so model changes stay in configuration and new provider code touches
      neither the machine nor any other tool.
    context: >
      In an Agent-as-Data inference is one tool among many. If prompt
      formats and API calls instead spread across many call sites, the line
      between deterministic harness and stochastic inference blurs, provider
      switches touch every tool, and usage data fragments across sites.
    problem: >
      How do you support provider adapters and model families, and account for
      their usage, without changing the machine or the other tools?
    applicability: >
      Use this when agents must compare or switch providers, run the same
      harness across model families, or aggregate token and latency data
      in one place. If a system is permanently bound to one model API and never
      compares models, the adapter may be needless indirection.
    forces:
      - Exactly one place should cross the inference boundary.
      - Switching provider should not touch the machine or other tools.
      - Evaluation should attribute outcome differences to the model alone.
      - Token usage and latency data should aggregate at one point.
    solution: >
      Funnel every model interaction through one inference tool backed by a
      provider adapter interface. The tool assembles a provider-agnostic prompt,
      the adapter translates to and from a provider's API, and a parser
      normalizes the reply to one result type. Everything else stays on the
      harness side of the boundary; changing the model is a one-field edit.
    implementation: >
      Keep prompt assembly provider-agnostic: current state, conversation
      history, and scoped manifest go in; provider payloads come only from the
      adapter. Normalize responses into the same tool-call/completion/error
      shape for the machine, and record token, latency, and model data at
      this boundary. Parsers must tolerate malformed or embedded tool calls,
      especially for open-weight models and changing API formats.
    invariants:
      - id: P5.1
        statement: No package outside the declared inference adapter boundary imports a model-provider SDK or issues model HTTP calls.
        check:
          kind: executable
          tier: linter
          command: >-
            cd magefiles && go test -count=1 -v . -run
            'TestPatternInvariantInferenceBoundary$|TestPatternInvariantInferenceBoundaryRejectsImportOutsideAdapter|TestPatternInvariantInferenceBoundaryProviderListIsDeclarative'
          negative_test: TestPatternInvariantInferenceBoundaryRejectsImportOutsideAdapter
          module: agent-core
          adapter_packages:
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/model/llm
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/tools/llm
          provider_imports:
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/model/llm/ollama
            - github.com/Nokia-Bell-Labs/declarative-agents/agent-core/internal/model/llm/cohere
    consequences:
      benefits:
        - Model-agnostic evaluation and provider changes isolated to adapters.
        - A single instrumentation point for tokens and latency.
      liabilities:
        - Translation adds overhead and risks provider-specific features going unused.
        - The parser must handle malformed or embedded tool-call output gracefully.
    grammar:
      within: [agent-as-data]
      requires: [machine-interpreter, agent-as-data, tool-contract]
      contains: []
      enables: []
      overlaps: [boundary-tool]
    syntax: >
      Within an Agent-as-Data, make inference a single boundary Tool Contract
      behind a provider adapter; Transition Spans then attach one
      gen_ai.chat span per invocation at exactly this point.
    examples:
      - system: Adapter pattern (Gang of Four)
        cite: gamma-gof-1994
        kind: external
        note: Convert one interface into another expected by a client — the classic structure of a per-provider adapter behind one interface.
      - system: Hexagonal Architecture (Ports and Adapters)
        cite: cockburn-hexagonal-2005
        kind: external
        note: An application core depends on a port; external systems plug in via adapters — exactly how the engine reaches any model provider.
      - system: Model Context Protocol
        cite: anthropic-mcp-2024
        kind: external
        note: A uniform boundary between an agent and heterogeneous external capabilities, including models.
      - system: Reference implementation — executor profile variants
        cite: reference-implementation
        kind: internal
        note: The default, qwen35b, and qwen27b executor profiles are loadable entry points over one machine/tool harness; qwen27b binds a different Ollama model declaration.
        evidence:
          classification: shipped
          checks:
            - paths:
                - applications/catalog/agents/executor/profile.yaml
                - applications/catalog/agents/executor/profile-qwen35b.yaml
                - applications/catalog/agents/executor/profile-qwen27b.yaml
              artifact: profile
              assertion: yaml_relation
              same_fields: [machine, tools, tool_config_dirs]
              different_fields: [tool_declarations]
            - path: applications/catalog/conformance/executor_test.go
              artifact: go_test
              assertion: go_test
              test: TestExecutorShippedProfileWiring
      - system: Reference implementation — model provider adapters
        cite: reference-implementation
        kind: internal
        note: Ollama and Cohere v2 are shipped model provider adapters behind the same invoke_llm boundary; both emit normalized chat results and GenAI tracing.
        evidence:
          classification: shipped
          checks:
            - path: agent-core/internal/model/llm/ollama/adapter_tracing_test.go
              artifact: go_test
              assertion: go_test
              test: TestChat_EmitsSemconvInferenceSpan
            - path: agent-core/internal/model/llm/cohere/tracing_test.go
              artifact: go_test
              assertion: go_test
              test: TestChat_Tracing
      - system: Design intent — further provider adapters
        cite: reference-implementation
        kind: internal
        note: Provider portability beyond Ollama and Cohere is design intent; adding OpenAI, Anthropic, or failover behavior requires new adapter code and focused cross-provider tests.
        evidence:
          classification: design_intent
    provenance: [reference-implementation, external-corroboration]

  # ---------------------------------------------------------------------------
  - id: bidirectional-log
    number: 6
    name: Bidirectional Log
    also_known_as: [Execution Rollback, Undo Engine]
    confidence: 2
    intent: >
      Walk the recorded execution backward, calling Undo on each tool in
      reverse, so recovery from a mistaken step is mechanical rather than
      probabilistic.
    context: >
      A Machine Interpreter records (state, signal, tool, result) as it runs, and
      each Tool Contract declares a reversibility tier. Agents make mistakes;
      without rollback the only recovery is to restart or to ask the model to
      fix its own errors on a contaminated history, which compounds mistakes.
    problem: >
      How do you recover from a wrong step deterministically, restoring external
      effects and giving the model clean context, across a mix of reversible
      and irreversible tools?
    applicability: >
      Use this for agents whose tools mutate files, resources, conversation
      state, or other external state; for speculative execution; and when
      irreversible effects must be auditable. Agents with no side effects, or
      only naturally idempotent side effects, gain little from rollback.
    forces:
      - Recovery should be mechanical, not a second probabilistic attempt.
      - Position, execution, and receipts persist through one port; external state restores through the receipts.
      - Reversible, compensatable, and irreversible tools need different handling.
      - Irreversible effects, once committed, must be recorded, not undone.
    solution: >
      Treat the execution as a bidirectional log, persisted one commit per step
      by the Dolt backend. To roll back, the Dolt adapter's git-style Revert
      rewinds persisted state to a step, then a lifecycle tool walks the later
      entries in reverse: each reversible tool's Undo decodes its receipt, a
      compensatable tool issues a corrective action from it, irreversible tools
      are skipped and logged. Resuming is a fresh continuation, not a replay.
    implementation: >
      Every state-mutating tool encodes an opaque Receipt during Execute; read-only
      tools return an empty receipt. The validator checks receipt presence for
      reversible tools, while each tool's round-trip test checks sufficiency. The
      checkpoint port saves the Position and the appended Execution entry as one
      unit, committed per step by the Dolt backend, on every dispatch and on
      suspend. Rollback is Dolt Revert(run_id, step_index) for persisted state
      plus the lifecycle tool's reverse receipt walk for external effects; it
      returns a resumable position, and discarded entries survive only in the
      rollback report.
    invariants:
      - id: P6.1
        statement: Every state-mutating reversible tool emits a sufficient receipt and provides non-noop receipt-consuming reversal.
        check:
          kind: executable
          tier: compiler
          command: >-
            cd agent-core && go test -count=1 -v ./internal/tools/catalog -run
            'TestValidateReceiptPresenceFailsReversibleToolWithoutReceipt|TestValidateReceiptContractFailsMutatingReversibleWithNoopUndo'
          negative_test: TestValidateReceiptPresenceFailsReversibleToolWithoutReceipt
      - id: P6.2
        statement: Rollback visits persisted execution entries in reverse order and reports every reversal, compensation, irreversible skip, and failure.
        check:
          kind: executable
          tier: conformance_test
          command: >-
            cd agent-core && go test -count=1 -v ./internal/tools/lifecycle -run
            'TestRollbackViaReceiptsContinuesPastFailure|TestRollbackViaReceiptsCleanWhenAllReverse'
          negative_test: TestRollbackViaReceiptsContinuesPastFailure
    consequences:
      benefits:
        - Deterministic recovery and cheap speculative exploration of reversible plans.
        - Skipped irreversible entries are auditable.
      liabilities:
        - Committing every step has cost; NoopCheckpoint keeps disabled runs free of it.
        - Once an irreversible tool commits its external effect, the receipt walk cannot reverse it; Revert rewinds only persisted state.
    grammar:
      within: [machine-interpreter]
      requires: [machine-interpreter, tool-contract]
      contains: []
      enables: [approval-gate, operator-port]
      overlaps: []
    syntax: >
      Over a Machine Interpreter's record and a set of Tool Contracts, walk
      the Bidirectional Log with its own lifecycle tool; its per-step Dolt commits are what
      an Approval Gate suspends into and what an Operator Port restores.
    examples:
      - system: Database transactions and rollback
        cite: gray-1978
        kind: external
        note: The canonical model of a durable, reversible sequence of operations with a commit boundary; rollback restores the prior consistent state.
      - system: Sagas
        cite: garcia-molina-sagas-1987
        kind: external
        note: Long-running work as a sequence of local steps each with a compensating action, undone in reverse — the direct model for mixed-tier rollback.
      - system: Memento pattern (Gang of Four)
        cite: gamma-gof-1994
        kind: external
        note: Capture an object's state so it can be restored later without violating encapsulation — the Receipt each tool encodes during Execute, with the tool as its only decoder.
      - system: Event Sourcing
        cite: fowler-event-sourcing-2005
        kind: external
        note: State as a replayable/reversible log of events; the execution record is the same kind of traversable log.
      - system: Reference implementation — checkpoint and receipt rollback mechanics
        cite: reference-implementation
        kind: internal
        note: The shipped runtime versions dispatch state in Dolt, reverts persisted history to a selected step, and walks tool-owned receipts in reverse; this evidence covers mechanics, not automatic profile wiring.
        evidence:
          classification: shipped
          checks:
            - path: agent-core/tools/builtin/checkpoint-rollback.yaml
              artifact: tool_declaration
              assertion: yaml_sequence_match
              field: tools
              match: {name: checkpoint_rollback, init: checkpoint_rollback}
            - path: agent-core/internal/runtime/checkpoint/dolt/dolt_checkpoint_revert_test.go
              artifact: go_test
              assertion: go_test
              test: TestDoltCheckpointRevertResetsToStepCommit
            - path: agent-core/internal/tools/lifecycle/receipt_rollback_test.go
              artifact: go_test
              assertion: go_test
              test: TestRollbackViaReceiptsCleanWhenAllReverse
      - system: Design intent — coding-agent rollback
        cite: reference-implementation
        kind: internal
        note: Automatic coding retry, gated deployment recovery, and generated compensation for mixed API plans are design intent, not shipped profile behavior; no production profile routes validation failure into checkpoint_rollback.
        evidence:
          classification: design_intent
    provenance: [reference-implementation, external-corroboration]

  # ---------------------------------------------------------------------------
  - id: transition-spans
    number: 7
    name: Transition Spans
    also_known_as: [Trace Instrumentation, Span Mapping, Observable Execution]
    confidence: 2
    intent: >
      Project run and dispatch boundaries onto OpenTelemetry spans so execution
      is observable with standard tooling without coupling the engine to any
      telemetry backend.
    context: >
      A Machine Interpreter already produces a semantic record — the execution —
      that an evaluator reads. But an operator needs a different view: timed,
      hierarchical, cross-service, and visualizable in standard backends.
    problem: >
      How do you make an agent's execution debuggable and auditable with
      standard tooling, and correlate multi-agent runs, without importing a
      telemetry SDK into the engine?
    applicability: >
      Use this when executions must be debugged, audited, or compared
      across models and when child agents must correlate into one distributed
      trace. For tiny local runs, a file adapter or no-op tracer may be enough,
      but the port should remain so instrumentation choices do not change engine
      behaviour.
    forces:
      - The engine must not depend on any telemetry backend.
      - Tool timing, token usage, and run duration must be available for evaluation.
      - Multi-agent executions must correlate into one distributed trace.
      - Instrumentation variance must not confound model comparison.
    solution: >
      Define a Tracer port the engine depends on and implement it with adapters
      (OTel, file, no-op). Map the machine onto the shipped span model: one
      invoke_agent run root with direct execute_tool or chat children. Record
      iteration and transition outcomes as run attributes, and command outcomes
      as child attributes. Propagate W3C Trace Context across supported agent
      boundaries so child executions root under the parent's trace.
    implementation: >
      The engine should depend only on a tracer port: start/end span, record
      event, set attribute, and propagate context. Emit stable semantic fields
      such as run id, state, signal, tool, iteration, model, token counts,
      duration, and error status. Use GenAI operation names invoke_agent,
      execute_tool, and chat; do not infer monetary cost from token usage.
      Keep an NDJSON adapter for offline evaluation and a no-op adapter for
      tests or low-overhead deployments.
    invariants:
      - id: P7.1
        statement: Each run emits one invoke_agent root whose dispatches are direct execute_tool or chat children with stable state, signal, tool, duration, and error attributes.
        check:
          kind: executable
          tier: mechanical_test
          command: >-
            cd agent-core && go test -count=1 -v ./internal/runtime/core -run
            TestLoopTraceShapeIsRunWithDirectToolChildren
          negative_test: TestLoopTraceShapeIsRunWithDirectToolChildren
      - id: P7.2
        statement: Every supported cross-process agent transport propagates W3C trace context and treats absent or malformed context as a new root.
        check:
          kind: executable
          tier: conformance_test
          command: >-
            cd agent-core && go test -count=1 -v ./internal/tools/rest -run
            'TestRESTClient_InjectsTraceparentFromActiveSpan|TestRESTServer_ExtractsTraceparentParentsMachineRequestSpan|TestRESTServer_TraceparentFallbackToNewRoot'
          negative_test: TestRESTServer_TraceparentFallbackToNewRoot
    consequences:
      benefits:
        - Backend independence and standard, searchable, visualizable traces.
        - Tool duration, run duration, token usage, and structured tool outcomes.
      liabilities:
        - Per-dispatch spans multiply span volume for long runs.
        - Metrics are derived post-ingestion, not emitted in real time by default.
    grammar:
      within: [machine-interpreter]
      requires: [machine-interpreter]
      contains: []
      enables: [convergence-taxonomy, boundary-tool]
      overlaps: [operator-port]
    syntax: >
      Over a Machine Interpreter, create one run span and direct dispatch
      children through a Tracer port; annotate transition outcomes on the run
      and let Convergence Taxonomy read the resulting artifacts.
    examples:
      - system: OpenTelemetry
        cite: otel-spec-2024
        kind: external
        note: The vendor-neutral standard for traces as DAGs of timed spans with attributes — the target model this pattern maps onto.
      - system: OpenTelemetry GenAI semantic conventions
        cite: otel-genai-semconv-2025
        kind: external
        note: Standard attributes for model calls (system, model, token counts) that make inference telemetry queryable across vendors.
      - system: W3C Trace Context
        cite: w3c-trace-context-2021
        kind: external
        note: The traceparent propagation format that links a child agent's trace into the parent's.
      - system: Dapper
        cite: sigelman-dapper-2010
        kind: external
        note: Google's large-scale distributed tracing infrastructure that established span trees and propagation as the way to observe distributed executions.
      - system: Reference implementation — grid evaluation
        cite: reference-implementation
        kind: internal
        note: The shipped loop emits one invoke_agent run span with direct execute_tool/chat children; evaluation reports iterations, token counts, duration, success/progression rates, and tool metric snapshots, but no monetary cost.
        evidence:
          classification: shipped
          checks:
            - path: agent-core/internal/runtime/core/loop_trace_shape_test.go
              artifact: go_test
              assertion: go_test
              test: TestLoopTraceShapeIsRunWithDirectToolChildren
            - path: agent-core/internal/evaluation/eval_metrics_test.go
              artifact: go_test
              assertion: go_test
              test: TestExtractToolSnapshots_WithMetrics
            - path: agent-core/internal/evaluation/eval_report_test.go
              artifact: go_test
              assertion: go_test
              test: TestTransitionSpansDocumentedEvaluationMetrics
    provenance: [reference-implementation, external-corroboration]

  # ---------------------------------------------------------------------------
  - id: boundary-tool
    number: 8
    name: Boundary Tool
    also_known_as: [Agent Delegation, Hierarchical Composition]
    confidence: 1
    intent: >
      Delegate a complete child execution through one boundary tool so every
      machine stays flat, independently validatable, and composable without
      structural coupling.
    context: >
      A Machine Interpreter distinguishes terminal (atomic) tools from
      non-terminal (boundary) tools whose Execute crosses into an external
      actor — a model, a human, or another agent — and returns only when that
      actor finishes. To the parent, a boundary tool is one dispatch and one
      signal even though an entire execution unfolds inside.
    problem: >
      How do you coordinate work across sub-agents without the parent knowing
      the child's control flow, and keep every machine flat and independently
      validatable?
    applicability: >
      Use this when work decomposes hierarchically and each child agent should
      be independently validated, audited, authorized, and rolled back. Avoid it
      when peers need open-ended negotiation or shared mutable control flow; this
      pattern is parent-to-child composition, not a general multi-agent society.
    forces:
      - Parent and child must not be structurally coupled.
      - Each machine must stay flat and independently validatable.
      - A child's execution must be independently auditable and rollbackable.
      - Authority must narrow, never widen, as work is delegated downward.
    solution: >
      Compose agents through non-terminal boundary tools. A boundary tool
      carries a child profile reference, not a code dependency; it collapses the
      child's whole execution into one signal the parent routes on. Hierarchy
      comes from stacking boundaries, not from nesting machines; authority
      attenuates at each boundary. A complete boundary adapter propagates trace
      context so the child roots under the parent.
    implementation: >
      A delegation tool is a Tool Contract whose Execute launches a child
      profile and returns one parent-level signal such as ChildSucceeded or
      ChildFailed. Pass only attenuated authority, budget, context, and trace
      context into the child, and test propagation for each adapter rather than
      inferring it from successful dispatch. The parent may compensate the child
      boundary as a whole, but it must not reach inside the child's execution
      log to undo individual tools.
    invariants:
      - id: P8.1
        statement: Every child-agent boundary names a child profile, passes only declared inputs and authority, and collapses the child run to declared parent-level outcomes.
        check:
          kind: executable
          tier: compiler
          command: >-
            cd agent-core && go test -count=1 -v ./internal/tools/catalog -run
            'TestValidateChildAgentConfigRequiresFields|TestValidateRunPointConfigRequiresFields'
          negative_test: TestValidateChildAgentConfigRequiresFields
      - id: P8.2
        statement: Workflow ordering and branching remain in MachineSpec while one boundary adapter's protocol, validation, and result reduction remain implementation structure.
        check:
          kind: manual
          reason: >-
            Deciding whether code hides independently meaningful workflow or
            implements one declared boundary requires comparing agent-visible
            effects and choices with the implementation's internal steps.
    consequences:
      benefits:
        - Isolation with flat machines; complexity stays local.
        - Authority attenuation and coherent hierarchical traces.
      liabilities:
        - A parent can only run a child's coarse boundary compensation, not undo the child's individual tools.
        - Composition is strictly hierarchical; there is no peer-to-peer negotiation.
        - Boundary adapters can compose execution while leaving a trace gap; the current critic run_point uses a no-op child tracer.
    grammar:
      within: [machine-interpreter]
      requires: [machine-interpreter, tool-contract, transition-spans]
      contains: []
      enables: []
      overlaps: [inference-boundary]
    syntax: >
      Over a Machine Interpreter, wrap a child Agent-as-Data as one boundary
      Tool Contract; require each adapter to propagate Transition Spans into the
      child, and use the Bidirectional Log to compensate the child as a whole.
    examples:
      - system: Supervisor trees (Erlang/OTP)
        cite: armstrong-2003
        kind: external
        note: Processes arranged in a supervision hierarchy where a parent starts, monitors, and restarts children — hierarchical composition with attenuated responsibility.
      - system: Actor model
        cite: hewitt-actor-1973
        kind: external
        note: Computation as actors that create other actors and communicate only by messages — the theoretical basis for composing isolated executions.
      - system: MapReduce
        cite: dean-mapreduce-2004
        kind: external
        note: A coordinator delegates isolated sub-computations to workers and merges results — a parent-to-child composition with results returned upward.
      - system: Reference implementation — bench/critic/executor stack
        cite: reference-implementation
        kind: internal
        note: Bench delegates to critics via self_invoke; critics run executors via nested run_point; this shipped evidence covers hierarchical dispatch, not nested trace linkage.
        evidence:
          classification: shipped
          checks:
            - path: applications/catalog/agents/bench/machine.yaml
              artifact: machine
              assertion: yaml_transition
              match: {state: Validating, signal: SuiteSelected, next: Launching, action: launch_evaluator}
            - path: applications/catalog/agents/bench/builtin.yaml
              artifact: tool_declaration
              assertion: yaml_sequence_match
              field: tools
              match: {name: launch_evaluator, init: self_invoke}
            - path: applications/catalog/agents/critic/machine.yaml
              artifact: machine
              assertion: yaml_transition
              match: {state: MaterializingPoints, signal: EvalPointsMaterialized, next: RunningPoints, action: run_point}
            - path: applications/catalog/agents/critic/builtin.yaml
              artifact: tool_declaration
              assertion: yaml_sequence_match
              field: tools
              match: {name: run_point, init: run_point}
      - system: Reference implementation — nested run_point trace propagation
        cite: reference-implementation
        kind: internal
        note: Nested span linkage is design intent; the shipped critic run_point path supplies a no-op tracer, so the child emits no linked spans.
        evidence:
          classification: design_intent
          checks:
            - path: agent-core/internal/evaluation/eval_run_point.go
              artifact: go_source
              assertion: go_composite_literal
              function: Execute
              type: tracing.NoopTracer
    provenance: [reference-implementation, external-corroboration]

  # ---------------------------------------------------------------------------
  - id: approval-gate
    number: 9
    name: Approval Gate
    also_known_as: [Human-in-the-Loop Gate, Suspend-and-Confirm]
    confidence: 1
    intent: >
      Suspend execution at a declared state, checkpoint it, and resume or roll
      back on an external decision without losing context across the suspension.
    context: >
      Some tools are irreversible — deploying, deleting, provisioning. A Tool
      Contract marks these, and Bidirectional Log provides checkpoints. Before
      such an action, an external authority should review and decide, possibly
      after the process has exited and hours have passed.
    problem: >
      How do you pause before an irreversible action for an external decision,
      survive process restarts during the wait, and resume in exactly the same
      state — without bolting a gate onto the control flow imperatively?
    applicability: >
      Use this before irreversible or high-risk tools such as deployment,
      deletion, provisioning, financial movement, or external notification,
      especially when the decision may take hours or require an audit trail. Do
      not gate ordinary reversible decisions; unnecessary gates serialize an
      otherwise fast pipeline through human latency.
    forces:
      - The gate must be a declared transition, not a thread block or a lost callback.
      - Execution must survive process boundaries during an arbitrary wait.
      - Resume must restore the exact pre-suspension state.
      - Every decision must leave an audit trail.
    solution: >
      Make the gate a signal triplet — AwaitApproval / Approved / Rejected —
      handled by ordinary transitions. A suspend tool emits AwaitApproval; the
      engine checkpoints all state, notifies the authority, and exits. Resume is
      signal injection into the loaded checkpoint: Approved routes to the gated
      tool, Rejected routes to rollback or revision. Manual and automated
      approval use the same path.
    implementation: >
      Place the gate before the irreversible tool, not inside it. The suspend
      tool is an internal lifecycle tool so the model cannot skip it by refusing
      to call it. A complete gate adds action summary, status, and authority
      decision metadata to the normal checkpoint and notifies the authority.
      The reference conformance fixture persists only standard Position and
      Execution, then resumes through --resume-checkpoint and --resume-signal;
      notification and decision metadata remain design intent. Recheck
      preconditions because the external world may have changed during the wait.
    invariants:
      - id: P9.1
        statement: Every approval gate is an explicit machine transition before the gated effect and handles both approved and rejected decisions.
        check:
          kind: executable
          tier: conformance_test
          command: >-
            cd applications/catalog && go test -count=1 -v ./conformance -run
            TestLifecycleApprovalSuspendResume
          negative_test: TestLifecycleApprovalSuspendResume
      - id: P9.2
        statement: Suspension persists the exact resumable position before exit, and resume validates program identity before injecting the decision signal.
        check:
          kind: executable
          tier: mechanical_test
          command: >-
            cd agent-core && go test -count=1 -v ./internal/runtime/core -run
            'TestLoop_SuspendSucceedsWhenCheckpointSaveSucceeds|TestSignalSource_ResumeValidatesProgramIdentity|TestResumeHonorsExplicitResumeSignal'
          negative_test: TestSignalSource_ResumeValidatesProgramIdentity
    consequences:
      benefits:
        - Irreversible tools are safe to hold in the registry; they run only after approval.
        - Cross-session continuity and auditable, composable multi-stage gates.
      liabilities:
        - Each gate serializes execution through human latency.
        - Checkpoints need storage cleanup, and preconditions may go stale during the wait.
    grammar:
      within: [machine-interpreter]
      requires: [machine-interpreter, tool-contract, bidirectional-log, phase-scoped-toolset]
      contains: []
      enables: [operator-port]
      overlaps: []
    syntax: >
      Over a Machine Interpreter, place an Approval Gate before any irreversible
      Tool Contract; it suspends into a Bidirectional Log checkpoint, and an
      Operator Port's pause command reuses the same suspension.
    examples:
      - system: Two-phase commit
        cite: gray-1978
        kind: external
        note: A coordinator prepares participants and waits for a decision before the irreversible commit — the suspend-then-decide-then-commit structure.
      - system: Temporal human-in-the-loop
        cite: temporal-2024
        kind: external
        note: Durable execution with signals that pause a workflow until an external decision arrives, surviving process restarts.
      - system: AWS Step Functions callback tasks
        cite: aws-step-functions-callback-2024
        kind: external
        note: A workflow task can suspend on an external callback token before resuming, matching the declared wait-resume structure of approval gates.
      - system: Reference implementation — approval lifecycle conformance fixture
        cite: reference-implementation
        kind: internal
        note: The conformance fixture suspends through Dolt and resumes a stored checkpoint with Approved or Rejected using universal CLI flags; it has no authority notification, decision metadata, rejection rollback, or deployed gate.
        evidence:
          classification: conformance_fixture
          checks:
            - path: applications/catalog/testdata/conformance/lifecycle/approval/profile.yaml
              artifact: profile
              assertion: yaml_reference
              field: machine
              target: applications/catalog/testdata/conformance/lifecycle/approval/machine.yaml
              target_artifact: machine
            - path: applications/catalog/testdata/conformance/lifecycle/approval/machine.yaml
              artifact: machine
              assertion: yaml_transition
              match: {state: Idle, signal: Seed, next: AwaitingApproval, action: suspend}
            - path: applications/catalog/testdata/conformance/lifecycle/approval/machine.yaml
              artifact: machine
              assertion: yaml_transition
              match: {state: AwaitingApproval, signal: Approved, next: ApplyingApproval, action: done}
            - path: applications/catalog/testdata/conformance/lifecycle/approval/machine.yaml
              artifact: machine
              assertion: yaml_transition
              match: {state: AwaitingApproval, signal: Rejected, next: Rejected}
            - path: applications/catalog/conformance/lifecycle_test.go
              artifact: go_test
              assertion: go_test
              test: TestLifecycleApprovalSuspendResume
            - path: agent-core/cmd/agent/main_bootstrap_test.go
              artifact: go_test
              assertion: go_test
              test: TestRootCommandHasNoLifecycleOnlyFlags
      - system: Design intent — deployed approval authority
        cite: reference-implementation
        kind: internal
        note: Authority notification, proposal and decision metadata, rejection-triggered rollback, multi-authority chains, and a deployed coding-agent confirmation gate are design intent, not shipped behavior.
        evidence:
          classification: design_intent
    provenance: [reference-implementation, external-corroboration]

  # ---------------------------------------------------------------------------
  - id: convergence-taxonomy
    number: 10
    name: Convergence Taxonomy
    also_known_as: [Outcome Classifier, Trace Classifier]
    confidence: 0
    intent: >
      Classify a completed execution into one of four convergence types by
      reading its transition patterns, so every outcome has an actionable root
      cause without re-running the agent.
    context: >
      Transition Spans and the Machine Interpreter produce a structured
      record of every run. Evaluation that reports only pass or fail says what
      happened, never why, so teams debug the wrong layer — retuning prompts
      when the model was cycling, or raising budget when the agent was diverging.
    problem: >
      How do you turn a pass/fail verdict into an actionable diagnosis of why a
      run succeeded or failed, mechanically and deterministically?
    applicability: >
      Use this for evaluation runs that produce structured state-transition
      traces, especially across many task/model/profile combinations. It is less
      useful when traces lack state data or when infrastructure failures such as
      auth errors, rate limits, and outages dominate; those need separate
      detectors before convergence classification.
    forces:
      - Each failure mode needs a distinct, named root cause and remedy.
      - Classification must be mechanical and deterministic — no inference.
      - The same execution must always yield the same class.
      - Equal pass rates with different quality must be distinguishable.
    solution: >
      Read the execution's state-visit sequence and apply three detectors — a
      retry-cycle counter, a repetition detector, and a terminal-state check —
      then map to exactly one of four types: Clean (succeeded, no retries),
      Recovery (succeeded after retries), Stuck (failed, cycling), Divergent
      (failed, unproductive). Each type points to a different fix; the classifier
      contains no model and runs in microseconds.
    implementation: >
      Classification reads only execution entries or equivalent trace attributes
      such as state, signal, tool, and terminal state. Count retry cycles,
      detect late repetition by sliding window or histogram, and map with a
      four-branch decision tree: succeeded with no cycles is Clean, succeeded
      with cycles is Recovery, failed with repetition is Stuck, failed without
      repetition is Divergent. Infrastructure crashes and timeouts before any
      dispatch are recorded outside the taxonomy. This four-type outcome taxonomy
      is the pattern's intent; the shipped evaluation subsystem
      (agent-core internal/evaluation) instead classifies per-tool metric
      snapshots into a six-class progression — Clean, Converged, Improving, Flat,
      Regressing, NoData — from which aggregate Recovery/Stuck rates are derived
      (see the reference-implementation known use below).
    invariants:
      - id: P10.1
        statement: Convergence classification is a deterministic closed mapping from recorded execution evidence and contains no inference call.
        check:
          kind: executable
          tier: mechanical_test
          command: >-
            cd agent-core && go test -count=1 -v ./internal/evaluation -run
            'TestClassify_MultiToolDeterministicOrder|TestClassify_ByteStableAcrossRepeatedRuns'
          negative_test: TestClassify_ByteStableAcrossRepeatedRuns
    consequences:
      benefits:
        - Actionable diagnostics and quantitative model comparison beyond binary metrics.
        - Regression sensitivity — distribution shifts that a pass rate hides.
      liabilities:
        - Four types can be too coarse; cross-cutting failures (rate limits, outages) need separate detection.
        - The repetition thresholds are sensitive and need empirical tuning.
    grammar:
      within: [machine-interpreter]
      requires: [machine-interpreter, transition-spans]
      contains: []
      enables: []
      overlaps: []
    syntax: >
      Over a Machine Interpreter's record surfaced by Transition Spans, apply
      the Convergence Taxonomy after each run; its four-type distribution across a
      grid is what an evaluation harness aggregates and gates on.
    examples:
      - system: Circuit breaker
        cite: nygard-2018
        kind: external
        note: A small state classifier (closed/open/half-open) derived from observed outcomes drives a distinct action per state — the same "classify state, choose remedy" discipline.
      - system: Result / Either monad
        cite: wadler-monads-1995
        kind: external
        note: Outcomes carried as a small closed set of typed cases the caller must handle exhaustively — the exhaustiveness the four-type taxonomy enforces.
      - system: Process mining
        cite: van-der-aalst-process-mining-2016
        kind: external
        note: Extracts and diagnoses process behaviour from event logs, directly supporting trace-based classification of completed executions without re-running them.
      - system: Workflow conformance checking
        cite: van-der-aalst-conformance-2012
        kind: external
        note: Compares observed traces with expected process models to identify deviations, strengthening the classifier's use of transition patterns as diagnostic evidence.
      - system: Reference implementation — bench convergence reports
        cite: reference-implementation
        kind: internal
        note: The evaluation subsystem classifies each run's per-tool metric snapshots into a six-class progression and derives aggregate Recovery/Stuck rates; per-type colored badges and a thresholded CI regression gate are design intent, not yet shipped.
        evidence:
          classification: shipped
          checks:
            - path: agent-core/internal/evaluation/eval_convergence_test.go
              artifact: go_test
              assertion: go_test
              test: TestClassify_MultiTool
            - path: agent-core/internal/evaluation/eval_report_test.go
              artifact: go_test
              assertion: go_test
              test: TestComputeModelStatsAggregatesRuns
    provenance: [reference-implementation]

  # ---------------------------------------------------------------------------
  - id: operator-port
    number: 11
    name: Operator Port
    also_known_as: [Runtime Probe, Control Plane, Live Inspector]
    confidence: 1
    intent: >
      Attach a control-plane server to the running engine so observers can query
      execution state and controllers can inject signals, all within the
      machine's declared state space.
    context: >
      A Machine Interpreter occupies one named state, holds one pending signal,
      and keeps a bounded history at every moment — inherent properties, not
      debug artifacts. Imperative agents are opaque at runtime: the only control
      is crude (pause, kill, restart) and the only visibility is ad-hoc logs.
    problem: >
      How do you inspect and steer a long-running agent live — pausing before an
      irreversible step, rolling back, injecting termination — without killing
      the process or corrupting the machine?
    applicability: >
      Use this for agents that run long enough to need live progress, operator
      intervention, or parent supervision of many children. For short-lived
      executions, post-hoc Transition Spans and Convergence Taxonomy are
      usually enough. The probe is for administrative intervention; ordinary
      business signals belong in the machine itself.
    forces:
      - Live inspection must not block or alter the transition it observes.
      - Injected control must obey the same machine rules as internal signals.
      - An operator must not be able to reach an undeclared state.
      - The probe must not touch the machine, tools, or business logic.
    solution: >
      Expose the engine through a control plane: a read plane (snapshot and
      event stream) and a control plane (inject a signal, or a lifecycle command
      that translates to an internal signal). Injected signals are validated
      against the machine and routed at the next dispatch cycle; an invalid
      signal is rejected. The machine is the authorization policy; the probe is
      only a messenger.
    implementation: >
      Make the probe opt-in. A recorder appends committed dispatch events to a
      bounded ring; read endpoints expose snapshots and event streams without
      blocking the loop. Control endpoints enqueue signals or lifecycle commands
      for the next dispatch cycle and return a machine-violation error if the
      current state has no matching transition. Bind endpoints locally or add
      authentication; the pattern supplies attachment points, not a security
      model. The shipped monitor profile owns /monitor read, SSE, OpenAPI, and
      exit routes and discovers its ephemeral address from launch output.
      Generic injection and lifecycle-control bindings have conformance tests
      but no production profile; rollback intervention remains design intent.
    invariants:
      - id: P11.1
        statement: Operator read endpoints return bounded cached state without dispatching tools or mutating the observed run.
        check:
          kind: executable
          tier: conformance_test
          command: >-
            cd agent-core && go test -count=1 -v ./internal/tools/rest -run
            'TestMonitorREST_ReadOnlyCachedState|TestMonitorREST_FailureDoesNotMutateState'
          negative_test: TestMonitorREST_FailureDoesNotMutateState
      - id: P11.2
        statement: An injected operator signal is accepted only when the current machine state declares that signal and no conflicting dispatch owns the run.
        check:
          kind: executable
          tier: mechanical_test
          command: >-
            cd agent-core && go test -count=1 -v ./internal/runtime/core -run
            TestSignalAdmission_Outcomes
          negative_test: TestSignalAdmission_Outcomes
    consequences:
      benefits:
        - Live inspection without stopping, and control through declared transitions only.
        - Machine-validated safety — no backdoor, no undeclared state.
      liabilities:
        - The event ring consumes memory proportional to capacity.
        - Open HTTP endpoints are attack surface; the pattern gives attachment points, not a security model.
    grammar:
      within: [machine-interpreter]
      requires: [machine-interpreter, bidirectional-log, approval-gate]
      contains: []
      enables: []
      overlaps: [transition-spans]
    syntax: >
      Over a Machine Interpreter, attach an Operator Port whose read plane
      observes live execution; a complete control plane may reuse Approval Gate
      for pause and Bidirectional Log for rollback.
    examples:
      - system: Kubernetes liveness and readiness probes
        cite: k8s-probes
        kind: external
        note: A control plane queries and acts on a running workload's state through declared endpoints — live inspection and control without killing the process.
      - system: Temporal signals and queries
        cite: temporal-2024
        kind: external
        note: Running workflows expose query handlers for inspection and signal handlers for external steering while preserving workflow state and history.
      - system: Erlang/OTP system messages
        cite: erlang-sys-2024
        kind: external
        note: OTP processes can expose standardized debug, trace, suspend, resume, and status operations through system messages without changing process logic.
      - system: Chaos Engineering
        cite: basiri-chaos-2016
        kind: external
        note: Injecting controlled signals into a running system to observe and steer its behaviour — the disciplined runtime-injection practice this pattern makes safe via machine validation.
      - system: OpenTelemetry (live metrics)
        cite: otel-spec-2024
        kind: external
        note: The same instrumentation feed exported as live gauges and counters for real-time dashboards of an in-progress run.
      - system: Reference implementation — profile-owned monitor routes
        cite: reference-implementation
        kind: internal
        note: The shipped monitor profile owns its read, SSE, and OpenAPI routes; lifecycle exit is provided by agent-core's injected /api/lifecycle/exit endpoint (GH-1264), and the CLI proof discovers the ephemeral loopback address from REST launch output.
        evidence:
          classification: shipped
          checks:
            - path: applications/catalog/agents/runtime-state-reader/profile.yaml
              artifact: profile
              assertion: yaml_reference
              field: rest_definitions
              target: applications/catalog/agents/runtime-state-reader/rest.yaml
              target_artifact: rest_definition
            - path: applications/catalog/agents/runtime-state-reader/rest.yaml
              artifact: rest_definition
              assertion: yaml_value
              field: rest.servers.monitor.address
              equals: "127.0.0.1:0"
            - path: applications/catalog/agents/runtime-state-reader/rest.yaml
              artifact: rest_definition
              assertion: yaml_value
              field: rest.servers.monitor.endpoints.machine_spec.path
              equals: /monitor/machine
            - path: applications/catalog/agents/runtime-state-reader/rest.yaml
              artifact: rest_definition
              assertion: yaml_value
              field: rest.servers.monitor.endpoints.declared_machines.path
              equals: /monitor/machines
            - path: applications/catalog/agents/runtime-state-reader/rest.yaml
              artifact: rest_definition
              assertion: yaml_value
              field: rest.servers.monitor.endpoints.declared_machines.monitor_view
              equals: declared_machines
            - path: applications/catalog/agents/runtime-state-reader/rest.yaml
              artifact: rest_definition
              assertion: yaml_value
              field: rest.servers.monitor.endpoints.current_state.path
              equals: /monitor/state
            - path: applications/catalog/agents/runtime-state-reader/rest.yaml
              artifact: rest_definition
              assertion: yaml_value
              field: rest.servers.monitor.endpoints.tools.path
              equals: /monitor/tools
            - path: applications/catalog/agents/runtime-state-reader/rest.yaml
              artifact: rest_definition
              assertion: yaml_value
              field: rest.servers.monitor.endpoints.metrics.path
              equals: /monitor/metrics
            - path: applications/catalog/agents/runtime-state-reader/rest.yaml
              artifact: rest_definition
              assertion: yaml_value
              field: rest.servers.monitor.endpoints.recent_events.path
              equals: /monitor/events
            - path: applications/catalog/agents/runtime-state-reader/rest.yaml
              artifact: rest_definition
              assertion: yaml_value
              field: rest.servers.monitor.endpoints.event_stream.path
              equals: /monitor/events/stream
            - path: applications/catalog/agents/runtime-state-reader/rest.yaml
              artifact: rest_definition
              assertion: yaml_value
              field: rest.servers.monitor.endpoints.openapi.path
              equals: /monitor/openapi
            - path: agent-core/cmd/agent/main_monitor_test.go
              artifact: go_test
              assertion: go_test
              test: TestMonitorReleaseProfileProof
            - path: agent-core/cmd/agent/main_monitor_test.go
              artifact: go_test
              assertion: go_test
              test: TestMonitorCLIProfileServesUntilControlExit
      - system: Reference implementation — lifecycle-control binding conformance fixture
        cite: reference-implementation
        kind: internal
        note: The runtime conformance fixture proves lifecycle-control queueing and validation, but no production profile selects generic injection, pause, resume, or rollback bindings.
        evidence:
          classification: conformance_fixture
          checks:
            - path: agent-core/internal/tools/rest/server_lifecycle_test.go
              artifact: go_test
              assertion: go_test
              test: TestRESTServer_ControlQueueAndReadPolicyConformance
      - system: Design intent — long-running coding agents
        cite: reference-implementation
        kind: internal
        note: Design intent, not a shipped profile. An operator watching transitions stream past could spot Stuck-pattern cycles and inject a rollback signal to restore the last checkpoint and try a different approach; no shipped agent profile defines a RollbackRequested signal today.
        evidence:
          classification: design_intent
    provenance: [reference-implementation, external-corroboration]

# =============================================================================
# BIBLIOGRAPHY
# Full citations for every external example above, so this file stands alone.
# Keys shared with the monograph's references.yaml resolve to the same works.
# =============================================================================

bibliography:

  reference-implementation:
    text: >
      The declarative coding-agent harness from which these patterns were
      extracted (the single reference implementation described throughout the
      accompanying monograph). Internal example, not an independent source.

  # --- Patterns and architecture ---
  gamma-gof-1994:
    text: "Gamma, E., Helm, R., Johnson, R., & Vlissides, J. (1994). Design Patterns: Elements of Reusable Object-Oriented Software. Addison-Wesley."
  meyer-dbc-1997:
    text: "Meyer, B. (1997). Object-Oriented Software Construction (2nd ed.). Prentice Hall. (Design by Contract.)"
  cockburn-hexagonal-2005:
    text: "Cockburn, A. (2005). Hexagonal Architecture (Ports and Adapters)."
    url: https://alistair.cockburn.us/hexagonal-architecture/
  bernhardt-fcis-2012:
    text: "Bernhardt, G. (2012). Functional Core, Imperative Shell. Destroy All Software Screencasts."
    url: https://www.destroyallsoftware.com/screencasts/catalog/functional-core-imperative-shell
  fowler-event-sourcing-2005:
    text: "Fowler, M. (2005). Event Sourcing."
    url: https://martinfowler.com/eaaDev/EventSourcing.html

  # --- State machines, workflow, and verification ---
  harel-statecharts-1987:
    text: "Harel, D. (1987). Statecharts: A Visual Formalism for Complex Systems. Science of Computer Programming, 8(3), 231-274."
  xstate-2024:
    text: "Khourshid, D. (2024). XState: State Machines and Statecharts for the Modern Web."
    url: https://github.com/statelyai/xstate
  omg-bpmn-2011:
    text: "Object Management Group (2011). Business Process Model and Notation (BPMN), Version 2.0. formal/2011-01-03."
  w3c-scxml-2015:
    text: "World Wide Web Consortium (2015). State Chart XML (SCXML): State Machine Notation for Control Abstraction. W3C Recommendation."
    url: https://www.w3.org/TR/scxml/
  van-der-aalst-workflow-1998:
    text: "van der Aalst, W. M. P. (1998). The Application of Petri Nets to Workflow Management. Journal of Circuits, Systems and Computers, 8(1), 21-66."
  lamport-tla-2002:
    text: "Lamport, L. (2002). Specifying Systems: The TLA+ Language and Tools for Hardware and Software Engineers. Addison-Wesley."
  pnueli-translation-validation-1998:
    text: "Pnueli, A., Siegel, M., & Singerman, E. (1998). Translation Validation. Proceedings of TACAS 1998, LNCS 1384."
  bultan-action-lang-2000:
    text: "Bultan, T. (2000). Action Language: A Specification Language for Model Checking Reactive Systems. Proceedings of ICSE 2000."
  redux-2015:
    text: "Abramov, D., & Clark, A. (2015). Redux: A Predictable State Container for JavaScript Apps."
    url: https://redux.js.org

  # --- Agentic frameworks and orchestration ---
  langgraph-2024:
    text: "LangChain, Inc. (2024). LangGraph: Agent Runtime and Low-Level Orchestration Framework."
    url: https://github.com/langchain-ai/langgraph
  temporal-2024:
    text: "Temporal Technologies (2024). Temporal: Durable Execution Platform Documentation."
    url: https://docs.temporal.io
  aws-step-functions-2024:
    text: "Amazon Web Services (2024). AWS Step Functions Developer Guide: Amazon States Language."
    url: https://docs.aws.amazon.com/step-functions/latest/dg/concepts-amazon-states-language.html
  aws-step-functions-callback-2024:
    text: "Amazon Web Services (2024). AWS Step Functions Developer Guide: Wait for a Callback with the Task Token."
    url: https://docs.aws.amazon.com/step-functions/latest/dg/connect-to-resource.html#connect-wait-token
  yao-react-2023:
    text: "Yao, S., Zhao, J., Yu, D., Du, N., Shafran, I., Narasimhan, K., & Cao, Y. (2023). ReAct: Synergizing Reasoning and Acting in Language Models. Proceedings of ICLR 2023."
    url: https://arxiv.org/abs/2210.03629
  yang-swe-agent-2024:
    text: "Yang, J., Jimenez, C. E., Wettig, A., Lieret, K., Yao, S., Narasimhan, K., & Press, O. (2024). SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering. Proceedings of NeurIPS 2024."
    doi: 10.52202/079017-1601
  meta-harness-2026:
    text: "Lee, Y., Nair, R., Zhang, Q., Lee, K., Khattab, O., & Finn, C. (2026). Meta-Harness: End-to-End Optimization of Model Harnesses. arXiv:2603.28052."
    url: https://arxiv.org/abs/2603.28052
  autoharness-2026:
    text: "Lou, X., Lázaro-Gredilla, M., Dedieu, A., Wendelken, C., Lehrach, W., & Murphy, K. P. (2026). AutoHarness: Improving LLM Agents by Automatically Synthesizing a Code Harness. arXiv:2603.03329."
    url: https://arxiv.org/abs/2603.03329
  kim-scaling-agents-2025:
    text: "Kim, Y., Gu, K., Park, C., Park, C., Schmidgall, S., Heydari, A. A., Yan, Y., Zhang, Z., Zhuang, Y., Liu, Y., Malhotra, M., Liang, P. P., Park, H. W., Yang, Y., Xu, X., Du, Y., Patel, S., Althoff, T., McDuff, D., & Liu, X. (2025). Towards a Science of Scaling Agent Systems. arXiv:2512.08296."
    url: https://arxiv.org/abs/2512.08296
  harbor-harness-optimization-2026:
    text: "Sengupta, B., & Wang, J. (2026). HARBOR: Automated Harness Optimization. arXiv:2604.20938."
    url: https://arxiv.org/abs/2604.20938
  ning-code-as-harness-2026:
    text: "Ning, X., Tieu, K., Fu, D., Wei, T., Li, Z., Bei, Y., et al. (2026). Code as Agent Harness. arXiv:2605.18747."
    url: https://arxiv.org/abs/2605.18747
  wu-stateflow-2024:
    text: "Wu, Y., Yue, T., Zhang, S., Wang, C., & Wu, Q. (2024). StateFlow: Enhancing LLM Task-Solving through State-Driven Workflows. Proceedings of COLM 2024 (arXiv:2403.11322)."
    url: https://arxiv.org/abs/2403.11322
  tiendil-donna-2025:
    text: "Yaletski, A. (2025). Donna: A CLI Tool for Guiding Coding Agents Through Markdown-Defined State-Machine Workflows."
    url: https://github.com/Tiendil/donna
  jido-2025:
    text: "agentjido (2025). Jido: Autonomous Agent Framework for Elixir."
    url: https://github.com/agentjido/jido
  lean4agent-2026:
    text: "Wang, R., Huang, J., Wang, P., Liu, X., Kong, L., & Zhang, T. (2026). Lean4Agent: Formal Modeling and Verification for Agent Workflow and Trajectory. arXiv:2606.06523."
    url: https://arxiv.org/abs/2606.06523
  burns-borg-omega-kubernetes-2016:
    text: "Burns, B., Grant, B., Oppenheimer, D., Brewer, E., & Wilkes, J. (2016). Borg, Omega, and Kubernetes. Communications of the ACM, 59(5), 50-57."

  # --- Security and capabilities ---
  saltzer-schroeder-1975:
    text: "Saltzer, J. H., & Schroeder, M. D. (1975). The Protection of Information in Computer Systems. Proceedings of the IEEE, 63(9), 1278-1308. (Principle of Least Privilege.)"
  dennis-vanhorn-1966:
    text: "Dennis, J. B., & Van Horn, E. C. (1966). Programming Semantics for Multiprogrammed Computations. Communications of the ACM, 9(3), 143-155. (Capabilities.)"
  hardt-oauth-2012:
    text: "Hardt, D. (Ed.) (2012). The OAuth 2.0 Authorization Framework. RFC 6749, IETF."
    url: https://www.rfc-editor.org/rfc/rfc6749

  # --- Interfaces and protocols ---
  openapi-spec-2024:
    text: "OpenAPI Initiative (2024). OpenAPI Specification, Version 3.1.0."
    url: https://spec.openapis.org/oas/v3.1.0
  anthropic-mcp-2024:
    text: "Anthropic (2024). Model Context Protocol (MCP) Specification."
    url: https://modelcontextprotocol.io

  # --- Transactions and recovery ---
  gray-1978:
    text: "Gray, J. (1978). Notes on Data Base Operating Systems. In Operating Systems: An Advanced Course, LNCS 60, 393-481. (Two-phase commit; transactions.)"
  garcia-molina-sagas-1987:
    text: "Garcia-Molina, H., & Salem, K. (1987). Sagas. In Proceedings of ACM SIGMOD 1987."

  # --- Observability ---
  otel-spec-2024:
    text: "OpenTelemetry Authors (2024). OpenTelemetry Specification."
    url: https://opentelemetry.io/docs/specs/otel/
  otel-genai-semconv-2025:
    text: "OpenTelemetry Authors (2025). Semantic Conventions for Generative AI Systems."
    url: https://github.com/open-telemetry/semantic-conventions-genai
  w3c-trace-context-2021:
    text: "World Wide Web Consortium (2021). Trace Context. W3C Recommendation."
    url: https://www.w3.org/TR/trace-context/
  sigelman-dapper-2010:
    text: "Sigelman, B. H., Barroso, L. A., Burrows, M., Stephenson, P., Plakal, M., Beaver, D., Jaspan, S., & Shanbhag, C. (2010). Dapper, a Large-Scale Distributed Systems Tracing Infrastructure. Google Technical Report dapper-2010-1."

  # --- Concurrency and composition ---
  armstrong-2003:
    text: "Armstrong, J. (2003). Making Reliable Distributed Systems in the Presence of Software Errors. PhD thesis, KTH Royal Institute of Technology. (Erlang/OTP supervisor trees.)"
  erlang-sys-2024:
    text: "Erlang/OTP Authors (2024). Erlang/OTP System Architecture Support Libraries: sys."
    url: https://www.erlang.org/doc/apps/stdlib/sys.html
  hewitt-actor-1973:
    text: "Hewitt, C., Bishop, P., & Steiger, R. (1973). A Universal Modular ACTOR Formalism for Artificial Intelligence. In Proceedings of IJCAI 1973."
  dean-mapreduce-2004:
    text: "Dean, J., & Ghemawat, S. (2004). MapReduce: Simplified Data Processing on Large Clusters. In Proceedings of OSDI 2004."

  # --- Reliability, classification, and runtime control ---
  nygard-2018:
    text: "Nygard, M. T. (2018). Release It! Design and Deploy Production-Ready Software (2nd ed.). Pragmatic Bookshelf. (Circuit Breaker.)"
  wadler-monads-1995:
    text: "Wadler, P. (1995). Monads for Functional Programming. In Advanced Functional Programming, LNCS 925. (Either/Result.)"
  van-der-aalst-process-mining-2016:
    text: "van der Aalst, W. M. P. (2016). Process Mining: Data Science in Action (2nd ed.). Springer."
  van-der-aalst-conformance-2012:
    text: "van der Aalst, W. M. P., Adriansyah, A., & van Dongen, B. (2012). Replaying History on Process Models for Conformance Checking and Performance Analysis. Wiley Interdisciplinary Reviews: Data Mining and Knowledge Discovery, 2(2), 182-192."
  k8s-probes:
    text: "Kubernetes Authors. Configure Liveness, Readiness and Startup Probes. Kubernetes Documentation."
    url: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
  basiri-chaos-2016:
    text: "Basiri, A., Behnam, N., de Rooij, R., Hochstein, L., Kosewski, L., Reynolds, J., & Rosenthal, C. (2016). Chaos Engineering. IEEE Software, 33(3), 35-41."
