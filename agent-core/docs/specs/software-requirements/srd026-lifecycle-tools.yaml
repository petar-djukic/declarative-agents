# Copyright (c) 2026 Nokia
# SPDX-License-Identifier: BSD-3-Clause
id: srd026-lifecycle-tools
title: Lifecycle Tool Adapters

problem: |
  History and rollback must run through the same declarative path as ordinary
  agent actions, otherwise lifecycle work stays hidden behind command-line
  glue. Persistence is the srd035 checkpoint port, backed by the srd036 Dolt
  adapter by default. The machine still needs words it can dispatch. Lifecycle
  tool adapters fill that gap: checkpoint_history reads the Dolt commit history
  for inspection, and checkpoint_rollback rewinds a run with the Dolt adapter's
  git-style Revert and then walks the reverted execution entries in reverse,
  handing each restored, receipt-bearing Result to its tool's receipt-consuming
  Undo. Each adapter has typed config and declared signals. Each one also
  documents output, effects, errors, and undo behavior.

  These adapters are not shell wrappers around the agent binary. They are thin
  Go Commands that call the checkpoint port and Dolt adapter operations.
  MachineSpec decides when they run. ToolDef declarations define the contract
  that startup validation can check. The reverse receipt walk lives in the
  rollback adapter, not in the engine, and no adapter interprets a receipt.

goals:
  - G1: Define checkpoint_history and checkpoint_rollback as first-class STL builtin tool adapters.
  - G2: Define typed config, emitted signals, output summaries, and failure behavior for lifecycle tools.
  - G3: Define the checkpoint-port and Dolt-backend requirements for history reads and rollback execution.
  - G4: Define receipt-driven undo and reversibility for lifecycle adapter effects.
  - G5: Require lifecycle tools to be selected by ToolDef declarations and invoked by MachineSpec transitions.
  - G6: Define generic lifecycle control vocabulary for profile-owned pause, rollback, signal injection, and exit behavior.

requirements:
  R1:
    title: Adapter boundary
    items:
      - R1.1: Lifecycle tool adapters SHALL be implemented as STL builtin tools that satisfy core.Command through core.Builder.
      - R1.2: Lifecycle tool adapters SHALL call core checkpoint operations instead of duplicating checkpoint traversal or formatting logic.
      - R1.3: Lifecycle tool adapters SHALL NOT shell into the agent binary to perform history or rollback.
      - R1.4: Lifecycle tool adapters SHALL keep Cobra, profile loading, and CLI flag parsing outside internal/runtime/core.
      - R1.5: MachineSpec transitions SHALL invoke lifecycle adapter tools by declared tool name.

  R2:
    title: checkpoint_history
    items:
      - R2.1: checkpoint_history SHALL inspect one run selected by explicit run_id or the latest-run selector.
      - R2.2: checkpoint_history config SHALL decode into CheckpointHistoryConfig.
      - R2.3: checkpoint_history SHALL read through the srd035 checkpoint port backed by the srd036 Dolt adapter; it SHALL NOT require a key/blob StateStore.
      - R2.4: checkpoint_history SHALL resolve the selected run and read its Dolt commit history for the run branch.
      - R2.5: checkpoint_history SHALL reconstruct the ordered Execution entries (step index, command, from/to state, signal, and receipt presence) through Load.
      - R2.6: checkpoint_history SHALL format a stable digest of the execution entries as Result.Output.
      - R2.7: checkpoint_history SHALL emit ToolDone on success and CommandError on missing backend, missing run, or load failure.
      - R2.8: checkpoint_history SHALL declare no side effects beyond Result.Output.
      - R2.9: checkpoint_history SHALL use noop undo metadata and return an empty receipt.

  R3:
    title: checkpoint_rollback
    items:
      - R3.1: checkpoint_rollback SHALL roll back one run selected by explicit run_id or the latest-run selector to a target step index.
      - R3.2: checkpoint_rollback config SHALL decode into CheckpointRollbackConfig, with the target step distinguishable from missing and zero.
      - R3.3: checkpoint_rollback SHALL operate through the srd035 checkpoint port backed by the srd036 Dolt adapter; it SHALL NOT require a key/blob StateStore or a Workspace.
      - R3.4: checkpoint_rollback SHALL call the Dolt adapter Revert(run_id, step_index) to rewind DB-persisted state to the target step.
      - R3.5: After Revert, checkpoint_rollback SHALL load the target checkpoint's profile path and program digest, verify the current profile closure matches, rebuild the originating selected ToolDefs and REST definitions through the standard factory catalog, then walk reverted entries in reverse dispatch order and hand each restored, receipt-bearing Result to its tool's receipt-consuming Undo.
      - R3.6: checkpoint_rollback SHALL read the registered tool's declared rollback policy before resolving a builder or receipt. It SHALL count undo.strategy noop as a successful reversed no-op without requiring either, skip and log entries declared irreversible, and name skips in the output rather than inferring irreversibility from missing plumbing.
      - R3.7: checkpoint_rollback SHALL emit ToolDone on success and CommandError on missing backend, missing target step, Revert failure, missing rollback registry or non-noop builder, a non-noop reversible or compensatable builder without Reverser, a missing receipt required by a non-noop strategy, or receipt-walk Undo failure.
      - R3.8: checkpoint_rollback output SHALL include the run_id, target step index, the number of entries reverted, skipped irreversible entries, and compensatable entries that require later operator or machine action.
      - R3.9: checkpoint_rollback SHALL classify its own undo as compensatable because it rewrites persisted run state and invokes external compensation.
      - R3.10: checkpoint_rollback SHALL carry operator compensation metadata naming run_id, target step index, and operator_decision as required context; the engine never interprets the per-entry tool receipts it replays.
      - R3.11: Each receipt-walk attempt SHALL be observable on the rollback boundary span as rollback.entry_reversed, rollback.entry_compensation_required, rollback.entry_undo_failed, or rollback.entry_skipped with its step and command; the final structured report SHALL retain aggregate reversed, pending-compensation, failed, and skipped outcomes.
      - R3.12: A target checkpoint with no program identity, a changed program digest, or an unavailable originating builder SHALL produce CommandError and name the incompatibility; it SHALL NOT be classified as an irreversible tool or a clean rollback.

  R4:
    title: Typed config and runtime inputs
    items:
      - R4.1: Lifecycle tool config SHALL use typed structs decoded at builtin registration time.
      - R4.2: Checkpoint selector config SHALL default to latest when omitted.
      - R4.3: Rollback target iteration SHALL be represented so missing and zero are distinguishable.
      - R4.4: Checkpoint backend override, workspace directory, request path, and active profile SHALL remain runtime inputs rather than hidden lifecycle subcommand flags.
      - R4.5: Config decode errors SHALL include the tool name and mismatched field.
      - R4.6: Lifecycle adapters SHALL obtain the resolved checkpoint backend (the Dolt adapter by default) from runtime wiring rather than opening storage themselves.
      - R4.7: A checkpoint backend override (Dolt DSN or repository path) SHALL remain available for shared operator storage, external artifact retention, and tests that need an isolated store.

  R5:
    title: Tool declarations and machine integration
    items:
      - R5.1: Lifecycle adapters SHALL be declared as type builtin ToolDefs.
      - R5.2: Lifecycle adapter declarations SHALL include init, visibility, emits, parameters or config, output, side_effects, reversibility, undo, errors, and relationships.
      - R5.3: Lifecycle adapter declarations SHOULD be internal unless a profile intentionally exposes them to dynamic tool dispatch.
      - R5.4: Lifecycle profiles or tool-selection files SHALL select checkpoint_history and checkpoint_rollback by name.
      - R5.5: Startup validation SHALL reject lifecycle MachineSpec actions whose declared emits are not handled by transitions.
      - R5.6: Lifecycle adapter tools SHALL remain reusable across lifecycle profiles by changing ToolDef config, not Go source.

  R6:
    title: Observability and errors
    items:
      - R6.1: Lifecycle adapters SHOULD record command name, selected run, target step index, reverted-entry count, and resulting signal in command output, tracing, or structured diagnostics.
      - R6.2: Error messages SHALL distinguish checkpoint-backend errors, run/step selection errors, Dolt Revert errors, and receipt-walk Undo errors.
      - R6.3: Partial rollback failures SHALL preserve the error detail needed by an operator to choose retry, resume, or stop, including which reverted entry's Undo failed; declared manual compensation SHALL remain an explicit work item rather than an infrastructure failure.
      - R6.4: Lifecycle adapters SHALL not report a clean rollback when irreversible entries were skipped, compensations remain pending, or a receipt-walk Undo failed.

  R7:
    title: Generic lifecycle control vocabulary
    items:
      - R7.1: exit_agent or an equivalent configured lifecycle word SHALL be profile-selectable vocabulary, not a Knowledge Manager-specific handler.
      - R7.2: exit_agent input SHALL include or resolve exit reason, requested status, drain policy, and optional checkpoint policy from ToolDef config or validated runtime input.
      - R7.3: exit_agent output SHALL be structured JSON and include requested status, reason, drain policy, drain result when present, checkpoint ID when created, and resulting signal.
      - R7.4: exit_agent SHALL emit AgentExitRequested, AgentExited, CommandError, or a profile-declared equivalent signal that MachineSpec handles explicitly.
      - R7.5: exit_agent side effects SHALL declare lifecycle_state mutation, optional checkpoint_state mutation, and optional server shutdown when paired with REST server control.
      - R7.6: A successful exit_agent SHALL be irreversible with confirmation and emit no rollback receipt because completed shutdown cannot be automatically reversed; rollback SHALL report an explicit irreversible skip, and any failed exit SHALL also emit no receipt. A distinct pre-exit cancellation word MAY be compensatable when a profile declares a real cancellation path.
      - R7.7: Pause, resume, rollback request, and signal injection control words SHALL be selected by ToolDef declarations and MachineSpec transitions rather than hard-coded HTTP handlers.
      - R7.8: Lifecycle control words triggered by REST events SHALL consume validated event payloads from rest_server_await or rest_await_event.
      - R7.9: exit_agent SHALL use validated REST event payload fields as runtime overrides for ToolDef defaults.
      - R7.10: exit_agent SHALL emit CommandError when previous Result output intended as event context is malformed.

non_goals:
  - Defining the final request-file schema for lifecycle profiles.
  - Defining the complete operator CLI syntax for running lifecycle profiles.
  - Replacing core checkpoint operations.
  - Requiring all existing tools to have specialized rollback behavior before lifecycle adapters ship.
  - Defining lifecycle behavior that only one agent profile or Knowledge Manager handler can use.
  - Letting REST handlers exit, pause, resume, roll back, or inject signals without a declared lifecycle word.

acceptance_criteria:
  - id: AC1
    criterion: Lifecycle adapters are documented as STL builtin tools around core checkpoint operations, not shell wrappers around the agent binary.
    traces: [R1.1, R1.2, R1.3, R1.4, R1.5]

  - id: AC2
    criterion: checkpoint_history requirements define typed config, a checkpoint-port/Dolt dependency, Dolt commit-history reads through Load, formatted output, emitted signals, no side effects, and noop undo with an empty receipt.
    traces: [R2.1, R2.2, R2.3, R2.4, R2.5, R2.6, R2.7, R2.8, R2.9]

  - id: AC3
    criterion: checkpoint_rollback requirements define typed config, a checkpoint-port/Dolt dependency, a target step, verified reconstruction of the target declarative program, Dolt Revert plus a reverse receipt walk over restored Results, per-entry observability, skip-and-log of irreversible entries, output, emitted signals, and compensatable undo metadata with receipts left opaque to the engine.
    traces: [R3.1, R3.2, R3.3, R3.4, R3.5, R3.6, R3.7, R3.8, R3.9, R3.10, R3.11, R3.12]

  - id: AC4
    criterion: Runtime inputs stay outside hidden lifecycle subcommand flags, typed config keeps required values explicit, and lifecycle adapters use the resolved checkpoint backend (Dolt by default) unless an override is supplied.
    traces: [R4.1, R4.2, R4.3, R4.4, R4.5, R4.6, R4.7]

  - id: AC5
    criterion: ToolDef declarations and lifecycle MachineSpec transitions select lifecycle adapters by name and validate emitted signals.
    traces: [R5.1, R5.2, R5.3, R5.4, R5.5, R5.6]

  - id: AC6
    criterion: Lifecycle adapter failures and partial rollback states are observable enough for operator recovery.
    traces: [R6.1, R6.2, R6.3, R6.4]

  - id: AC7
    criterion: Generic lifecycle control vocabulary defines exit_agent inputs, outputs, signals, side effects, undo behavior, and REST-triggered routing through MachineSpec.
    traces: [R7.1, R7.2, R7.3, R7.4, R7.5, R7.6, R7.7, R7.8, R7.9, R7.10]

test_trace_plan:
  - test_suite: test-rel02.0-lifecycle
    covers: [AC1, AC2, AC3, AC4, AC5, AC6]
  - test_suite: test-rel03.0-rest-tools
    covers: [AC7]
  - existing_go_tests:
      - internal/tools/lifecycle/checkpoint_history_test.go
      - internal/tools/lifecycle/checkpoint_rollback_test.go
      - internal/tools/lifecycle/checkpoint_compensation_test.go
      - internal/tools/lifecycle/receipt_rollback_test.go
      - internal/tools/catalog/receipt_contract_test.go
      - internal/runtime/core/checkpoint_test.go
      - internal/runtime/core/checkpoint_adapters_test.go
      - internal/runtime/core/dolt_checkpoint_revert_test.go

depends_on:
  - srd_id: srd001-core-types
    symbols_used: [Builder, Command, Result]
  - srd_id: srd005-agentic-loop
    symbols_used: [MachineSpec]
  - srd_id: srd022-builtin-tools
    symbols_used: [BuiltinRegistry, ToolDef]
  - srd_id: srd025-rollback-lifecycle
    symbols_used: [Checkpoint, Execution, Entry]
  - srd_id: srd035-checkpoint-port
    symbols_used: [Checkpoint, Execution, Entry, Receipt]
  - srd_id: srd036-dolt-state-persistence
    symbols_used: [DoltCheckpoint, Revert]

related_documents:
  - cfg-tool-authoring-standard
  - cfg-tool-declaration-format
  - cfg-runtime-contract
  - cfg-rest-tool-format
  - srd022-builtin-tools
  - srd025-rollback-lifecycle
  - srd027-rest-tool-runtime

implementation:
  - internal/tools/lifecycle/checkpoint.go
  - internal/tools/lifecycle/receipt_rollback.go
  - internal/tools/catalog/receipt_contract.go
  - internal/runtime/core/checkpoint.go
  - internal/runtime/core/checkpoint_adapters.go

implementation_status:
  done:
    - testdata/conformance/lifecycle/history/profile.yaml and testdata/conformance/lifecycle/rollback/profile.yaml select the lifecycle machines and builtin tool declarations.
    - Hidden lifecycle subcommands have been removed from cmd/agent; lifecycle operations use the universal profile-first runtime path.
  planned:
    - checkpoint_history reimplemented as a Dolt commit-history read through the srd035 checkpoint port.
    - checkpoint_rollback reimplemented as Dolt Revert plus a reverse receipt walk over restored Results, replacing the core rollback traversal and Workspace restore.
    - exit_agent lifecycle control word.
    - REST-triggered lifecycle control profiles for exit, pause, rollback request, resume, and signal injection.

diagrams:
  requirements_trace: |
    ```mermaid
    flowchart LR
      Problem["Problem: History and rollback must run through the same declar..."] --> Goals["Goals: Lifecycle Tool Adapters"]
      Goals --> R1["R1: Adapter boundary"]
      Goals --> R2["R2: checkpoint_history"]
      Goals --> R3["R3: checkpoint_rollback"]
      Goals --> R4["R4: Typed config and runtime inputs"]
      Goals --> R5["R5: Tool declarations and machine integration"]
      Goals --> R6["R6: Observability and errors"]
      Goals --> R7["R7: Generic lifecycle control vocabulary"]
      AC1["AC1: Lifecycle adapters are documented as STL builtin tools ar..."]
      R1 --> AC1
      AC2["AC2: checkpoint_history requirements define typed config, Stat..."]
      R2 --> AC2
      AC3["AC3: checkpoint_rollback requirements define typed config, Sta..."]
      R3 --> AC3
      AC4["AC4: Runtime inputs stay outside hidden lifecycle subcommand f..."]
      R4 --> AC4
      AC5["AC5: ToolDef declarations and lifecycle MachineSpec transition..."]
      R5 --> AC5
      AC6["AC6: Lifecycle adapter failures and partial rollback states ar..."]
      R6 --> AC6
      AC7["AC7: Generic lifecycle control vocabulary defines exit_agent i..."]
      R7 --> AC7
    ```
