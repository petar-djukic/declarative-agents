# Copyright (c) 2026 Nokia
# SPDX-License-Identifier: BSD-3-Clause
id: srd014-spec-graph
title: Specification Graph Loader and Validator

problem: |
  As the number of SRDs, use cases, and test suites grows, manually
  verifying cross-references becomes error-prone. Broken touchpoints,
  orphaned SRDs, uncovered requirements, and stale citations can hide
  silently. A specification graph loads all artifacts into a labeled
  property graph and runs automated consistency checks.

goals:
  - G1: Parse SRDs, use cases, test suites, roadmaps, and SPECIFICATIONS.yaml into typed structs.
  - G2: Build a labeled property graph connecting all artifacts by their relationships.
  - G3: Validate cross-artifact consistency and report findings.
  - G4: Corpus-level loading from a project directory.
  - G5: Run deterministic jurist charter suites over a target directory.

requirements:
  R1:
    title: Parsing
    items:
      - R1.1: ParseSRD reads a single SRD YAML file into an SRD struct with requirements, acceptance criteria, and dependencies.
      - R1.2: ParseUseCase reads a use case YAML file into a UseCase struct with flow, touchpoints, and success criteria.
      - R1.3: ParseTestSuite reads a test suite YAML file into a TestSuite struct with test cases and traces.
      - R1.4: ParseRoadmap reads road-map.yaml into a Roadmap struct with releases and use case references.
      - R1.5: ParseSpecIndex reads SPECIFICATIONS.yaml into a SpecIndex struct.
      - R1.6: Tagged goals, non-goals, flows, out-of-scope items, and touchpoints SHALL reject unsupported or ambiguous YAML shapes with artifact and field diagnostics; every accepted list element SHALL produce exactly one ordered logical item.

  R2:
    title: Graph construction
    items:
      - R2.1: "Graph uses a directed labeled property graph with node kinds: release, srd, req-group, req-item, ac, use-case, test-suite, test-case."
      - R2.2: "Edge relationships include: contains, depends-on, traces, covers, tests."
      - R2.3: BuildGraph populates the graph from a Corpus of all parsed artifacts.

  R3:
    title: Validation checks
    items:
      - R3.1: Detect orphaned SRDs not referenced by any use case.
      - R3.2: Detect broken touchpoints referencing non-existent SRDs.
      - R3.3: Detect broken citations in test cases referencing non-existent acceptance criteria.
      - R3.4: Detect uncovered requirement items without test case traces.
      - R3.5: Detect uncovered acceptance criteria without test case traces.
      - R3.6: Detect depends_on violations referencing non-existent SRDs.
      - R3.7: Detect releases without test suites.
      - R3.8: Findings carry check identifier, severity level, human-readable message, and available provenance.

  R4:
    title: Corpus loading
    items:
      - R4.1: LoadCorpus reads all specification artifacts from a project directory tree.
      - R4.2: SRD files are discovered under docs/specs/software-requirements/.
      - R4.3: Use case files under docs/specs/use-cases/, test suites under docs/specs/test-suites/.
      - R4.4: Road-map from docs/road-map.yaml, spec index from docs/SPECIFICATIONS.yaml.

  R5:
    title: Jurist charter suites
    items:
      - R5.1: Jurist charter YAML declares suite identity, check IDs, check kinds, severity, target globs, and check-specific configuration.
      - R5.2: Supported charter check kinds include grep_check, ref_check, consistency_check, and spec_corpus.
      - R5.3: Charter execution is deterministic, local, read-only, and does not call an LLM or the network.
      - R5.4: The jurist target directory comes from runtime data such as --directory, while profile or request data names one or more charter suite paths.
      - R5.5: Charter findings record suite ID, check ID, check kind, severity, message, and any available file and line provenance.
      - R5.6: The built-in spec_corpus charter preserves the current specification corpus validation behavior.
      - R5.7: grep_check target search runs through a declared rg exec word, followed by a machine-visible reduction step; pkg/spec does not scan target files for grep matches.
      - R5.8: ref_check target extraction and file/directory inventory loading run through a declared external scan word, followed by a machine-visible reduction step; pkg/spec retains only plan lowering and finding reduction.
      - R5.9: consistency_check YAML loading and required-path inventory traversal run through a declared external scan word; the reducer retains YAML-path semantics and line provenance without opening or statting target files.

non_goals:
  - Generating specifications from code.
  - Modifying specification files.
  - UI or interactive reporting.
  - Using LLMs, network APIs, or non-deterministic inputs during validation.

acceptance_criteria:
  - id: AC1
    criterion: ParseSRD, ParseUseCase, ParseTestSuite, ParseRoadmap, and ParseSpecIndex produce typed structs from YAML files without silently dropping malformed tagged-list elements.
    traces: [R1.1, R1.2, R1.3, R1.4, R1.5, R1.6]

  - id: AC2
    criterion: BuildGraph constructs a directed labeled property graph from a Corpus with correct node kinds and edge relationships.
    traces: [R2.1, R2.2, R2.3]

  - id: AC3
    criterion: Validate detects orphaned SRDs, broken touchpoints and citations, uncovered items and ACs, depends_on violations, and releases without test suites.
    traces: [R3.1, R3.2, R3.3, R3.4, R3.5, R3.6, R3.7, R3.8]

  - id: AC4
    criterion: LoadCorpus discovers and parses all artifacts from the standard directory layout.
    traces: [R4.1, R4.2, R4.3, R4.4]

  - id: AC5
    criterion: Jurist charter suites load from YAML, run deterministic check kinds, expose grep_check and ref_check as declared external scans plus finding reduction steps, and report provenance-rich findings.
    traces: [R5.1, R5.2, R5.3, R5.4, R5.5, R5.7, R5.8, R5.9]

  - id: AC6
    criterion: The built-in spec_corpus charter reproduces the current specification corpus validation behavior used by mage audit.
    traces: [R5.2, R5.6]

depends_on: []

diagrams:
  requirements_trace: |
    ```mermaid
    flowchart LR
      Problem["Problem: As the number of SRDs, use cases, and test suites gro..."] --> Goals["Goals: Specification Graph Loader and Validator"]
      Goals --> R1["R1: Parsing"]
      Goals --> R2["R2: Graph construction"]
      Goals --> R3["R3: Validation checks"]
      Goals --> R4["R4: Corpus loading"]
      AC1["AC1: ParseSRD, ParseUseCase, ParseTestSuite, ParseRoadmap, Par..."]
      R1 --> AC1
      AC2["AC2: BuildGraph constructs a directed labeled property graph f..."]
      R2 --> AC2
      AC3["AC3: Validate detects orphaned SRDs, broken touchpoints and ci..."]
      R3 --> AC3
      AC4["AC4: LoadCorpus discovers and parses all artifacts from the st..."]
      R4 --> AC4
    ```
