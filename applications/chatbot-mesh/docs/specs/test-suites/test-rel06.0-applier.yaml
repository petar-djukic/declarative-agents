# Copyright (c) 2026 Nokia
# SPDX-License-Identifier: BSD-3-Clause
id: test-rel06.0-applier
title: Test Suite for Release 06.0 Declarative Applier Agent
release: "06.0"

overview: |
  This suite validates the applier agent (srd006): the declarative deployment API
  that replaces the standard-library Go provisioner. It drives a decided values patch
  through validate, apply, and verify, asserting that a non-conforming patch is
  rejected by the chart-schema dry-run, that a valid patch is applied as a values-file
  rollout and verified via kubectl rollout status, that an apply or verify failure
  rolls back, and that the authority boundary holds. The shipped profile and
  recording-fake tracer cover every declared terminal, real Helm validates the
  chart schema, and integration:applierLive proves apply and rollback on kind.

traces:
  - rel06.0-uc001-declarative-applier

preconditions:
  - agent-core is available for the request runtime, REST server, and exec tools.
  - The applier profile is deployed with its state, apply, and rollout servers on the Service the provisioner served; its image carries helm and kubectl.
  - The chart carries a values.schema.json, and the apply path is reached only by the creator, authenticated apart from chat.

test_cases:
  - use_case: rel06.0-uc001-declarative-applier
    name: Applier validates, applies, and verifies a values change through the deployment API
    integration_target: mage integration:applier
    go_test: TestAssertApplierCallsRejectsMissingAndForbidden, TestApplierAuthorityProblemRejectsTransportAuthority, TestApplierScenariosCoverEveryTerminal, TestFakeScriptsClassifyTheDeclaredInvocations
    status: implemented
    description: |
      Drives a decided values patch against the shipped applier profile: the apply
      endpoint receives the patch, the applier validates it against the chart's
      values schema by a helm dry-run, rejecting a non-conforming patch with a client
      error and no rollout; for a valid patch it materializes the values as a file and
      applies helm upgrade --reuse-values --atomic; it verifies the rollout via
      kubectl rollout status and reports the phase, rolling the release back on a
      verify stall. Asserts the authority boundary: no invocation carries an endpoint,
      a credential, or per-field --set construction.

      What it rests on, so the status is not read for more than it says: helm and
      kubectl are recording fakes on the applier's PATH, taking their exit codes
      from the scenario. That is what lets one run walk every terminal of both
      machines -- four on the apply machine, three on the rollout read -- since a
      machine of exec words tells its outcomes apart only by which word failed. It is
      evidence about the machine, the arguments it constructs, and the responses it
      maps. It is not evidence that real helm and kubectl behave as the declarations
      assume, nor that the applier works against a cluster; the chart schema is
      covered by its own case below against real helm, and integration:applierLive
      supplies the on-cluster evidence (GH-735).

      Two hops this case does not cover: the creator posting the patch through its
      declared client, which Integration.ControlPlane drives against a fake
      deployment API, and the network-layer restriction of the apply path to
      creator-labelled pods, which TestApplierApplyStaysCreatorOnly asserts over the
      rendered chart.
    inputs:
      valid_patch: add a RAG source through a values-plane patch
      invalid_patch: a patch violating the chart values schema
      agents: [creator, applier]
    expected:
      invalid_patch_rejected_by_schema_dry_run_no_rollout: true
      valid_patch_applied_as_values_file_reuse_values_atomic: true
      no_per_field_set_construction: true
      rollout_verified_via_kubectl_rollout_status: true
      apply_or_verify_failure_rolls_back: true
      patch_carries_no_endpoint_or_credential: true
    traces:
      - rel06.0-uc001-declarative-applier S1
      - rel06.0-uc001-declarative-applier S2
      - rel06.0-uc001-declarative-applier S3
      - rel06.0-uc001-declarative-applier S4
      - rel06.0-uc001-declarative-applier S5
      - srd006-applier AC1
      - srd006-applier AC2
      - srd006-applier AC3
      - srd006-applier AC4
      - srd006-applier AC5
      - srd003-chatbot-deployment AC4

  - use_case: rel06.0-uc001-declarative-applier
    name: The chart schema rejects a non-conforming values patch
    integration_target: mage test (magefiles/applier_schema_test.go, GH-732)
    go_test: TestChartSchemaAcceptsAConformingPatch, TestChartSchemaRejectsANonConformingPatch, TestChartSchemaConstrainsTheRAGUnitName
    status: done
    description: |
      The applier's validate step is a helm dry-run against the chart's
      values.schema.json, so the rejection is only as real as the schema. The case
      above cannot prove it: its fake helm returns whatever exit code the scenario
      sets, and a schema that accepted every document would pass it unchanged.

      Real helm renders the real chart with two fixtures. The rejection must name
      /ragUnits/0/name and the pattern it violated, not merely exit non-zero -- a
      chart failing to render for an unrelated reason exits non-zero too. A third
      assertion pins the constraint the fixture rests on, so loosening the schema
      reads as the guard being removed rather than as a broken fixture.

      Substitution: the applier issues helm upgrade --dry-run, which needs a
      reachable cluster. helm template validates against the same schema without one.
      The applier's own command form is proven on a cluster by GH-735.
    inputs:
      conforming: testdata/integration/applier-values/conforming.yaml
      non_conforming: testdata/integration/applier-values/non-conforming.yaml
    expected:
      conforming_patch_renders: true
      non_conforming_patch_rejected_by_name_and_pattern: true
      rag_unit_name_constrained_to_a_dns_label: true
    traces:
      - rel06.0-uc001-declarative-applier S2
      - srd006-applier AC2

  - use_case: rel06.0-uc001-declarative-applier
    name: An applier-enabled chart renders a coherent surface
    integration_target: mage test (magefiles/applier_chart_test.go, GH-733)
    go_test: TestApplierServiceTargetsItsDeployment, TestApplierMountsEveryProfileItStarts, TestApplierRendersItsWholeSurface
    status: done
    description: |
      helm/ci/kind-values.yaml disables the applier, because its image bundles helm,
      kubectl, and the chart while the smoke tests kind-load only the runtime image.
      Every cluster-level test therefore stands up a mesh without it, and the
      packaging path that would carry the applier into a cluster is proven here.

      Asserted over the staged render: the Service selects the pods the Deployment
      creates, and the profile the Deployment's args name is a path the profiles
      volume actually projects. The second is GH-485 at the render -- the applier
      Deployment once mounted a profile the staging list did not copy, and an enabled
      applier started with nothing to run.
    inputs:
      rendered_chart: staged chart with applier.enabled
    expected:
      service_targets_the_deployment_pods: true
      every_started_profile_is_projected: true
      whole_applier_object_set_renders: true
    traces:
      - rel06.0-uc001-declarative-applier S1
      - srd006-applier AC1

  - use_case: rel06.0-uc001-declarative-applier
    name: Live applier infrastructure failures are bounded and classified
    integration_target: mage test (magefiles/applier_live_infrastructure_test.go, GH-872)
    go_test: TestApplierLiveInfrastructureHealthy, TestApplierLiveInfrastructureClassifiesHostAPIFailure, TestApplierLiveInfrastructureClassifiesPodAPIFailure, TestApplierLiveInitialReadinessFailureCollectsDiagnostics, TestApplierLiveDiagnosticsAreBoundedAndKeepFailures, TestApplierLiveDiagnosticsIncludeInitPreviousAndOrderedBootstrapEvidence, TestApplierLiveApplyFailureClassification
    status: implemented
    description: |
      Deterministic command-runner tests pin the live tier's infrastructure
      boundary without claiming a cluster run succeeded. Before an apply, the host
      must reach the Kubernetes readyz endpoint and kubectl running inside the real
      applier Deployment must reach the same endpoint using the pod's mounted
      service account and cluster network.

      An initial Deployment-readiness failure and a later apply assertion failure
      both collect evidence under one bounded deadline. The ordered bundle retains
      Helm state and actual release Secret sizes; Deployment, ReplicaSet, pod
      condition/restart/waiting/termination state and readiness events; stage-chart
      init output; current and previous applier output; node capacity; the chart
      ConfigMap archive key; and the applier Service/endpoints. A failed apply
      recheck is reported as infrastructure unavailable while a healthy recheck
      preserves the machine or Helm assertion as a semantic failure. Timeout and
      partial-command-failure fixtures prove diagnostics terminate and keep all
      sections and partial evidence even when the cluster is unhealthy.
    inputs:
      healthy: host and applier-pod readyz probes answer ok
      host_failure: host Kubernetes API probe fails
      pod_failure: in-pod Kubernetes API probe fails after the host probe succeeds
      diagnostics: timed-out and failed command-runner responses
    expected:
      healthy_paths_reach_semantic_assertions: true
      host_api_failure_classified_as_infrastructure: true
      pod_network_or_api_failure_classified_as_infrastructure: true
      initial_readiness_failure_captures_bootstrap_evidence: true
      diagnostics_have_one_overall_deadline: true
      init_current_and_previous_logs_are_retained: true
      healthy_recheck_preserves_semantic_failure: true
      unhealthy_recheck_does_not_claim_live_success: true
    traces:
      - rel06.0-uc001-declarative-applier S3
      - srd006-applier AC3

  - use_case: rel06.0-uc001-declarative-applier
    name: A live on-cluster rollout through the applier
    integration_target: mage integration:applierLive (GH-735, GH-751)
    status: done
    description: |
      What the fake CLIs cannot stand in for. The applier image is built on the
      runtime under test, loaded into kind, and the mesh installed with the
      applier enabled; a values patch then goes through the apply endpoint and
      the release moves to a new revision, which is the evidence a 200 is not --
      the fake tier returns one of those without helm existing.

      The compensating leg is live too. The tier stages a test-only post-upgrade
      hook and permissions into both the installed chart and the chart at /chart.
      A reserved valid parameter patch activates the hook. Helm first waits for
      the ordinary upgrade, then the hook uses the applier image's real kubectl
      to regress the chatbot Deployment before helm upgrade returns. The
      applier's subsequent declared kubectl rollout status therefore times out
      deterministically, reaches RollingBack, and invokes real helm rollback.
      The case requires the 500 rolled_back response, two new Helm revisions
      (upgrade and rollback), restored computed values, and a ready chatbot
      Deployment. It does not expect the revision number to move backwards:
      Helm records a rollback as a new revision.

      This hook closes the narrow timing window without racing an out-of-band
      scale or patch against helm --wait. It exists only in the live tier's
      temporary staged chart; the shipped chart and its RBAC are unchanged.

      The same non-conforming document the schema case validates locally is
      rejected here against the real chart schema, leaving the release untouched.

      Immediately after the host Helm install, the tier reads and decodes the
      actual revision-1 release Secret before waiting for the applier Deployment;
      later checks retain the same release-budget assertion across the apply and
      rollback revisions. Before each live apply, bounded host and in-pod readyz
      probes distinguish Kubernetes API or pod-network loss from the semantic
      proof.

      Initial readiness and later failures retain the ordered bootstrap bundle
      described above plus kind node logs and namespace evidence before an owned
      cluster is deleted. The bundle is written below build/kind-evidence with
      Deployment/ReplicaSet/pod state, init and previous logs, resource capacity,
      chart delivery, Service/endpoints, events, and release Secret size. A reused
      cluster is neither harvested by the release hook nor deleted.
      Infrastructure-unavailable is a distinct failure classification and does not
      satisfy this case: status done records what a healthy completed run proves,
      not that an unhealthy environment passed.

      Finding this tier produced: the applier Role could not read the Secrets
      helm queries to find its own release, so an apply failed at the validate
      step and the caller was told their values were bad. Nor could it read or
      write the NetworkPolicy, ServiceAccount, Role, and RoleBinding the chart it
      re-renders contains, nor the ReplicaSets and Pods helm --wait watches. The
      Role now covers what the chart it manages holds, and every rule traces to
      an observed denial.
    inputs:
      cluster: kind with the applier image built and loaded
      patch: testdata/integration/applier-values/conforming.yaml
      rollback_trigger: testdata/integration/applier-values/rollback-trigger.yaml
      rejected: testdata/integration/applier-values/non-conforming.yaml
    expected:
      revision_one_release_secret_asserted_before_readiness: true
      failed_owned_bootstrap_retains_evidence_before_delete: true
      reused_cluster_not_harvested_or_deleted_by_release: true
      real_helm_upgrade_changes_the_release: true
      post_upgrade_verify_failure_reaches_rolled_back: true
      real_helm_rollback_restores_prior_computed_values: true
      chatbot_deployment_recovers_after_rollback: true
      rollout_endpoint_reports_complete_with_real_counts: true
      non_conforming_patch_rejected_on_the_cluster: true
    traces:
      - rel06.0-uc001-declarative-applier S3
      - rel06.0-uc001-declarative-applier S4
      - srd006-applier AC3

  - use_case: rel06.0-uc001-declarative-applier
    name: A broken rollout read is distinguishable from a progressing one, and the counts reach the panel
    integration_target: mage test (magefiles/applier_rollout_test.go, magefiles/provisioning_workflow_orchestrator_intake_test.go, GH-686)
    go_test: TestApplierRolloutMachineSeparatesBrokenRead, TestApplierRolloutEndpointMapsThreeOutcomes, TestApplierRolloutTerminalMappingsAgree, TestApplierRolloutCountsWordContract, TestApplierRolloutCountsRenderJSON, TestRolloutCountsSurviveTheCreatorHop, TestRolloutCountsSurviveTheProvisioningWorkflowOrchestratorHop, TestPanelRolloutInterfaceMatchesWhatIsServed
    status: done
    description: |
      Asserts over the declared profiles that the rollout read reaches three
      outcomes rather than two. kubectl rollout status exits non-zero for a
      rollout that is still progressing and for one it could not read at all, so
      a phase taken from that exit code alone reported an absent Deployment, a
      wrong kubeconfig, a denied RBAC read, or an unreachable API server as
      ongoing progress -- in a panel that polls every 3s, indefinitely.

      A second read of the Deployment object separates the two and supplies the
      counts, and the three terminal states map to three responses from two
      signals. The hop assertions follow the counts to the panel and compare the
      declared TypeScript interface against what the poll actually serves, in
      both directions.
    inputs:
      declared_config: the applier, creator, and provisioning-workflow-orchestrator profiles and the panel's provisioningApi.ts
      deployment_shapes: rolled out, no replica ready yet, freshly created with no revision annotation
    expected:
      unreadable_deployment_returns_gateway_error_not_a_phase: true
      complete_and_progressing_remain_distinguishable: true
      both_phases_carry_ready_desired_revision: true
      counts_survive_the_creator_and_provisioning_workflow_orchestrator_hops: true
      panel_interface_declares_exactly_what_is_served: true
    traces:
      - rel06.0-uc001-declarative-applier S7
      - srd006-applier AC3

  - use_case: rel06.0-uc001-declarative-applier
    name: The rendered chart routes provisioning to the provisioning-workflow-orchestrator
    integration_target: mage test (magefiles/provisioning_route_render_test.go, GH-681)
    go_test: TestProvisioningIngressRoutesToTheProvisioningWorkflowOrchestrator, TestApplierApplyStaysCreatorOnly, TestProvisioningWorkflowOrchestratorAdmitsOnlyTheIngressControllerOnIntent, TestNoProvisioningRouteWithoutTheControlPlane
    status: done
    description: |
      Asserts over the rendered chart, not the template text, that browser
      provisioning traffic and the apply surface agree on one authority path: the
      ingress /provisioning backend is the provisioning-workflow-orchestrator Service, the applier policy
      still admits only creator-labelled pods on the apply port, and the provisioning-workflow-orchestrator
      policy admits the ingress controller on the intent port alone.

      This is the check that would have caught GH-502. Each template was individually
      correct -- the ingress named a real Service, the policy named a real selector --
      and only the pair read together showed the route was dead.
    inputs:
      rendered_chart: default values with applier.enabled and networkPolicy.enabled
    expected:
      provisioning_ingress_backend: provisioning-workflow-orchestrator Service
      applier_apply_ingress_from: creator-labelled pods only
      provisioning_workflow_orchestrator_policy_admits_ingress_controller_on_intent_port: true
      provisioning_workflow_orchestrator_monitor_and_control_not_ingress_reachable: true
    traces:
      - rel06.0-uc001-declarative-applier S6
      - srd006-applier AC6
      - srd003-chatbot-deployment AC7
      - srd004-provisioning-workflow-orchestrator AC6

  - use_case: rel06.0-uc001-declarative-applier
    name: The panel route reaches only the authorized surface
    integration_target: mage integration:policyProof (GH-682)
    status: done
    description: |
      Proves against a live cluster with NetworkPolicy enforcement active that the
      browser provisioning route reaches the provisioning-workflow-orchestrator and that the applier apply
      port refuses traffic from any pod not labelled creator.

      Enforcement is asserted before anything else. A CNI that ignores NetworkPolicy
      would make every negative assertion here pass vacuously, which is the failure
      mode this case exists to rule out, so an unenforcing cluster fails rather than
      skips.

      The proof pairs the pinned Kubernetes 1.36.1 kind node with Calico v3.32.1,
      whose tested Kubernetes range is 1.34-1.36. It resolves the CNI, node, and
      kube-controllers images to checked-in amd64/arm64 digests, host-pulls those
      immutable images, retags them to the references in the immutable Calico
      manifest, and kind-loads every image before applying the manifest. The
      BusyBox policy probe is digest-pinned and locally loaded by the same rule,
      with imagePullPolicy Never. The kind node therefore never needs registry TLS
      for CNI bootstrap or proof execution.

      A failed bootstrap still owns the cluster creation it completed. Before
      deleting that cluster, the failure path exports kind logs and captures
      kube-system descriptions, pod inventory, and container logs; reused clusters
      remain untouched.
    inputs:
      cluster: kind Kubernetes 1.36.1 with compatible, locally loaded Calico v3.32.1
      positive: request through the ingress to the provisioning-workflow-orchestrator intent surface
      negative: request to the applier apply port from a non-creator pod
    expected:
      network_policy_enforcement_asserted_active: true
      cni_images_digest_pinned_and_loaded_before_manifest_apply: true
      policy_probe_image_digest_pinned_locally_loaded_and_never_pulled: true
      failed_owned_bootstrap_captures_kube_system_evidence_before_delete: true
      reused_cluster_never_deleted_or_inspected_for_failure_evidence: true
      browser_route_reaches_provisioning-workflow-orchestrator: true
      applier_apply_refuses_non_creator: true
      creator_instance_port_not_ingress_reachable: true
      provisioning_workflow_orchestrator_reaches_creator_instance: true
      creator_reaches_applier_apply: true
    traces:
      - rel06.0-uc001-declarative-applier S6
      - srd006-applier AC6
      - srd003-chatbot-deployment AC7
      - srd004-provisioning-workflow-orchestrator AC6
      - srd005-creator AC6
