# Copyright (c) 2026 Nokia
# SPDX-License-Identifier: BSD-3-Clause
rest:
  version: v1
  auth:
    none:
      type: none
  limits:
    local_provider:
      timeout: 130s
      read_timeout: 130s
      max_request_bytes: 1048576
      max_response_bytes: 1048576
      redirect:
        mode: none
      network:
        schemes: [http]
        hosts: [127.0.0.1, localhost]
        ports: [11434, 18085, 18095]
    local_chat_requests:
      timeout: 130s
      read_timeout: 130s
      max_request_bytes: 1048576
      max_response_bytes: 1048576
      redirect:
        mode: none
      network:
        schemes: [http]
        hosts: [127.0.0.1, localhost]
        ports: [18080]
    local_chatbot_control:
      timeout: 30s
      read_timeout: 5s
      max_request_bytes: 16384
      max_response_bytes: 65536
      redirect:
        mode: none
      network:
        schemes: [http]
        hosts: [127.0.0.1, localhost]
        ports: [18081]
    local_chatbot_monitor:
      timeout: 5s
      read_timeout: 5s
      max_request_bytes: 4096
      max_response_bytes: 1048576
      redirect:
        mode: none
      network:
        schemes: [http]
        hosts: [127.0.0.1, localhost]
        ports: [18082]

  clients:
    embedding:
      base_url: http://127.0.0.1:11434
      auth_ref: none
      limits_ref: local_provider
      operations:
        embed_query:
          method: POST
          path: /api/embeddings
          params:
            body_schema:
              type: object
              required: [input]
              properties:
                input: {type: string}
            body_source: previous_result
            input_mapping:
              input: $.message
            carry_forward: [input]
          body:
            # One environment reference names the query embedding model for both this
            # request and declare_query_model, which puts that identity into command
            # state for the per-RAG comparison (srd002 R3.3). Keep the two in step: a
            # drift between them would compare against an identity nothing embedded with.
            model: ${CHATBOT_EMBEDDING_MODEL:-qwen3-embedding:8b}
            prompt: "{{ params.input }}"
          success: {status: [200], signal: QueryEmbedded}
          response:
            output:
              embedding: $.embedding
          side_effects:
            - kind: external_api
              target: ollama.embeddings
              state: read_only
          reversibility:
            classification: reversible
            undo: noop
    rag:
      base_url: http://127.0.0.1:18085
      auth_ref: none
      limits_ref: local_provider
      operations:
        query:
          method: POST
          path: /api/v1/rag/query
          # The operation, method, path, auth, and network policy remain trusted
          # configuration. Only the authority comes from the current item in the
          # declared topology; the allowlist above still constrains it.
          base_url_source: command_state
          base_url_selector: $from(rag_unit).base_url
          params:
            body_schema:
              type: object
              required: [query_embeddings]
              properties:
                query_embeddings: {type: array}
            body_source: command_state
            input_mapping:
              query_embeddings: $from(embed_query).mapped.embedding
          body:
            query_embeddings: "{{ params.query_embeddings }}"
            n_results: 5
          success: {status: [200], signal: QueryResponded}
          failures:
            # An embedding-space mismatch (srd001 R2.3) is a 400 the RAG server
            # returns; map it to QueryRejected so the machine excludes this source
            # (srd002 R3.3) distinctly from a degraded (CommandError) RAG.
            - {status: [400], signal: QueryRejected}
          response:
            output:
              ids: $.ids
              documents: $.documents
              distances: $.distances
              embedding_model: $.embedding_model
          side_effects:
            - kind: external_api
              target: rag_server.query
              state: read_only
          reversibility:
            classification: reversible
            undo: noop
    provisioning-workflow-orchestrator:
      # The declared client the provisioning panel uses to delegate a user's
      # provisioning intent to the provisioning-workflow-orchestrator (srd002 R5.1, srd004 R1). Fixed
      # authority: the chatbot never edits deployment config itself and never feeds a
      # runtime host, URL, or credential into this client. The base URL is a
      # values-driven Service DNS name, defaulting to loopback for a local run.
      base_url: ${PROVISIONING_WORKFLOW_ORCHESTRATOR_URL:-http://127.0.0.1:18100}
      auth_ref: none
      limits_ref: local_provider
      operations:
        delegate_provision:
          method: POST
          path: /api/v1/provision
          params:
            body_schema:
              type: object
              required: [rag_name, collection]
              properties:
                rag_name: {type: string}
                collection: {type: string}
                embedding_model: {type: string}
                directory: {type: string}
            body_source: previous_result
            input_mapping:
              rag_name: $.rag_name
              collection: $.collection
              embedding_model: $.embedding_model
              directory: $.directory
          body:
            rag_name: "{{ params.rag_name }}"
            collection: "{{ params.collection }}"
            embedding_model: "{{ params.embedding_model }}"
            directory: "{{ params.directory }}"
          success: {status: [200], signal: ProvisionDelegated}
          side_effects:
            - kind: external_api
              target: provisioning-workflow-orchestrator.provision
              state: intent_delegated
          reversibility:
            classification: reversible
            undo: noop

  servers:
    chatbot_chat:
      address: 127.0.0.1:18080
      limits_ref: local_chat_requests
      endpoints:
        chat:
          method: POST
          path: /api/v1/chat
          binding: machine_request
          request:
            body_schema:
              type: object
              required: [message]
              properties:
                message: {type: string}
                history: {type: array}
          machine_request:
            profile: profile.yaml
            machine: request-machine.yaml
            timeout: 130s
            request:
              body:
                message: $.message
                history: $.history
            response:
              # Keyed by terminal state, not signal: compose_response reports the
              # unresolved selectors for every source outcome that did not happen,
              # so the turn can reach LLMResponded on either ResponseComposed or
              # CommandError and both must map to the same 200 (rest-tool-format
              # machine_request_response_mapping, GH-615).
              terminal_states:
                LLMResponded:
                  status: 200
                  content_type: application/json
                  body:
                    answer: $.answer
                    metadata: $.metadata
                Failed:
                  status: 500
                  content_type: application/json
                  body:
                    error: command_error
                    message: $.message
        monitor_proxy:
          method: GET
          path: /monitor-proxy/{agent}/{path...}
          binding: monitor_proxy
          # Same-origin reverse proxy so the SPA reads every mesh agent's monitor
          # state and SSE stream through this server (agent-core monitor_proxy,
          # GH-358). The upstreams are the monitored-agents surface srd003
          # co-generates; keep them in sync with agents/chatbot/ui/ui.yaml monitored_agents and
          # the SPA agent list in agents/chatbot/ui/app/src/monitorApi.ts.
          monitor_proxy:
            upstreams:
              chatbot: http://127.0.0.1:18082
              rag0: http://127.0.0.1:18087
              rag1: http://127.0.0.1:18097
              collector: http://127.0.0.1:18193
          request:
            path:
              agent: {type: string}
              path: {type: string}
        ui:
          method: GET
          path: /ui/{path...}
          binding: static_assets
          static_assets:
            root: "${CHATBOT_UI_ROOT:-agents/chatbot/ui/app/dist}"
            index: index.html
            spa: true
          request:
            path:
              path: {type: string}
        root_redirect:
          method: GET
          path: /
          binding: redirect
          redirect:
            location: /ui/
            status: 302

    chatbot_control:
      address: 127.0.0.1:18081
      limits_ref: local_chatbot_control
      queue:
        name: chatbot_control
        capacity: 8
        timeout: 30s
      shutdown:
        timeout: 5s
        drain_policy: drain_then_stop
      endpoints:
        health:
          method: GET
          path: /api/lifecycle/health
          binding: health

    monitor:
      address: 127.0.0.1:18082
      limits_ref: local_chatbot_monitor
      lifecycle_exit: {disabled: true}
      queue:
        name: chatbot_monitor
        capacity: 8
        overflow: reject
        timeout: 100ms
      shutdown:
        timeout: 2s
        drain_policy: drain_then_stop
        stop_listeners: true
        unblock_await_signal: ServerStopped
      endpoints:
        machine_spec:  {method: GET, path: /monitor/machine,       binding: read_state,    monitor_view: machine_spec}
        current_state: {method: GET, path: /monitor/state,         binding: read_state,    monitor_view: current_state}
        tools:         {method: GET, path: /monitor/tools,         binding: read_state,    monitor_view: tools}
        metrics:       {method: GET, path: /monitor/metrics,       binding: read_state,    monitor_view: metrics}
        recent_events: {method: GET, path: /monitor/events,        binding: read_state,    monitor_view: events}
        event_stream:  {method: GET, path: /monitor/events/stream, binding: stream_events, monitor_view: events}
