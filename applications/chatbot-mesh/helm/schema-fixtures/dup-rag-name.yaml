# Copyright (c) 2026 Nokia
# SPDX-License-Identifier: BSD-3-Clause
# Two RAG units with the same name. values.schema.json cannot express by-key
# uniqueness, so the rag-units template fail-guards it (GH-465): helm render fails.
ragUnits:
  - {name: rag0, description: First duplicate., collection: corpus, embeddingModel: qwen3-embedding:8b, replicas: 1}
  - {name: rag0, description: Second duplicate., collection: corpus2, embeddingModel: qwen3-embedding:8b, replicas: 1}
