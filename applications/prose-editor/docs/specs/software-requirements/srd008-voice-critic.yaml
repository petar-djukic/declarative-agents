id: srd008-voice-critic
title: Voice Critic State Machine and Pangram Boundary
status: specified

problem: |
  Prose Editor needs an assurance service that evaluates the immutable original
  and selected style candidate without inheriting an editor's reasoning or
  authority. The critic must judge semantic preservation, structural intent,
  voice match, tightening quality, unsupported additions, and anchor-copy risk.
  A critic that sees editor rationale can repeat the editor's assumptions; a
  critic that can edit or persist content can approve its own repair.

  Pangram adds a separate privacy and factory-semantics problem. Its API stores
  uploaded text and returns an asynchronous task whose HTTP 200 body may still
  be pending or failed. An API key is not consent, a successful HTTP status is
  not a completed analysis, and its document fractions are not a verdict.
  This SRD requires an exact-hash consent gate, separate original and candidate
  submissions and polls, explicit advisory comparison, and a verdict produced
  independently before Pangram is consulted. Runtime declarations, the helper,
  profile, machine, REST configuration, and tests remain planned.

goals:
  - "G1: Define one flat independent critic machine with closed states, signals, actions, labels, and terminals."
  - "G2: Bind every assessment to exact immutable original and style-candidate bytes and hashes."
  - "G3: Independently query voice-rag and assess all six required finding categories."
  - "G4: Require exact-hash affirmative consent before either Pangram upload."
  - "G5: Scan and compare original and candidate separately while keeping Pangram advisory."
  - "G6: Return pass, warn, or reject, with exactly one responsible editor stage for every reject."
  - "G7: Exclude editing, workproduct, GitHub, filesystem, and corpus-write authority."

applicability:
  release: "00.0"
  implementation_status: documentation_only_runtime_planned
  owner: voice-critic
  machine_form: planned_flat_machine_spec
  runtime_machine_path: agents/voice-critic/machine.yaml
  runtime_machine_status: not_implemented
  critic_helper_status: not_implemented

request_contract:
  source: workflow-orchestrator
  required_fields:
    - original_content
    - original_artifact_id
    - original_content_hash
    - candidate_content
    - candidate_artifact_id
    - candidate_content_hash
    - candidate_stage
    - saga_id
    - stage
    - attempt
    - invocation_id
    - idempotency_key
    - target_voice
    - bounded_acceptance_policy
    - pangram_consent
    - policy_digest
  pangram_consent_fields:
    - decision
    - original_content_hash
    - candidate_content_hash
    - disclosure_identity
    - decided_at
    - expires_at
  constraints:
    - stage equals critique and candidate_stage equals style.
    - Content fields contain exact bytes and never paths, repository locators, or editor reasoning.
    - The declared lowercase SHA-256 hashes equal the supplied exact byte sequences.
    - The original identity names the immutable captured original and the candidate identity names the selected style attempt.
    - Affirmative consent is valid only when decision equals granted, both consent hashes exactly equal the supplied content hashes, the disclosure identity matches configured policy, and the decision has not expired.
    - Denied, absent, expired, malformed, or hash-mismatched consent produces a non-granted advisory status and cannot select an upload action.
    - Request fields cannot select an endpoint, route, credential, model, corpus, helper operation, tool, path, ref, verdict, responsible stage, or publication behavior.

independence_contract:
  supplied_information:
    - immutable original bytes, identity, hash, and byte length
    - immutable selected style-candidate bytes, identity, hash, and byte length
    - target voice and bounded acceptance policy
    - saga, attempt, invocation, and policy identities
    - exact-hash Pangram consent record
  prohibited_information:
    - editor prompts, chain-of-thought, hidden reasoning, self-critique, or confidence
    - editor conversation, model history, retry rationale, or rejected alternatives
    - structure, voice, or style editor verdict recommendations
    - editor-selected Pangram interpretation
  retrieval_rule: |
    The critic formulates its own bounded comparison query, obtains a configured
    embedding, and queries only voice-rag. It does not reuse the voice editor's
    selected anchors or anchor assessment. Retrieved passages supply voice
    evidence only; they cannot add facts, claims, or acceptance authority.
  testable_identity: |
    Two requests with identical allowed fields and configured dependencies must
    present the same critic input regardless of editor reasoning or conversation
    state. Planned schemas reject every prohibited field, and the critic profile
    has no selector or tool that can read editor execution state.

assessment_contract:
  required_findings:
    semantic_preservation:
      question: Does the candidate preserve propositions, scope, polarity, modality, causality, qualifications, uncertainty, citations, numbers, terminology, and conclusions?
      reject_stage_rule: Assign structure when organization changes meaning; voice when voice realization changes meaning; style when tightening changes meaning.
    structural_intent:
      question: Does the candidate retain required content and the intended organization without unsupported movement, omission, duplication, or connector claims?
      reject_stage_rule: Assign structure.
    voice_match:
      question: Does the candidate match the target voice using independently retrieved rhetorical-role and register evidence without generic normalization?
      reject_stage_rule: Assign voice.
    tightening_quality:
      question: Does tightening remove supported verbosity without flattening voice, clipping prose, or changing normative force?
      reject_stage_rule: Assign style.
    unsupported_additions:
      question: Does the candidate add any fact, example, implication, recommendation, citation, number, term, conclusion, or retrieved passage content absent from the original?
      reject_stage_rule: Assign the earliest editor stage whose output introduced the addition, based on supplied lineage evidence; if only final candidate evidence is available, use the stage whose operation class matches the changed span and record bounded uncertainty.
    anchor_copy_risk:
      question: Does the candidate copy suspicious exact, normalized, or distinctive phrasing from independently retrieved voice passages?
      reject_stage_rule: Assign voice.
  finding_fields:
    - category
    - status
    - severity
    - bounded_summary
    - original_span_ids
    - candidate_span_ids
    - evidence_ids
    - responsible_stage_if_reject
  verdict_rules:
    - verdict is exactly one of pass, warn, or reject.
    - pass requires all six findings and no reject-severity finding.
    - warn requires all six findings, no reject-severity finding, and at least one bounded advisory concern.
    - reject requires at least one reject-severity finding and exactly one top-level responsible_stage in structure, voice, or style.
    - pass and warn require top-level responsible_stage to be null.
    - Every reject-severity finding names the same top-level responsible_stage or explains why it is downstream evidence of that stage.
    - Feedback is bounded, actionable for the responsible editor, and contains no rewritten replacement content.
    - The evaluation binds both supplied hashes and is invalid if either differs.

pangram_advisory_contract:
  external_authority: petar-djukic/coding-skills/.claude/scripts/pangram.py
  report_authority: petar-djukic/coding-skills/.claude/scripts/pangram_report.py
  consent_gate:
    status_values: [granted, denied, absent, expired, hash_mismatch, invalid]
    dominance_rule: |
      Only granted may reach compose_pangram_original_request. Every path to
      pangram_submit_original or pangram_submit_candidate is dominated by
      CriticEvaluationValid and PangramConsentGranted. The granted status is
      helper-derived from the exact supplied bytes, both hashes, and the consent
      record before any REST action. No API key, prior consent, model statement,
      environment variable, retry, or operator assumption substitutes for it.
  provider_shape:
    submit:
      method: POST
      path: /task
      fixed_body_field: public_dashboard_link
      fixed_body_value: false
      success_body: task_id
    poll:
      method: GET
      path: /task/{task_id}
      terminal_success_stage: STAGE_SUCCESS
      terminal_failure_stage: STAGE_FAILED
      pending: every other documented or bounded nonterminal stage
    evidence_fields:
      - prediction_short
      - headline
      - fraction_ai
      - fraction_ai_assisted
      - fraction_human
      - num_ai_segments
      - num_ai_assisted_segments
      - num_human_segments
      - windows
      - dashboard_link
    interpretation:
      - There is no single top-level AI score.
      - A STAGE_FAILED HTTP 200 body is failure, not a zero-risk result.
      - Window thresholds are uncalibrated advisory worklist evidence.
      - Dashboard identity is recorded as sensitive bounded metadata and never made public by configuration.
  scan_order:
    - Submit and poll the original task to completed, failed, denied, unavailable, or timed_out.
    - Submit and poll the candidate task separately to completed, failed, denied, unavailable, or timed_out.
    - Never reuse one task ID, response body, span map, or hash for the other document.
    - Compare document fractions, segment counts, and available paragraph-window evidence only after both records have explicit status.
  comparison_status_values:
    - compared
    - original_unavailable
    - candidate_unavailable
    - both_unavailable
    - consent_denied
    - consent_absent
    - consent_invalid
    - comparison_failed
  advisory_only_rule: |
    evaluate_voice_candidate runs, parses, and passes deterministic hash,
    category, verdict, responsible-stage, and feedback validation before the
    consent predicate or any Pangram action. The final verdict predicates read
    only $from(critic_validation_report).verdict. Pangram comparison is added
    only by the fixed response composer after the verdict branch is selected.
    Therefore no Pangram value, movement, label, failure, or absence can produce
    or change pass, warn, reject, or responsible_stage.
  unavailable_rule: |
    Denial, absence, expiry, hash mismatch, provider rejection, authentication
    failure, insufficient credits, rate limit, malformed response, STAGE_FAILED,
    timeout, helper failure, and comparison failure each produce an explicit
    bounded advisory status. They never become a favorable score and never
    silently accept. A pass or warn remains possible only from independently
    validated non-Pangram findings, with the unavailable status present in the
    response.

response_contract:
  schema_version: prose-editor.critic-response/v1
  required_fields:
    - schema_version
    - outcome
    - verdict
    - original_artifact_id
    - original_content_hash
    - candidate_artifact_id
    - candidate_content_hash
    - findings
    - responsible_stage
    - feedback
    - voice_evidence_provenance
    - pangram
    - critic_metadata
  outcomes:
    completed:
      verdict_values: [pass, warn, reject]
      meaning: The independent evaluation and cross-field validation completed.
    failed:
      verdict: null
      meaning: Request, retrieval, model, parse, helper, budget, or response preparation failed before a valid verdict.
  pangram_fields:
    - consent_status
    - original_scan
    - candidate_scan
    - comparison
  scan_record_fields:
    - document_kind
    - content_hash
    - status
    - task_id
    - request_identity
    - submitted_at
    - completed_at
    - fractions
    - segment_counts
    - paragraph_windows
    - dashboard_identity
    - bounded_error
  critic_metadata:
    - saga_id
    - stage
    - attempt
    - invocation_id
    - idempotency_key
    - producer
    - producer_version
    - query_identity
    - embedding_model
    - evidence_ids
    - helper_identity_and_version
    - report_schema_identities
    - report_hashes
    - scalar_predicate_outcomes
    - bounded_failure_category
  mutation_rule: |
    The response is data only. The critic does not edit either document, return
    replacement prose, write a file or workproduct, mutate a saga or manifest,
    invoke GitHub, or write a retrieval corpus.

flat_machine_contract:
  initial_state: awaiting_request
  terminal_states: [passed, warned, rejected, failed]
  budgets:
    machine:
      max_iterations: 128
      max_tokens: 300000
      max_duration: 10m0s
      max_consecutive_parse_errors: omitted
    policy:
      model_evaluation_limit: 1
      pangram_poll_limit_per_document: 60
      pangram_poll_interval: configured_bounded_delay
      rule: |
        There is no model retry or verdict-revision cycle. Each Pangram poll
        cycle increments a helper-validated count and exits at the hard limit.
        Machine duration and iteration limits are independent upper bounds.

  authority:
    selected_srd003_words:
      - validate_critic_request
      - parse_critic_request_report
      - critic_request_is_valid
      - compose_critic_voice_query
      - embed_critic_voice_query
      - query_critic_voice_rag
      - compose_critic_evaluation_context
      - evaluate_voice_candidate
      - parse_critic_evaluation
      - compose_critic_validation_input
      - validate_critic_evaluation
      - parse_critic_validation_report
      - critic_evaluation_is_valid
      - check_pangram_consent
      - compose_pangram_original_request
      - pangram_submit_original
      - pangram_poll_original
      - compose_pangram_candidate_request
      - pangram_submit_candidate
      - pangram_poll_candidate
      - compose_pangram_observation_input
      - interpret_pangram_observation
      - parse_pangram_observation
      - pangram_observation_is_completed
      - pangram_observation_is_pending
      - pangram_poll_budget_available
      - wait_pangram_poll
      - compose_pangram_scan_evidence
      - compose_pangram_consent_unavailable
      - compose_pangram_original_unavailable
      - compose_pangram_candidate_unavailable
      - compose_pangram_comparison_unavailable
      - compose_pangram_denied_comparison_input
      - compose_pangram_comparison_input
      - compare_pangram_results
      - parse_pangram_comparison
      - critic_verdict_is_pass
      - critic_verdict_is_warn
      - compose_critic_pass_response
      - compose_critic_warn_response
      - compose_critic_reject_response
      - compose_critic_failed_response
    prohibited_word_groups:
      - orchestrator_workproducts
      - orchestrator_children
      - orchestrator_github
      - corpus_wrapper
    prohibited_words:
      - generate_structure_edit
      - generate_voice_edit
      - generate_style_edit
      - query_structure_rag
      - query_tightening_rag
      - write_original
      - write_attempt
      - append_manifest_revision
      - project_manifest
    prohibited_capabilities:
      - edit, rewrite, repair, or return replacement document content
      - receive or infer editor reasoning
      - read or write a filesystem, saga workproduct, manifest, or repository
      - invoke git, gh, GitHub, generic shell, or arbitrary REST
      - write Chroma, ingest a corpus, or route among corpora
      - accept or persist an artifact
      - publish, commit, push, or create a pull request

  action_bindings:
    validate_request: {word: validate_critic_request, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    parse_request: {word: parse_critic_request_report, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    check_request: {word: critic_request_is_valid, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    compose_voice_query: {word: compose_critic_voice_query, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    embed_voice_query: {word: embed_critic_voice_query, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    query_voice_evidence: {word: query_critic_voice_rag, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    compose_evaluation_context: {word: compose_critic_evaluation_context, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    evaluate_candidate: {word: evaluate_voice_candidate, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    parse_evaluation: {word: parse_critic_evaluation, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    compose_validation_input: {word: compose_critic_validation_input, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    validate_evaluation: {word: validate_critic_evaluation, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    parse_validation: {word: parse_critic_validation_report, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    check_evaluation: {word: critic_evaluation_is_valid, authority: srd003 word_vocabulary.editor_and_critic_retrieval}
    check_consent: {word: check_pangram_consent, authority: srd003 word_vocabulary.critic_pangram}
    compose_original_request: {word: compose_pangram_original_request, authority: srd003 word_vocabulary.critic_pangram}
    submit_original: {word: pangram_submit_original, authority: srd003 word_vocabulary.critic_pangram}
    poll_original: {word: pangram_poll_original, authority: srd003 word_vocabulary.critic_pangram}
    compose_candidate_request: {word: compose_pangram_candidate_request, authority: srd003 word_vocabulary.critic_pangram}
    submit_candidate: {word: pangram_submit_candidate, authority: srd003 word_vocabulary.critic_pangram}
    poll_candidate: {word: pangram_poll_candidate, authority: srd003 word_vocabulary.critic_pangram}
    compose_observation_input: {word: compose_pangram_observation_input, authority: srd003 word_vocabulary.critic_pangram}
    interpret_observation: {word: interpret_pangram_observation, authority: srd003 word_vocabulary.critic_pangram}
    parse_observation: {word: parse_pangram_observation, authority: srd003 word_vocabulary.critic_pangram}
    check_observation_completed: {word: pangram_observation_is_completed, authority: srd003 word_vocabulary.critic_pangram}
    check_observation_pending: {word: pangram_observation_is_pending, authority: srd003 word_vocabulary.critic_pangram}
    check_poll_budget: {word: pangram_poll_budget_available, authority: srd003 word_vocabulary.critic_pangram}
    wait_poll: {word: wait_pangram_poll, authority: srd003 word_vocabulary.critic_pangram}
    compose_scan_evidence: {word: compose_pangram_scan_evidence, authority: srd003 word_vocabulary.critic_pangram}
    compose_consent_unavailable: {word: compose_pangram_consent_unavailable, authority: srd003 word_vocabulary.critic_pangram}
    compose_original_unavailable: {word: compose_pangram_original_unavailable, authority: srd003 word_vocabulary.critic_pangram}
    compose_candidate_unavailable: {word: compose_pangram_candidate_unavailable, authority: srd003 word_vocabulary.critic_pangram}
    compose_comparison_unavailable: {word: compose_pangram_comparison_unavailable, authority: srd003 word_vocabulary.critic_pangram}
    compose_denied_comparison_input: {word: compose_pangram_denied_comparison_input, authority: srd003 word_vocabulary.critic_pangram}
    compose_comparison_input: {word: compose_pangram_comparison_input, authority: srd003 word_vocabulary.critic_pangram}
    compare_advisory: {word: compare_pangram_results, authority: srd003 word_vocabulary.critic_pangram}
    parse_comparison: {word: parse_pangram_comparison, authority: srd003 word_vocabulary.critic_pangram}
    check_verdict_pass: {word: critic_verdict_is_pass, authority: srd003 word_vocabulary.critic_pangram}
    check_verdict_warn: {word: critic_verdict_is_warn, authority: srd003 word_vocabulary.critic_pangram}
    compose_pass_response: {word: compose_critic_pass_response, authority: srd003 word_vocabulary.critic_pangram}
    compose_warn_response: {word: compose_critic_warn_response, authority: srd003 word_vocabulary.critic_pangram}
    compose_reject_response: {word: compose_critic_reject_response, authority: srd003 word_vocabulary.critic_pangram}
    compose_failed_response: {word: compose_critic_failed_response, authority: srd003 word_vocabulary.critic_pangram}

  states:
    - {name: awaiting_request, meaning: Initial state; accepts one orchestrator-supplied immutable comparison request.}
    - {name: validating_request, meaning: Run the fixed request and exact-hash consent validator.}
    - {name: parsing_request_report, meaning: Parse request-helper JSON.}
    - {name: checking_request, meaning: Compare one request_status scalar.}
    - {name: composing_voice_query, meaning: Render the critic-owned voice comparison query.}
    - {name: embedding_voice_query, meaning: Obtain a configured embedding.}
    - {name: querying_voice_evidence, meaning: Query only voice-rag.}
    - {name: composing_evaluation_context, meaning: Render independent evaluation context without Pangram or editor reasoning.}
    - {name: evaluating_candidate, meaning: Ask the configured model for six findings and a provisional verdict.}
    - {name: parsing_evaluation, meaning: Parse the raw model JSON.}
    - {name: composing_validation_input, meaning: Render the fixed evaluation-validator input.}
    - {name: validating_evaluation, meaning: Check hashes, categories, verdict-stage relation, and feedback bounds.}
    - {name: parsing_validation_report, meaning: Parse validator JSON.}
    - {name: checking_evaluation, meaning: Compare one evaluation_status scalar.}
    - {name: checking_consent, meaning: Compare one validated consent_gate_status scalar.}
    - {name: composing_original_request, meaning: Render the private original submission.}
    - {name: submitting_original, meaning: POST only the original to Pangram.}
    - {name: polling_original, meaning: GET only the original task.}
    - {name: composing_original_observation_input, meaning: Render original poll-helper input.}
    - {name: interpreting_original_observation, meaning: Classify original task stage and evidence.}
    - {name: parsing_original_observation, meaning: Parse original advisory record.}
    - {name: checking_original_completed, meaning: Compare original status with completed.}
    - {name: composing_original_completed_evidence, meaning: Preserve completed original advisory output under its stable label.}
    - {name: checking_original_pending, meaning: Compare original status with pending.}
    - {name: checking_original_poll_budget, meaning: Compare original poll count with the hard limit.}
    - {name: waiting_original_poll, meaning: Wait one fixed bounded interval before the next original poll.}
    - {name: composing_original_unavailable, meaning: Record explicit original advisory unavailability.}
    - {name: composing_candidate_request, meaning: Render the private candidate submission.}
    - {name: submitting_candidate, meaning: POST only the candidate to Pangram.}
    - {name: polling_candidate, meaning: GET only the candidate task.}
    - {name: composing_candidate_observation_input, meaning: Render candidate poll-helper input.}
    - {name: interpreting_candidate_observation, meaning: Classify candidate task stage and evidence.}
    - {name: parsing_candidate_observation, meaning: Parse candidate advisory record.}
    - {name: checking_candidate_completed, meaning: Compare candidate status with completed.}
    - {name: composing_candidate_completed_evidence, meaning: Preserve completed candidate advisory output under its stable label.}
    - {name: checking_candidate_pending, meaning: Compare candidate status with pending.}
    - {name: checking_candidate_poll_budget, meaning: Compare candidate poll count with the hard limit.}
    - {name: waiting_candidate_poll, meaning: Wait one fixed bounded interval before the next candidate poll.}
    - {name: composing_candidate_unavailable, meaning: Record explicit candidate advisory unavailability.}
    - {name: composing_all_pangram_unavailable, meaning: Record denied or invalid consent for both scans.}
    - {name: composing_comparison_input, meaning: Render separate original and candidate advisory records for comparison.}
    - {name: comparing_advisory, meaning: Compute bounded advisory movement without reading the verdict.}
    - {name: parsing_comparison, meaning: Parse advisory comparison JSON.}
    - {name: composing_comparison_unavailable, meaning: Record explicit comparison failure.}
    - {name: checking_verdict_pass, meaning: Compare only the independently validated verdict with pass.}
    - {name: checking_verdict_warn, meaning: Compare only the independently validated verdict with warn.}
    - {name: composing_pass_response, meaning: Add advisory evidence to the fixed pass response.}
    - {name: composing_warn_response, meaning: Add advisory evidence to the fixed warn response.}
    - {name: composing_reject_response, meaning: Add advisory evidence to the fixed reject response.}
    - {name: composing_failed_response, meaning: Render a failure without a verdict.}
    - {name: passed, meaning: Terminal; completed response has verdict pass and no responsible stage.}
    - {name: warned, meaning: Terminal; completed response has verdict warn and no responsible stage.}
    - {name: rejected, meaning: Terminal; completed response has verdict reject and exactly one responsible stage.}
    - {name: failed, meaning: Terminal; no valid critic verdict is returned.}

  signal_classes:
    seed: [SeedRequest]
    generic_factory:
      - ToolDone
      - ToolFailed
      - LLMResponded
      - CommandError
      - BudgetExhausted
    parsed:
      - CriticRequestReportParsed
      - CriticRequestReportInvalid
      - CriticEvaluationParsed
      - CriticEvaluationInvalid
      - CriticValidationReportParsed
      - CriticValidationReportInvalid
      - PangramObservationParsed
      - PangramObservationInvalid
      - PangramComparisonParsed
      - PangramComparisonInvalid
    predicate:
      - CriticRequestValid
      - CriticRequestRejected
      - CriticEvaluationValid
      - CriticEvaluationRejected
      - PangramConsentGranted
      - PangramConsentNotGranted
      - PangramObservationCompleted
      - PangramObservationNotCompleted
      - PangramObservationPending
      - PangramObservationFailed
      - PangramPollRetryAllowed
      - PangramPollTimedOut
      - PangramPollDelayElapsed
      - CriticPassed
      - CriticNotPassed
      - CriticWarned
      - CriticRejected
    compose:
      - CriticVoiceQueryReady
      - CriticEvaluationContextReady
      - CriticValidationInputReady
      - PangramOriginalRequestReady
      - PangramCandidateRequestReady
      - PangramObservationInputReady
      - PangramScanEvidenceReady
      - PangramUnavailableEvidenceReady
      - PangramComparisonInputReady
      - CriticResponsePassed
      - CriticResponseWarned
      - CriticResponseRejected
      - CriticResponseFailed
    rest:
      - CriticQueryEmbedded
      - CriticQueryRejected
      - CriticVoiceEvidenceReturned
      - QueryRejected
      - PangramOriginalSubmitted
      - PangramOriginalDenied
      - PangramOriginalObserved
      - PangramOriginalPollDenied
      - PangramCandidateSubmitted
      - PangramCandidateDenied
      - PangramCandidateObserved
      - PangramCandidatePollDenied

  transition_contract:
    notation: |
      Each row is one required MachineSpec transition. Every exec helper emits
      only ToolDone, ToolFailed, or CommandError and is followed by
      parse_structured. Every parsed multi-way report uses explicit
      value_predicate states, each comparing one scalar and emitting exactly
      two configured outcomes plus CommandError. Every compose action emits one
      fixed configured signal. REST signals are only operation success or
      failure signals configured by HTTP status mapping, plus CommandError.
    independent_evaluation:
      - {from: awaiting_request, on: SeedRequest, to: validating_request, action: validate_request, label: critic_request_report_raw}
      - {from: validating_request, on: ToolDone, to: parsing_request_report, action: parse_request, label: critic_request_report}
      - {from: parsing_request_report, on: CriticRequestReportParsed, to: checking_request, action: check_request, label: critic_request_check}
      - {from: checking_request, on: CriticRequestValid, to: composing_voice_query, action: compose_voice_query, label: critic_voice_query}
      - {from: composing_voice_query, on: CriticVoiceQueryReady, to: embedding_voice_query, action: embed_voice_query, label: critic_voice_embedding}
      - {from: embedding_voice_query, on: CriticQueryEmbedded, to: querying_voice_evidence, action: query_voice_evidence, label: critic_voice_retrieval}
      - {from: querying_voice_evidence, on: CriticVoiceEvidenceReturned, to: composing_evaluation_context, action: compose_evaluation_context, label: critic_evaluation_context}
      - {from: composing_evaluation_context, on: CriticEvaluationContextReady, to: evaluating_candidate, action: evaluate_candidate, label: critic_evaluation_raw}
      - {from: evaluating_candidate, on: LLMResponded, to: parsing_evaluation, action: parse_evaluation, label: critic_evaluation}
      - {from: parsing_evaluation, on: CriticEvaluationParsed, to: composing_validation_input, action: compose_validation_input, label: critic_validation_input}
      - {from: composing_validation_input, on: CriticValidationInputReady, to: validating_evaluation, action: validate_evaluation, label: critic_validation_report_raw}
      - {from: validating_evaluation, on: ToolDone, to: parsing_validation_report, action: parse_validation, label: critic_validation_report}
      - {from: parsing_validation_report, on: CriticValidationReportParsed, to: checking_evaluation, action: check_evaluation, label: critic_evaluation_check}
      - {from: checking_evaluation, on: CriticEvaluationValid, to: checking_consent, action: check_consent, label: pangram_consent_check}
    original_scan:
      - {from: checking_consent, on: PangramConsentGranted, to: composing_original_request, action: compose_original_request, label: pangram_original_request}
      - {from: checking_consent, on: PangramConsentNotGranted, to: composing_all_pangram_unavailable, action: compose_consent_unavailable, label: pangram_all_evidence}
      - {from: composing_original_request, on: PangramOriginalRequestReady, to: submitting_original, action: submit_original, label: pangram_original_submission}
      - {from: submitting_original, on: PangramOriginalSubmitted, to: polling_original, action: poll_original, label: pangram_original_poll}
      - {from: submitting_original, on: PangramOriginalDenied/CommandError, to: composing_original_unavailable, action: compose_original_unavailable, label: pangram_original_evidence}
      - {from: polling_original, on: PangramOriginalObserved, to: composing_original_observation_input, action: compose_observation_input, label: pangram_observation_input}
      - {from: polling_original, on: PangramOriginalPollDenied/CommandError, to: composing_original_unavailable, action: compose_original_unavailable, label: pangram_original_evidence}
      - {from: composing_original_observation_input, on: PangramObservationInputReady, to: interpreting_original_observation, action: interpret_observation, label: pangram_observation_raw}
      - {from: interpreting_original_observation, on: ToolDone, to: parsing_original_observation, action: parse_observation, label: pangram_observation}
      - {from: parsing_original_observation, on: PangramObservationParsed, to: checking_original_completed, action: check_observation_completed, label: pangram_original_completed_check}
      - {from: checking_original_completed, on: PangramObservationCompleted, to: composing_original_completed_evidence, action: compose_scan_evidence, label: pangram_original_evidence}
      - {from: composing_original_completed_evidence, on: PangramScanEvidenceReady, to: composing_candidate_request, action: compose_candidate_request, label: pangram_candidate_request}
      - {from: checking_original_completed, on: PangramObservationNotCompleted, to: checking_original_pending, action: check_observation_pending, label: pangram_original_pending_check}
      - {from: checking_original_pending, on: PangramObservationPending, to: checking_original_poll_budget, action: check_poll_budget, label: pangram_original_poll_budget}
      - {from: checking_original_pending, on: PangramObservationFailed, to: composing_original_unavailable, action: compose_original_unavailable, label: pangram_original_evidence}
      - {from: checking_original_poll_budget, on: PangramPollRetryAllowed, to: waiting_original_poll, action: wait_poll, label: pangram_original_poll_delay}
      - {from: waiting_original_poll, on: PangramPollDelayElapsed, to: polling_original, action: poll_original, label: pangram_original_poll}
      - {from: checking_original_poll_budget, on: PangramPollTimedOut, to: composing_original_unavailable, action: compose_original_unavailable, label: pangram_original_evidence}
      - {from: composing_original_unavailable, on: PangramUnavailableEvidenceReady, to: composing_candidate_request, action: compose_candidate_request, label: pangram_candidate_request}
    candidate_scan:
      - {from: composing_candidate_request, on: PangramCandidateRequestReady, to: submitting_candidate, action: submit_candidate, label: pangram_candidate_submission}
      - {from: submitting_candidate, on: PangramCandidateSubmitted, to: polling_candidate, action: poll_candidate, label: pangram_candidate_poll}
      - {from: submitting_candidate, on: PangramCandidateDenied/CommandError, to: composing_candidate_unavailable, action: compose_candidate_unavailable, label: pangram_candidate_evidence}
      - {from: polling_candidate, on: PangramCandidateObserved, to: composing_candidate_observation_input, action: compose_observation_input, label: pangram_observation_input}
      - {from: polling_candidate, on: PangramCandidatePollDenied/CommandError, to: composing_candidate_unavailable, action: compose_candidate_unavailable, label: pangram_candidate_evidence}
      - {from: composing_candidate_observation_input, on: PangramObservationInputReady, to: interpreting_candidate_observation, action: interpret_observation, label: pangram_observation_raw}
      - {from: interpreting_candidate_observation, on: ToolDone, to: parsing_candidate_observation, action: parse_observation, label: pangram_observation}
      - {from: parsing_candidate_observation, on: PangramObservationParsed, to: checking_candidate_completed, action: check_observation_completed, label: pangram_candidate_completed_check}
      - {from: checking_candidate_completed, on: PangramObservationCompleted, to: composing_candidate_completed_evidence, action: compose_scan_evidence, label: pangram_candidate_evidence}
      - {from: composing_candidate_completed_evidence, on: PangramScanEvidenceReady, to: composing_comparison_input, action: compose_comparison_input, label: pangram_comparison_input}
      - {from: checking_candidate_completed, on: PangramObservationNotCompleted, to: checking_candidate_pending, action: check_observation_pending, label: pangram_candidate_pending_check}
      - {from: checking_candidate_pending, on: PangramObservationPending, to: checking_candidate_poll_budget, action: check_poll_budget, label: pangram_candidate_poll_budget}
      - {from: checking_candidate_pending, on: PangramObservationFailed, to: composing_candidate_unavailable, action: compose_candidate_unavailable, label: pangram_candidate_evidence}
      - {from: checking_candidate_poll_budget, on: PangramPollRetryAllowed, to: waiting_candidate_poll, action: wait_poll, label: pangram_candidate_poll_delay}
      - {from: waiting_candidate_poll, on: PangramPollDelayElapsed, to: polling_candidate, action: poll_candidate, label: pangram_candidate_poll}
      - {from: checking_candidate_poll_budget, on: PangramPollTimedOut, to: composing_candidate_unavailable, action: compose_candidate_unavailable, label: pangram_candidate_evidence}
      - {from: composing_candidate_unavailable, on: PangramUnavailableEvidenceReady, to: composing_comparison_input, action: compose_comparison_input, label: pangram_comparison_input}
      - {from: composing_all_pangram_unavailable, on: PangramUnavailableEvidenceReady, to: composing_comparison_input, action: compose_denied_comparison_input, label: pangram_comparison_input}
    advisory_comparison_and_verdict:
      - {from: composing_comparison_input, on: PangramComparisonInputReady, to: comparing_advisory, action: compare_advisory, label: pangram_comparison_raw}
      - {from: comparing_advisory, on: ToolDone, to: parsing_comparison, action: parse_comparison, label: pangram_comparison}
      - {from: comparing_advisory, on: ToolFailed/CommandError, to: composing_comparison_unavailable, action: compose_comparison_unavailable, label: pangram_comparison}
      - {from: parsing_comparison, on: PangramComparisonParsed, to: checking_verdict_pass, action: check_verdict_pass, label: critic_pass_check}
      - {from: parsing_comparison, on: PangramComparisonInvalid/CommandError, to: composing_comparison_unavailable, action: compose_comparison_unavailable, label: pangram_comparison}
      - {from: composing_comparison_unavailable, on: PangramUnavailableEvidenceReady, to: checking_verdict_pass, action: check_verdict_pass, label: critic_pass_check}
      - {from: checking_verdict_pass, on: CriticPassed, to: composing_pass_response, action: compose_pass_response, label: critic_response}
      - {from: checking_verdict_pass, on: CriticNotPassed, to: checking_verdict_warn, action: check_verdict_warn, label: critic_warn_check}
      - {from: checking_verdict_warn, on: CriticWarned, to: composing_warn_response, action: compose_warn_response, label: critic_response}
      - {from: checking_verdict_warn, on: CriticRejected, to: composing_reject_response, action: compose_reject_response, label: critic_response}
      - {from: composing_pass_response, on: CriticResponsePassed, to: passed, action: none}
      - {from: composing_warn_response, on: CriticResponseWarned, to: warned, action: none}
      - {from: composing_reject_response, on: CriticResponseRejected, to: rejected, action: none}
    failure:
      to_failed_response:
        - {from: validating_request, on: ToolFailed/CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: parsing_request_report, on: CriticRequestReportInvalid/CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: checking_request, on: CriticRequestRejected/CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: embedding_voice_query, on: CriticQueryRejected/CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: querying_voice_evidence, on: QueryRejected/CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: evaluating_candidate, on: CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: parsing_evaluation, on: CriticEvaluationInvalid/CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: validating_evaluation, on: ToolFailed/CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: parsing_validation_report, on: CriticValidationReportInvalid/CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: checking_evaluation, on: CriticEvaluationRejected/CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: checking_consent, on: CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: checking_verdict_pass, on: CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
        - {from: checking_verdict_warn, on: CommandError/BudgetExhausted, to: composing_failed_response, action: compose_failed_response, label: critic_response}
      observation_to_explicit_unavailable:
        - {from: interpreting_original_observation, on: ToolFailed/CommandError/BudgetExhausted, to: composing_original_unavailable, action: compose_original_unavailable, label: pangram_original_evidence}
        - {from: parsing_original_observation, on: PangramObservationInvalid/CommandError/BudgetExhausted, to: composing_original_unavailable, action: compose_original_unavailable, label: pangram_original_evidence}
        - {from: checking_original_completed, on: CommandError/BudgetExhausted, to: composing_original_unavailable, action: compose_original_unavailable, label: pangram_original_evidence}
        - {from: checking_original_pending, on: CommandError/BudgetExhausted, to: composing_original_unavailable, action: compose_original_unavailable, label: pangram_original_evidence}
        - {from: checking_original_poll_budget, on: CommandError/BudgetExhausted, to: composing_original_unavailable, action: compose_original_unavailable, label: pangram_original_evidence}
        - {from: waiting_original_poll, on: CommandError/BudgetExhausted, to: composing_original_unavailable, action: compose_original_unavailable, label: pangram_original_evidence}
        - {from: interpreting_candidate_observation, on: ToolFailed/CommandError/BudgetExhausted, to: composing_candidate_unavailable, action: compose_candidate_unavailable, label: pangram_candidate_evidence}
        - {from: parsing_candidate_observation, on: PangramObservationInvalid/CommandError/BudgetExhausted, to: composing_candidate_unavailable, action: compose_candidate_unavailable, label: pangram_candidate_evidence}
        - {from: checking_candidate_completed, on: CommandError/BudgetExhausted, to: composing_candidate_unavailable, action: compose_candidate_unavailable, label: pangram_candidate_evidence}
        - {from: checking_candidate_pending, on: CommandError/BudgetExhausted, to: composing_candidate_unavailable, action: compose_candidate_unavailable, label: pangram_candidate_evidence}
        - {from: checking_candidate_poll_budget, on: CommandError/BudgetExhausted, to: composing_candidate_unavailable, action: compose_candidate_unavailable, label: pangram_candidate_evidence}
        - {from: waiting_candidate_poll, on: CommandError/BudgetExhausted, to: composing_candidate_unavailable, action: compose_candidate_unavailable, label: pangram_candidate_evidence}
      compose_budget_to_failed:
        - All compose states route independently injected BudgetExhausted to failed without invoking another boundary action.
      terminal_response:
        - {from: composing_failed_response, on: CriticResponseFailed, to: failed, action: none}

  factory_constraints:
    exec: |
      validate_critic_request, validate_critic_evaluation,
      interpret_pangram_observation, and compare_pangram_results each fix one
      operation of a planned non-shell JSON helper, fixed stdin limits, fixed
      structured output, no runtime args, and emit only ToolDone, ToolFailed,
      or CommandError. Structured mode wraps stdout under output with exit_code.
    invoke_llm: |
      evaluate_voice_candidate emits only LLMResponded or CommandError. Its raw
      output is not a verdict until parse_critic_evaluation and the fixed
      validator helper succeed.
    rest: |
      Each REST word uses rest_client_invoke. Its non-error signals come only
      from configured HTTP success or failure status mappings. Submit operations
      map POST /task responses; poll operations map GET /task/{task_id}
      responses. REST does not inspect Pangram stage values or invent completed,
      failed, pending, score, or verdict signals. The helper, parser, and scalar
      predicate chain performs stage interpretation.
    parse_structured: |
      Each parser selects one string from an explicit stable label, emits its
      configured parsed or invalid signal, and emits CommandError only when
      selection or normalized output encoding fails.
    value_predicate: |
      Each predicate selects one validated scalar, performs one configured
      comparison, and emits exactly two configured outcomes plus CommandError.
    delay: |
      wait_pangram_poll fixes a positive duration and configured
      PangramPollDelayElapsed signal. It emits only that signal or CommandError
      on cancellation; runtime input cannot select the duration.
    compose: |
      Each compose word has one fixed template and one fixed configured signal.
      Compose does not emit CommandError; planned label validation must prove
      every selector resolves. An unresolved selector is recorded on Result and
      cannot be treated as valid runtime behavior.

structural_proofs:
  critic_independence:
    cut_state: composing_evaluation_context
    argument: |
      The only incoming path follows request validation and a critic-owned
      voice-rag query. The request schema rejects editor reasoning and the
      selected profile exposes no editor conversation, checkpoint, filesystem,
      workproduct, or child-invocation word. The fixed context composer selects
      only validated request and critic retrieval labels. Pangram has not run.
  consent_dominance:
    upload_states: [submitting_original, submitting_candidate]
    dominator: checking_consent
    required_signal: PangramConsentGranted
    argument: |
      Every graph path to either submit state passes through checking_consent.
      The granted signal comes from one scalar comparison over helper-validated
      consent_gate_status. The helper first proves exact bytes and both consent
      hashes match. Non-granted and fault paths have no upload edge.
  advisory_only:
    verdict_source: $from(critic_validation_report).verdict
    prohibited_sources:
      - $from(pangram_original_evidence)
      - $from(pangram_candidate_evidence)
      - $from(pangram_comparison)
    argument: |
      Independent model evaluation, schema parsing, and deterministic validation
      complete before consent routing. Pangram comparison runs afterward. Both
      verdict predicates select only critic_validation_report.verdict. Response
      composers add Pangram evidence after the pass, warn, or reject branch is
      fixed and cannot alter the validated fields.
  responsible_stage_closure:
    allowed_stages: [structure, voice, style]
    argument: |
      The validation helper rejects a reject with zero, null, multiple, or
      unknown responsible stages and rejects pass or warn with a non-null stage.
      critic_verdict_is_warn reaches the reject composer only through the
      schema-closed reject alternative. That composer copies the one validated
      stage. No semantic stage, Pangram stage, critic stage, or free-form stage
      exists.
  bounded_cycles:
    cycles:
      - polling_original -> composing_original_observation_input -> interpreting_original_observation -> parsing_original_observation -> checking_original_completed -> checking_original_pending -> checking_original_poll_budget -> waiting_original_poll -> polling_original
      - polling_candidate -> composing_candidate_observation_input -> interpreting_candidate_observation -> parsing_candidate_observation -> checking_candidate_completed -> checking_candidate_pending -> checking_candidate_poll_budget -> waiting_candidate_poll -> polling_candidate
    argument: |
      The helper validates and increments one poll count. The budget predicate
      permits re-entry only below sixty. At the limit it emits timed_out and
      leaves the cycle through explicit unavailable evidence. Machine iteration,
      duration, and token limits remain independent finite bounds.
  authority_closure:
    argument: |
      Every action maps to one voice-critic-owned srd003 word. The profile has
      one read-only voice-rag path, bounded model inference, four fixed helper
      operations, deterministic parse/predicate/compose words, and configured
      Pangram REST operations. It has no editing, filesystem, workproduct,
      GitHub, child, lifecycle, corpus-write, generic shell, or arbitrary REST
      word. The helper has no shell or runtime operation selector.

requirements:
  R1:
    title: Flat machine and exact factory semantics
    items:
      - id: R1.1
        text: |
          The planned voice-critic MachineSpec SHALL use exactly the states,
          signals, labels, transitions, actions, budgets, and four terminals in
          flat_machine_contract. It SHALL contain no nested machine, payload
          guard, dynamic dispatch, script, shell, or hidden router.
        evidence: Planned graph expansion reports no duplicate, missing, unreachable, unused, or terminal-origin transition.
      - id: R1.2
        text: |
          Every action SHALL map to one critic-owned srd003 word. Exec,
          invoke_llm, REST, parse_structured, value_predicate, delay, and compose
          words SHALL retain the exact factory behavior in factory_constraints.
        evidence: Planned factory, action, word, signal, transition, terminal, label, and authority closure compares declarations with agent-core code and builtin declarations.

  R2:
    title: Immutable request and critic independence
    items:
      - id: R2.1
        text: |
          validate_critic_request SHALL accept only the immutable original and
          selected style candidate bytes, artifact identities, exact hashes,
          target voice, bounded policy, invocation identity, and consent record.
          It SHALL reject path-bearing, authority-bearing, or editor-reasoning
          fields and every byte/hash or stage mismatch.
        evidence: Planned request fixtures cover valid identities, changed bytes, wrong hashes, wrong stage, path, endpoint, credential, tool, editor prompt, reasoning, conversation, confidence, and verdict recommendation fields.
      - id: R2.2
        text: |
          The critic SHALL formulate and embed its own bounded voice comparison
          query and query only voice-rag read-only. It SHALL not reuse the voice
          editor's selected anchors, hidden reasoning, conversation, or
          self-assessment.
        evidence: Planned provenance and negative-selector tests prove structural_proofs.critic_independence.

  R3:
    title: Complete independent assessment
    items:
      - id: R3.1
        text: |
          evaluate_voice_candidate SHALL receive a fixed context containing the
          exact original and candidate, both hashes, bounded policy, target
          voice, and independently retrieved voice passages. It SHALL receive no
          Pangram result or editor reasoning.
        evidence: Planned context snapshots compare allowed and prohibited fields and prove Pangram states are unreachable before evaluation validation.
      - id: R3.2
        text: |
          Every completed assessment SHALL contain semantic_preservation,
          structural_intent, voice_match, tightening_quality,
          unsupported_additions, and anchor_copy_risk findings with bounded
          evidence and feedback.
        evidence: Planned schema and adversarial fixtures reject every omitted, duplicated, unknown, unbounded, or hash-mismatched category.
      - id: R3.3
        text: |
          Raw model output SHALL emit only LLMResponded or CommandError and
          SHALL pass through parse_critic_evaluation, a fixed non-shell
          cross-field validation helper, parse_critic_validation_report, and one
          evaluation_status predicate before any verdict or Pangram route.
        evidence: Planned traces reject direct model verdict signals, malformed JSON, hash mismatch, category gaps, bad verdict-stage relations, and helper failure.

  R4:
    title: Verdict and responsible stage
    items:
      - id: R4.1
        text: |
          A completed response SHALL contain exactly one verdict in pass, warn,
          or reject. Pass and warn SHALL contain no responsible stage. Every
          reject SHALL contain exactly one responsible_stage in structure,
          voice, or style and bounded actionable feedback for that stage.
        evidence: Planned schema and route fixtures cover each verdict and zero, null, multiple, unknown, semantic, critic, and Pangram stage values.
      - id: R4.2
        text: |
          Semantic or unsupported-addition findings SHALL not create a semantic
          editor. A reject SHALL assign the defect to the responsible structure,
          voice, or style operation under assessment_contract.
        evidence: Planned fixtures assign semantic failures to each allowed editor stage and prove no fourth retry route.
      - id: R4.3
        text: |
          Separate fixed pass, warn, reject, and failed response composers SHALL
          each emit one configured response signal. Failed SHALL carry no
          verdict and SHALL never be parsed by the orchestrator as reject.
        evidence: Planned response and orchestrator compatibility tests validate exact response schema, signals, hashes, stage cardinality, and failed transport behavior.

  R5:
    title: Exact-hash Pangram consent
    items:
      - id: R5.1
        text: |
          No Pangram submit action SHALL be reachable unless the request helper
          validates decision granted against the exact original and candidate
          hashes, disclosure identity, and expiry, and check_pangram_consent
          emits PangramConsentGranted.
        evidence: Planned graph dominance and negative tests cover granted, denied, absent, expired, malformed, and either-hash-mismatched consent.
      - id: R5.2
        text: |
          API key presence, endpoint reachability, prior consent, model text,
          retry state, or environment configuration SHALL NOT count as consent.
          A non-granted status SHALL create explicit advisory evidence without
          invoking either submit or poll word.
        evidence: Planned call-count and trace tests report zero Pangram network operations for every non-granted case.

  R6:
    title: Separate Pangram scans and comparison
    items:
      - id: R6.1
        text: |
          The critic SHALL submit and poll one task for the original and a
          separate task for the candidate. It SHALL bind each task, response,
          advisory record, and error to that document kind and exact hash and
          SHALL never reuse one task identity for the other.
        evidence: Planned fixtures compare task IDs, request bodies, hashes, statuses, and response labels across both scans.
      - id: R6.2
        text: |
          REST submit and poll words SHALL emit only configured HTTP
          status-mapping signals or CommandError. They SHALL NOT emit task-stage,
          score, completed, failed, pending, verdict, or responsible-stage
          signals. A fixed helper, parse_structured, and one-scalar predicate
          chain SHALL interpret each HTTP 200 poll body.
        evidence: Planned factory-semantic tests compare REST declarations with rest-tool-format and internal/tools/rest response mapping code, including STAGE_FAILED HTTP 200 and pending HTTP 200 fixtures.
      - id: R6.3
        text: |
          compare_pangram_results SHALL compare separate document fractions,
          segment counts, and available paragraph-window evidence and SHALL
          preserve explicit unavailable statuses. It SHALL not reduce the
          provider response to one AI score.
        evidence: Planned completed/completed, completed/failed, failed/completed, both-unavailable, changed-paragraph-count, and malformed-comparison fixtures validate the advisory schema.

  R7:
    title: Advisory-only and failure behavior
    items:
      - id: R7.1
        text: |
          Pangram score, prediction, fraction, segment count, window, movement,
          failure, or absence SHALL NOT alone produce or change pass, warn,
          reject, or responsible_stage. Verdict predicates SHALL read only the
          independently validated evaluation report.
        evidence: Planned data-flow and mutation tests prove structural_proofs.advisory_only and hold verdict output constant while varying every Pangram field.
      - id: R7.2
        text: |
          Denied, absent, invalid, unavailable, rejected, failed, timed-out, or
          malformed Pangram evidence SHALL remain explicit in the response and
          SHALL never be converted to a favorable default or silent acceptance.
        evidence: Planned failure fixtures assert exact advisory status and no fabricated zero score, Human Written label, or completed status.
      - id: R7.3
        text: |
          Original and candidate uploads SHALL be irreversible observations.
          Retry and rollback SHALL not claim to erase submitted text or provider
          tasks. Request identity and bounded redacted status SHALL be retained.
        evidence: Planned receipt and rollback tests report none_for_upload and never issue remote compensation.

  R8:
    title: Bounded execution and authority exclusion
    items:
      - id: R8.1
        text: |
          Each Pangram poll cycle SHALL be bounded by a helper-validated count,
          one scalar budget predicate, machine iterations, and machine duration.
          Exhaustion SHALL produce explicit timed_out advisory evidence and
          continue to the independent verdict route.
        evidence: Planned boundary traces prove both cycles terminate at the hard limit and preserve the validated verdict.
      - id: R8.2
        text: |
          voice-critic SHALL expose no editing, replacement-content,
          filesystem, workproduct, manifest, GitHub, child-agent, lifecycle,
          corpus-write, generic shell, or arbitrary REST authority.
        evidence: Planned profile, action, word, field, and side-effect closure proves structural_proofs.authority_closure.
      - id: R8.3
        text: |
          This SRD SHALL remain specified while the profile, MachineSpec,
          ToolDefs, non-shell helper, REST definitions, service, tests, and
          executable evidence remain planned.
        evidence: Architecture, specification index, roadmap, and implementation_status agree.

  R9:
    title: Validation gates
    items:
      - id: R9.1
        text: |
          Planned validation SHALL parse YAML; prove complete requirement-to-AC
          traces; prove exact factory semantics; prove action, word, signal,
          transition, terminal, label, and authority closure; prove consent
          dominance, critic independence, advisory-only data flow,
          responsible-stage closure, and bounded cycles; inspect the diff; and
          run mage audit.
        evidence: The future test suite names every check and reports no new warning.

non_goals:
  - Implement agents/voice-critic/machine.yaml, a profile, ToolDefs, helper executable, REST definitions, service, or tests.
  - Edit, rewrite, repair, or return replacement document prose.
  - Read or write the saga filesystem, manifest, workproducts, GitHub repository, or publication.
  - Receive editor reasoning, conversation, prompts, rejected alternatives, or verdict recommendations.
  - Query structure-rag or tightening-rag, write Chroma, ingest a corpus, or alter canonical RAG behavior.
  - Add a shell, new Go builtin, application-local REST adapter, generic URL, runtime executable, helper operation, credential, model, corpus, path, ref, or tool selection.
  - Treat Pangram, a detector score, a lexical denylist, model confidence, or voice-rag distance as a verdict.
  - Claim an unavailable Pangram result is favorable or that an upload can be undone.
  - Add a semantic editor, critic retry stage, Pangram retry stage, or free-form responsible stage.

acceptance_criteria:
  - id: AC1
    criterion: |
      One flat signal-complete machine maps every action to a critic-owned
      srd003 word, matches actual exec, invoke_llm, REST, parse_structured,
      value_predicate, delay, and compose emissions, and closes four terminal
      outcomes.
    traces: [R1.1, R1.2]
  - id: AC2
    criterion: |
      Only exact immutable original and style-candidate bytes, hashes,
      identities, bounded policy, and consent enter; editor reasoning and
      authority-bearing fields are rejected; the critic retrieves its own
      read-only voice evidence.
    traces: [R2.1, R2.2]
  - id: AC3
    criterion: |
      Independent evaluation precedes Pangram and contains all six finding
      categories; raw LLM output is parsed and deterministically validated
      before any verdict or consent route.
    traces: [R3.1, R3.2, R3.3]
  - id: AC4
    criterion: |
      Completed responses contain pass, warn, or reject; every reject has
      exactly one structure, voice, or style responsible stage and bounded
      feedback; failed responses have no verdict.
    traces: [R4.1, R4.2, R4.3]
  - id: AC5
    criterion: |
      Graph dominance proves neither Pangram upload is reachable without
      affirmative unexpired consent bound to both exact hashes, and every
      non-granted case performs zero Pangram calls.
    traces: [R5.1, R5.2]
  - id: AC6
    criterion: |
      Original and candidate use separate submit and poll tasks; REST emits only
      configured HTTP outcomes; helper, parser, and scalar chains interpret task
      stages; comparison preserves fractions, segments, windows, and explicit
      advisory status without inventing one AI score.
    traces: [R6.1, R6.2, R6.3]
  - id: AC7
    criterion: |
      Data-flow proof shows Pangram cannot decide or alter verdict or responsible
      stage; unavailable or denied service remains explicit and never silently
      accepts; irreversible upload effects are reported honestly.
    traces: [R7.1, R7.2, R7.3]
  - id: AC8
    criterion: |
      Poll and machine cycles are finite, and profile closure denies editing,
      replacement content, filesystem, workproduct, GitHub, lifecycle,
      corpus-write, shell, and arbitrary REST authority.
    traces: [R8.1, R8.2]
  - id: AC9
    criterion: |
      Documentation reports runtime as planned, and YAML, complete AC traces,
      exact factory semantics, all closure checks, consent dominance, critic
      independence, advisory-only flow, responsible-stage closure, bounded
      cycles, diff inspection, and mage audit remain validation gates.
    traces: [R8.3, R9.1]

shared_requirement_traces:
  - contract: srd001-prose-editor-services
    path: srd001-prose-editor-services.yaml
    requirements: [R2.1, R2.2, R4.1, R5.1, R7.1, R8.2]
  - contract: srd002-writing-saga-workproducts
    path: srd002-writing-saga-workproducts.yaml
    requirements: [R2.1, R4.1, R4.3, R5.1, R7.3]
  - contract: srd003-writing-agent-tools
    path: srd003-writing-agent-tools.yaml
    requirements: [R1.2, R2.1, R2.2, R3.1, R3.3, R4.3, R5.1, R6.1, R6.2, R6.3, R7.3, R8.2]
  - contract: srd004-workflow-orchestrator
    path: srd004-workflow-orchestrator.yaml
    requirements: [R4.1, R4.2, R4.3]
  - contract: cfg-machine-format
    path: ../../../../../agent-core/docs/specs/config-formats/machine-format.yaml
    requirements: [R1.1, R1.2, R8.1, R9.1]
  - contract: cfg-rest-tool-format
    path: ../../../../../agent-core/docs/specs/config-formats/rest-tool-format.yaml
    requirements: [R5.1, R6.1, R6.2, R6.3, R7.3]
  - contract: srd001-rag-server-agent
    path: ../../../../chatbot-mesh/docs/specs/software-requirements/srd001-rag-server-agent.yaml
    requirements: [R2.2]

use_case_traces:
  - id: rel00.0-uc001-edit-document
    status: planned
    path: ../use-cases/rel00.0-uc001-edit-document.yaml
  - id: rel00.1-uc001-tracer-saga
    status: implemented
    path: ../use-cases/rel00.1-uc001-tracer-saga.yaml

test_suite_traces:
  - id: test-rel00.0-prose-editor
    status: planned
    path: ../test-suites/test-rel00.0-prose-editor.yaml
    acceptance_criteria: [AC1, AC2, AC3, AC4, AC5, AC6, AC7, AC8, AC9]
  - id: test-rel00.1-prose-editor
    status: implemented
    path: ../test-suites/test-rel00.1-prose-editor.yaml
    acceptance_criteria: [AC1, AC2, AC3, AC4]

related_documents:
  - path: ../../ARCHITECTURE.yaml
    usage: Defines the critic assurance flow and Pangram privacy boundary.
  - path: srd001-prose-editor-services.yaml
    usage: Owns critic responsibility, lifecycle, scaling, failure, and authority.
  - path: srd002-writing-saga-workproducts.yaml
    usage: Owns immutable inputs, critique lineage, persistence, and orchestrator retry behavior.
  - path: srd003-writing-agent-tools.yaml
    usage: Owns every selected word, exact factory emission, effect, error, compensation, and authority.
  - path: srd004-workflow-orchestrator.yaml
    usage: Owns critic invocation, child-response parsing, persistence, and responsible-stage replay.
  - path: ../../../../../agent-core/docs/specs/config-formats/machine-format.yaml
    usage: Owns flat states, signals, transitions, actions, labels, terminals, budgets, and validation.
  - path: ../../../../../agent-core/docs/specs/config-formats/rest-tool-format.yaml
    usage: Owns configured REST HTTP status mappings, request authority, limits, response mapping, and errors.
  - path: ../../../../../agent-core/docs/constitutions/design.yaml
    usage: Requires YAML, numbered requirements, traceability, and tool-language boundaries.
  - external: petar-djukic/coding-skills/.claude/skills/filter-tells/SKILL.md
    usage: Establishes that Pangram uploads require per-document consent and remain external evidence rather than a verdict.
  - external: petar-djukic/coding-skills/.claude/scripts/pangram.py
    usage: Establishes POST /task, GET /task/{id}, terminal stage handling, documented HTTP errors, retained dashboard identity, and three document fractions.
  - external: petar-djukic/coding-skills/.claude/scripts/pangram_report.py
    usage: Establishes prose-span mapping, separate baseline/current comparison, paragraph-count caveat, fraction and segment deltas, and advisory thresholds.

implementation_status:
  specification: specified
  runtime: release_00_1_no_pangram_deterministic_tracer
  profile: release_00_1_implemented
  machine: release_00_1_independent_critic_implemented
  tool_declarations: release_00_1_subset_implemented
  critic_helper: not_implemented
  rest_definitions: not_implemented
  model_declarations: release_00_1_implemented
  service: not_implemented
  tests: structural_deterministic_responses_and_fault_injection
  executable_evidence: deterministic_tracer
