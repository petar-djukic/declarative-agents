# Copyright (c) 2026 Nokia
# SPDX-License-Identifier: BSD-3-Clause
name: critic-session
purpose: |-
  The critic uses a two-level nested state machine architecture.

  The session-level machine (agents/critic/machine.yaml) iterates
  over profile-resolved evaluation points defined in a suite YAML:
  selected profile, sample, grid point, repetition, and any legacy harness
  metadata retained by compatibility suites. For each point it invokes a
  nested core.Loop that runs the per-point pipeline.

  The point-level machine (agents/critic/point.yaml) orchestrates
  a single evaluation point: it creates a point directory, copies the
  sample workspace/docs, initializes and commits a git baseline, snapshots
  the experiment configuration, invokes the generator agent as a subprocess,
  checks the results, and collects metrics. It is a deterministic
  pipeline that sequences atomic tools — no LLM calls.

  The former `agent eval` subcommand has been removed. Evaluation is
  now normally driven by `--profile agents/critic/profile.yaml`; direct
  `--machine`/`--tools` invocation remains compatibility-only startup input.

  This file is the critic session-level machine.
configuration:
  machine: agents/critic/machine.yaml
  point_machine: agents/critic/point.yaml
  session_tools: agents/critic/tools.yaml
  point_tools: agents/critic/tools-point.yaml
invariants:
- The critic never calls an LLM directly
- All agent outcomes (success, failure, timeout) proceed to run_oracle_check
- Oracle failures are domain results and still proceed through trace, version, summary, and metrics collection
- The workspace is a fresh temporary directory for each evaluation point
- Metrics are collected even when the agent fails or times out
- The session machine declares sequential for_each iteration over all materialized evaluation points
- Session and point machines have separate tool selections
- run_point builds the nested point registry from agents/critic/tools-point.yaml
lifecycle: |-
  1. Idle → ParsingSuiteConfig: parse_suite_config
  2. ParsingSuiteConfig → DiscoveringSuiteSamples: discover_suite_samples
  3. DiscoveringSuiteSamples → ExpandingEvalGrid: expand_eval_grid
  4. ExpandingEvalGrid → InitializingEvalSession: init_eval_session
  5. InitializingEvalSession → ReportingSuiteSummary: report_suite_summary
  6. ReportingSuiteSummary → MaterializingPoints: materialize_eval_points
  7. MaterializingPoints → RunningPoints: run_point for_each point (sequential, fail-fast)
  8. RunningPoints → Reporting: report_session (after iterator join)
  9. Reporting → Done
pipeline_diagram: |-
  ```mermaid
  stateDiagram-v2
    [*] --> Idle
    Idle --> ParsingSuiteConfig: Seed / parse_suite_config
    ParsingSuiteConfig --> DiscoveringSuiteSamples: SuiteConfigParsed / discover_suite_samples
    DiscoveringSuiteSamples --> ExpandingEvalGrid: SuiteSamplesDiscovered / expand_eval_grid
    ExpandingEvalGrid --> InitializingEvalSession: EvalGridExpanded / init_eval_session
    InitializingEvalSession --> ReportingSuiteSummary: EvalSessionInitialized / report_suite_summary
    ReportingSuiteSummary --> MaterializingPoints: SuiteLoaded / materialize_eval_points
    MaterializingPoints --> RunningPoints: EvalPointsMaterialized / run_point for_each
    RunningPoints --> Reporting: PointsCompleted / report_session
    Reporting --> Done: SessionReported
    ParsingSuiteConfig --> Failed: CommandError
    DiscoveringSuiteSamples --> Failed: CommandError
    ExpandingEvalGrid --> Failed: CommandError
    InitializingEvalSession --> Failed: CommandError
    ReportingSuiteSummary --> Failed: CommandError
    MaterializingPoints --> Failed: CommandError
    RunningPoints --> Failed: CommandError
    Reporting --> Failed: CommandError
  ```
initial_state: Idle
budget:
  command_timeout: 16m
  max_iterations: 10000
states:
- name: Idle
  meaning: Initial state before the suite is loaded.
- name: ParsingSuiteConfig
  meaning: Reading and validating suite YAML metadata.
- name: DiscoveringSuiteSamples
  meaning: Discovering samples from the resolved suite samples directory.
- name: ExpandingEvalGrid
  meaning: Expanding suite grid parameters into concrete grid points.
- name: InitializingEvalSession
  meaning: Creating the session output directory and resolving runtime defaults.
- name: ReportingSuiteSummary
  meaning: Reporting suite point counts before iteration begins.
- name: MaterializingPoints
  meaning: Materializing deterministic profile, grid, sample, and repetition combinations.
- name: RunningPoints
  meaning: Sequentially dispatching each point through run_point with MachineSpec for_each.
- name: Reporting
  meaning: Printing session summary (pass/fail/timeout counts, total duration).
- name: Done
  run_status: succeeded
  meaning: Terminal. All points evaluated and reported.
- name: Failed
  run_status: failed
  meaning: Terminal. Infrastructure error prevented evaluation.
terminal_states:
- Done
- Failed
signals:
- name: Seed
  trigger: Loop initialization.
- name: CommandError
  trigger: Infrastructure-level error.
- name: SuiteConfigParsed
  trigger: Suite YAML metadata parsed and validated.
- name: SuiteSamplesDiscovered
  trigger: Suite samples discovered.
- name: EvalGridExpanded
  trigger: Suite grid expanded into concrete grid points.
- name: EvalSessionInitialized
  trigger: Session output directory and runtime defaults initialized.
- name: SuiteLoaded
  trigger: Suite setup summary reported.
- name: EvalPointsMaterialized
  trigger: The deterministic point collection is available in command state.
- name: PointDone
  trigger: Per-point pipeline completed (any outcome).
- name: PointsCompleted
  trigger: The declared point iterator joined after all items completed.
- name: SessionReported
  trigger: Summary printed.
transitions:
- state: Idle
  signal: Seed
  next: ParsingSuiteConfig
  action: parse_suite_config
- state: ParsingSuiteConfig
  signal: SuiteConfigParsed
  next: DiscoveringSuiteSamples
  action: discover_suite_samples
- state: DiscoveringSuiteSamples
  signal: SuiteSamplesDiscovered
  next: ExpandingEvalGrid
  action: expand_eval_grid
- state: ExpandingEvalGrid
  signal: EvalGridExpanded
  next: InitializingEvalSession
  action: init_eval_session
- state: InitializingEvalSession
  signal: EvalSessionInitialized
  next: ReportingSuiteSummary
  action: report_suite_summary
- state: ReportingSuiteSummary
  signal: SuiteLoaded
  next: MaterializingPoints
  action: materialize_eval_points
  label: eval_points
- state: MaterializingPoints
  signal: EvalPointsMaterialized
  next: RunningPoints
  action: run_point
  label: point_result
  for_each:
    items: $from(eval_points).points
    as: point
    mode: sequential
    failure: fail_fast
    continue_on: [PointDone]
    abort_on: [CommandError]
    join:
      next: RunningPoints
      label: point_results
      signals:
        all_success: PointsCompleted
        failed: CommandError
        empty: PointsCompleted
- state: RunningPoints
  signal: PointsCompleted
  next: Reporting
  action: report_session
- state: Reporting
  signal: SessionReported
  next: Done
- state: ParsingSuiteConfig
  signal: CommandError
  next: Failed
- state: DiscoveringSuiteSamples
  signal: CommandError
  next: Failed
- state: ExpandingEvalGrid
  signal: CommandError
  next: Failed
- state: InitializingEvalSession
  signal: CommandError
  next: Failed
- state: ReportingSuiteSummary
  signal: CommandError
  next: Failed
- state: MaterializingPoints
  signal: CommandError
  next: Failed
- state: RunningPoints
  signal: CommandError
  next: Failed
- state: Reporting
  signal: CommandError
  next: Failed
