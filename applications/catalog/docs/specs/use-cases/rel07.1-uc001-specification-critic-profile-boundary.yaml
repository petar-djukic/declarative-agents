# Copyright (c) 2026 Nokia
# SPDX-License-Identifier: BSD-3-Clause
id: rel07.1-uc001-specification-critic-profile-boundary
title: Specification Critic Profile Boundary
historical_aliases: [rel07.1-uc001-jurist-profile-boundary]

summary: |
  The specification-critic profile proves a deterministic, LLM-free specification validator
  boundary. A maintainer points the profile at a target directory and one or
  more charter suites, and the machine runs explicit load, validate, rg search,
  finding reduction, and report steps. The run produces a provenance-rich
  validation report and a Passed or Failed terminal state.

  The proof belongs in applications/catalog because the charter suites, the word
  selection in agents/specification-critic/machine.yaml, and the tool selection in
  agents/specification-critic/tools.yaml are profile-owned. agent-core supplies the
  specification validation tools, but it should not encode the charter suites or
  the target directory as core-owned assets.

actor: Profile maintainer verifying specification-critic charter validation behavior.

trigger: |
  A maintainer runs the specification-critic profile against a target directory and a charter
  suite:

    agent --profile "agents/specification-critic/profile.yaml" \
          --directory <target-directory>

preconditions:
  - agents/specification-critic/profile.yaml validates and selects agents/specification-critic/machine.yaml and agents/specification-critic/tools.yaml.
  - agents/specification-critic/tools.yaml selects load_corpus, validate_specs, jurist_rg, reduce_grep_checks, and format_report with no LLM tool family.
  - rg is available on PATH, as required by the existing filesystem find capability.
  - agents/specification-critic/suites/demo-charter.yaml supplies a deterministic charter suite over the target directory.

flow:
  - id: F1
    text: The maintainer resolves the specification-critic profile from the demo.yaml catalog_root or the repository checkout without developer-specific absolute paths.
  - id: F2
    text: The machine loads the specification corpus from the target directory in the Loading state via load_corpus.
  - id: F3
    text: The machine validates non-grep charter checks, iterates grep_check plans through the declared jurist_rg executable word, and visibly reduces rg events into findings.
  - id: F4
    text: The machine formats a report in the Reporting state via format_report for both pass and fail outcomes.
  - id: F5
    text: The machine reaches the Passed or Failed terminal state, and findings identify suite, check, severity, message, and any file and line provenance.
  - id: F6
    text: Audit gates select audit-profile.yaml, whose machine declares Go module, package, test-list, and full test-run exec steps, resolves formal claims, reduces pass, fail, skip, and did-not-run events, and reports through format_report. The catalog gate stages a catalog-local executable binding that preserves the same Go JSON inventory and execution streams while running conformance from a unique stable test-binary path; the generic profile remains reusable by other modules.

touchpoints:
  - id: T1
    target: srd005-specification-critic R1
    reason: The specification-critic profile models deterministic, LLM-free, read-only corpus validation.
  - id: T2
    target: srd005-specification-critic R2
    reason: Specification-critic inputs, the specification validation tool family, and corpus, graph, consistency, and report responsibilities are profile-owned.
  - id: T3
    target: srd005-specification-critic R3
    reason: Specification-critic outputs are a formatted report, provenance-rich findings, and a Passed or Failed status.
  - id: T4
    target: sm-agent-role-taxonomy specification-critic
    reason: The role entry names the specification-critic actor boundary, inputs, selected tool family, outputs, and checks.
  - id: T5
    target: test-rel07.1-profile-boundaries specification_critic_profile_boundary
    reason: The suite records the profile, machine word selection, charter suite, and expected report shape.

success_criteria:
  - id: S1
    criterion: The specification-critic profile selects load_corpus, validate_specs, jurist_rg, reduce_grep_checks, and format_report and selects no LLM tool family.
    traces:
      - srd005-specification-critic R1.1
      - srd005-specification-critic R2.2
      - sm-agent-role-taxonomy specification-critic checks
  - id: S2
    criterion: Charter checks run deterministically and read-only over the target directory across repeated runs, with grep_check search and reduction visible in the machine trace.
    traces:
      - srd005-specification-critic R1.2
      - srd005-specification-critic R2.3
  - id: S3
    criterion: Both pass and fail outcomes produce a formatted report whose findings identify suite, check, severity, message, and available file and line provenance.
    traces:
      - srd005-specification-critic R3.1
      - srd005-specification-critic R3.2
      - srd005-specification-critic R3.3
  - id: S4
    criterion: Core, profile, and example audit gates invoke the specification-critic audit profile; the agent CLI has no test-evidence mode and pkg/spec performs only deterministic reduction.
    traces:
      - srd005-specification-critic R2.5
      - srd005-specification-critic R3.1

out_of_scope:
  - Adding an LLM boundary or network access to charter validation.
  - Defining Go package APIs for the agent-core specification validation tools.
  - Changing charter check semantics during this documentation update.
  - Renaming the Jurist charter format or jurist_rg tool word, which remain stable capability vocabulary.

test_suite_trace: test-rel07.1-profile-boundaries
status: specified
